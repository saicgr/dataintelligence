import type { ConvItem } from "./types";

/**
 * Additional code-review practice items spanning SQL, PySpark, Python and
 * TypeScript. Faults are grounded in real docs/behavior (see PR notes); each is
 * a prose-review question graded by the AI interviewer against the rubric.
 * GENERATED from scripts/codereview_extra.json — edit that + re-run the codegen.
 */
export const CODEREVIEW_EXTRA_ITEMS: ConvItem[] = [
  {
    id: "cr-pyspark-multi-fault-perf",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "senior",
    title: "PySpark: Find all faults and make this performant",
    company: "Databricks Onsite · Data Engineering Technical Round",
    difficulty: "hard",
    prompt: "Review this PySpark ETL job that joins a large events table with a small country-lookup table, filters for a specific region, and writes the result. List what you'd change and why; you don't need to rewrite the code.\n\n```python\nfrom spark.sql import SparkSession\nfrom pyspark.sql.functions import col\n\nspark = SparkSession.builder \\\n    .appName(\"EventsEnrichment\") \\\n    .getOrCreate()\n\nevents_df   = spark.read.parquet(\"s3://data-lake/events/\")\ncountry_df  = spark.read.parquet(\"s3://data-lake/country_lookup/\")  # ~200 KB, 250 rows\n\n# Enrich events with country name\nenriched_df = events_df.join(country_df)\n\n# Keep only EMEA traffic\nfiltered_df = enriched_df.filter(col(\"region\") == \"EMEA\")\n\nfiltered_df.show(20)\n\ntotal = filtered_df.count()\nprint(f\"Total EMEA events: {total}\")\n\nfiltered_df.repartition(1).write.mode(\"overwrite\").parquet(\"s3://data-lake/output/emea_events/\")\n```",
    hints: ["The very first line of the file will prevent this script from running at all — look at the import path carefully.", "The join has no condition specified. What does Spark do when you call `.join()` with no `on=` argument on two DataFrames? What is the output cardinality, and why is it catastrophic here?", "Think about what happens to executor heap memory when you force hundreds of millions of EMEA rows into a single partition just before writing. Then think about `show()` and `count()` both firing without a `cache()` — how many times does the full pipeline run?"],
    starter: "",
    idealAnswer: "**1. Wrong import — `from spark.sql import SparkSession` (NameError at startup)**\n`spark` is not a Python package; it is the variable name conventionally given to a `SparkSession` instance. The correct import is `from pyspark.sql import SparkSession`. This line raises `ModuleNotFoundError` immediately and the script never starts.\n\n**2. Cartesian / cross join — no join key specified**\n`events_df.join(country_df)` with no `on=` argument produces a cross join: every row in `events_df` is paired with every row in `country_df`. With 200 million events × 250 country rows = **50 billion output rows** before the filter even runs. This will overwhelm every executor with a full shuffle and almost certainly OOM the cluster. Fix: `events_df.join(country_df, on=\"country_code\", how=\"left\")`.\n\n**3. Filter applied after the join — no predicate pushdown**\nThe `filter(col(\"region\") == \"EMEA\")` is placed after the join, so the join must process the entire events dataset before any rows are discarded. Applying the filter to `events_df` *before* the join (or at minimum relying on the Catalyst optimizer pushing it down, which it cannot reliably do across a cross join) dramatically reduces the shuffle volume. Fix: `events_df = events_df.filter(col(\"region\") == \"EMEA\")` before the join.\n\n**4. Small dimension table not broadcast**\n`country_df` is ~200 KB (250 rows), far below the default `spark.sql.autoBroadcastJoinThreshold` of 10 MB. Even after fixing the join key, Spark may default to a sort-merge join. Using `broadcast()` guarantees a map-side join with zero shuffle on the large table: `from pyspark.sql.functions import broadcast` → `events_df.join(broadcast(country_df), on=\"country_code\")`.\n\n**5. Two actions without `cache()` — full lineage recomputed twice**\n`filtered_df.show(20)` and `filtered_df.count()` are two separate actions. Because `filtered_df` is not cached, Spark recomputes the entire DAG — re-reading S3, re-joining, re-filtering — for each action. Fix: `filtered_df.cache()` (or `.persist()`) after building the DataFrame and before the first action, then `filtered_df.unpersist()` when done.\n\n**6. `repartition(1)` before write → executor `java.lang.OutOfMemoryError: Java heap space`**\n`repartition(1)` triggers a full shuffle that funnels all data into a single partition on one executor. For a large EMEA dataset (potentially tens of GB after the cross-join fiasco is fixed), this single task must hold the entire dataset in heap memory. When it exceeds the executor's configured heap (`spark.executor.memory`), the JVM throws `java.lang.OutOfMemoryError: Java heap space` and the task fails. Fix: remove `repartition(1)` entirely and let Spark write a sensibly partitioned Parquet output. If a small file count is needed, use `coalesce(N)` with a reasonable N (e.g., target 128–256 MB per part file).",
    rubric: ["Identifies the wrong import module (`spark.sql` vs `pyspark.sql`) as a hard runtime error, not just a style issue", "Correctly names the cross/cartesian join, explains the O(M×N) row explosion, and gives the right fix (`on=` parameter with correct join type)", "Explains predicate pushdown: filter should precede or be pushed into the join to reduce shuffle data volume", "Recommends `broadcast()` hint for the 200 KB dimension table and explains why it eliminates the shuffle on the large table", "Notes that two actions without `cache()` recompute the full lineage twice and proposes `cache()` / `persist()` before the first action", "Explains the OOM root cause: `repartition(1)` collapses all data onto one executor task, exhausting JVM heap; recommends removing it or using `coalesce(N)` with a target partition size"],
  },
  {
    id: "cr-pyspark-collect-udf-2",
    category: "codereview",
    executes: false,
    mode: "text",
    free: true,
    level: "junior",
    title: "PySpark: Driver OOM and Python UDF overhead",
    company: "Snowflake · Data Engineering Phone Screen",
    difficulty: "easy",
    prompt: "Review this PySpark job that reads a large web-clickstream table, normalises a URL column, and reports the top-10 domains. Identify any correctness or performance problems.\n\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import udf, desc\nfrom pyspark.sql.types import StringType\nimport re\n\nspark = SparkSession.builder.appName(\"ClickstreamAnalysis\").getOrCreate()\n\nclickstream_df = spark.read.parquet(\"s3://data-lake/clickstream/\")  # ~500 GB, 4 billion rows\n\n# Extract domain from URL\ndef extract_domain(url):\n    if url is None:\n        return None\n    match = re.search(r\"https?://([^/]+)\", url)\n    return match.group(1) if match else None\n\ndomain_udf = udf(extract_domain, StringType())\n\nenriched_df = clickstream_df.withColumn(\"domain\", domain_udf(clickstream_df[\"url\"]))\n\ndomain_counts = enriched_df.groupBy(\"domain\").count().orderBy(desc(\"count\"))\n\n# Collect all domain counts for post-processing in Python\nall_results = domain_counts.collect()\n\ntop10 = all_results[:10]\nfor row in top10:\n    print(row[\"domain\"], row[\"count\"])\n```",
    hints: ["The UDF runs Python logic row-by-row via JVM serialisation. Is there a Spark-native function that already does what `extract_domain` does?", "What happens when `domain_counts.collect()` is called on a dataset that could have millions of distinct domains? Where does all that data go?", "Even if `collect()` fits in memory today, there is a simpler Spark-native way to get only the top-N rows without pulling everything to the driver."],
    starter: "",
    idealAnswer: "**1. Python UDF instead of native `regexp_extract` — serialisation overhead**\n`extract_domain` is a pure regex operation. PySpark Python UDFs serialize every row from the JVM to a Python process (via Py4J / pickle), execute the Python function, then deserialize results back — adding per-row overhead that can make a UDF 3–7× slower than the equivalent built-in function. Spark's `regexp_extract(col(\"url\"), r\"https?://([^/]+)\", 1)` from `pyspark.sql.functions` runs natively on the JVM executor, is Catalyst-transparent, and requires no serialisation. Fix: replace the UDF with `regexp_extract`.\n\n**2. `collect()` sends all distinct domain counts to the driver — OOM risk**\n`domain_counts` could have millions of distinct domain strings. `collect()` pulls every row across the network to the driver's Python process and holds it in driver memory. On a 500 GB clickstream, this is unsafe and unnecessary. Fix: use `.limit(10).collect()` after `orderBy`, or better, use the Spark action `.take(10)` which stops after fetching 10 rows without pulling the full result set to the driver.\n\n**3. Correctness — `collect()` then slice vs. distributed top-N**\nSlicing `all_results[:10]` in Python after `collect()` is logically correct but operationally dangerous. The ordered `collect()` also forces a full sort+shuffle across the entire cluster just to surface 10 rows. `domain_counts.limit(10).collect()` or `domain_counts.head(10)` lets Spark optimise the top-N extraction.",
    rubric: ["Identifies the Python UDF serialisation overhead and names `regexp_extract` or another native alternative", "Explains that `collect()` moves all data to driver memory and quantifies the risk (millions of domain rows from a 4-billion-row dataset)", "Proposes `.limit(10)` or `.take(10)` as the correct driver-safe alternative for top-N retrieval", "Distinguishes correctness (the slice is logically fine) from performance (the full sort+collect is wasteful)"],
  },
  {
    id: "cr-pyspark-withcolumn-loop-schema",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "mid",
    title: "PySpark: withColumn loop and schema inference cost",
    company: "LinkedIn · Data Platform Eng · Virtual Onsite Round 2",
    difficulty: "medium",
    prompt: "Review this PySpark pipeline that reads a raw CSV, casts 50 metric columns, and writes a cleaned Parquet. Identify bugs and performance problems.\n\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col\nfrom pyspark.sql.types import DoubleType\n\nspark = SparkSession.builder.appName(\"MetricsCleaner\").getOrCreate()\n\n# Read raw metrics — 50 columns, ~100 GB\nraw_df = spark.read \\\n    .option(\"header\", True) \\\n    .option(\"inferSchema\", True) \\\n    .csv(\"s3://data-lake/raw_metrics/\")\n\nMETRIC_COLS = [f\"metric_{i}\" for i in range(50)]\n\n# Cast every metric column to DoubleType\nfor col_name in METRIC_COLS:\n    raw_df = raw_df.withColumn(col_name, col(col_name).cast(DoubleType()))\n\n# Compute summary stats for QA\nsummary = raw_df.describe(METRIC_COLS)\nsummary.show()\n\nraw_df.write.mode(\"overwrite\").parquet(\"s3://data-lake/clean_metrics/\")\n```",
    hints: ["When Spark reads a CSV with `inferSchema=True`, what extra work must it do before the logical plan is even created? How many passes over the data does that imply?", "Each call to `withColumn` inside a loop creates a new projection node in the logical plan. What does a plan with 50 nested projections look like, and what problems does that cause at planning and execution time?", "After the loop, `summary.show()` and `raw_df.write` are two separate actions. Does anything get recomputed?"],
    starter: "",
    idealAnswer: "**1. `inferSchema=True` on a 100 GB CSV — double scan cost**\nWhen `inferSchema=True`, Spark reads the entire dataset once to sample types (or fully scans it depending on mode), then reads it again for the actual plan. On 100 GB this is an expensive extra full pass. CSV carries no embedded type metadata, so Spark must examine every value. Fix: define an explicit `StructType` schema and pass it as `.schema(explicit_schema)`; this reduces the read to a single pass and removes type ambiguity.\n\n**2. `withColumn` in a loop — query plan explosion**\nEach call to `withColumn` appends a new `Project` node to the logical plan. After 50 iterations the plan has 50 nested projection layers. Spark's Catalyst optimizer must parse, analyze, and optimize this deep tree. In practice this causes: (a) planning time that grows super-linearly, (b) risk of `StackOverflowError` in the analyzer for very deep plans (commonly observed at 100–200 `withColumn` calls), and (c) invisible performance degradation not visible in the Spark UI. Fix: replace the loop with a single `select()` call: `raw_df = raw_df.select([col(c).cast(DoubleType()).alias(c) if c in METRIC_COLS else col(c) for c in raw_df.columns])`. Spark 3.3+ also offers `withColumns(dict)` for a single plan node.\n\n**3. No `cache()` between `summary.show()` and `raw_df.write` — double recomputation**\n`raw_df` after the loop is a lazy transformation chain. `summary.show()` triggers one full execution (reading CSV twice from S3 due to `inferSchema`, applying 50 projections). Then `raw_df.write` triggers another full execution. Caching `raw_df` after the loop (and before `describe()`) avoids re-reading and re-transforming the 100 GB CSV a second time.",
    rubric: ["Correctly identifies `inferSchema=True` as causing an extra full CSV scan and proposes an explicit `StructType` schema", "Explains the `withColumn`-in-loop query plan explosion (nested Project nodes), names the symptoms (slow planning, StackOverflowError risk), and proposes a single `select()` replacement", "Identifies that two actions (`show` and `write`) without `cache()` recompute the full pipeline twice", "Distinguishes the performance cost of `inferSchema` (extra scan at read time) from the query plan cost of the `withColumn` loop (optimizer overhead)", "Mentions Spark 3.3+ `withColumns()` as an alternative or notes `select()` as the idiomatic fix"],
  },
  {
    id: "cr-pyspark-groupby-skew-aqe",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "senior",
    title: "PySpark: GroupBy skew, straggler tasks, and missing AQE",
    company: "Meta · Data Infrastructure · Senior DE Onsite Loop",
    difficulty: "hard",
    prompt: "Review this PySpark aggregation job that computes per-advertiser revenue from a large ad-events table. The job consistently has one straggler task that takes 40× longer than the median. Identify root causes and fixes.\n\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, sum as spark_sum, count\n\nspark = SparkSession.builder \\\n    .appName(\"AdvertiserRevenue\") \\\n    .config(\"spark.sql.shuffle.partitions\", \"200\") \\\n    .getOrCreate()\n\nevents_df = spark.read.parquet(\"s3://data-lake/ad_events/\")  # 2 TB, ~20B rows\n# advertiser_id distribution: top-3 advertisers account for 70% of all rows\n\nrevenue_df = events_df \\\n    .groupBy(\"advertiser_id\") \\\n    .agg(\n        spark_sum(col(\"revenue_usd\")).alias(\"total_revenue\"),\n        count(\"*\").alias(\"event_count\")\n    )\n\nrevenue_df.write.mode(\"overwrite\").parquet(\"s3://data-lake/output/advertiser_revenue/\")\n```",
    hints: ["The comment says three advertisers account for 70% of rows. After the `groupBy` shuffle, what does one of the 200 partitions look like in terms of row count? What does Spark's task timeline show for that partition?", "Spark 3.0 introduced Adaptive Query Execution (AQE). What specific AQE feature is designed for exactly this scenario, and what configuration keys control it?", "If AQE is not available or you need a manual fix, what technique can you use on the skewed key before the groupBy to break the hot partition into N balanced sub-partitions?"],
    starter: "",
    idealAnswer: "**1. Data skew on hot advertiser keys — straggler task root cause**\nThe `groupBy(\"advertiser_id\")` triggers a shuffle that routes all rows with the same `advertiser_id` to the same partition. With the top-3 advertisers holding 70% of 20 billion rows (~14 billion rows), up to 3 of the 200 shuffle partitions will each contain ~4.7 billion rows. Those partitions become *straggler tasks* — running 40× longer than the median — while other executors sit idle. This is classic data skew / hot-key problem.\n\n**2. `spark.sql.shuffle.partitions = 200` is likely too low for 2 TB**\n200 shuffle partitions means each partition targets ~10 GB. The default recommendation is 128–256 MB per partition, which would suggest 8,000–16,000 partitions for 2 TB. Even without skew, 200 partitions creates large tasks prone to spill. A better static baseline: `spark.conf.set(\"spark.sql.shuffle.partitions\", \"8000\")`. However, AQE handles this dynamically (see below).\n\n**3. AQE skew-join / coalescing not enabled**\nSpark 3.0+ AQE (`spark.sql.adaptive.enabled = true`, default since Spark 3.2) can automatically coalesce small post-shuffle partitions and — critically — detect and split skewed partitions at runtime via `spark.sql.adaptive.skewJoin.enabled = true`. AQE reads actual shuffle file statistics and splits any partition exceeding `spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes` (default 256 MB) into sub-tasks. This directly eliminates the straggler without code changes. Fix: ensure AQE is enabled and do not hard-code `shuffle.partitions` (let AQE coalesce dynamically).\n\n**4. Manual salting as an alternative**\nIf AQE is unavailable (Spark < 3.0 or EMR legacy), apply *salting*: append a random integer suffix (0–N) to the hot keys before the groupBy — `F.concat(col(\"advertiser_id\"), F.lit(\"_\"), (F.rand() * 10).cast(\"int\"))` — to spread the hot key across N partitions. Then perform a second aggregation to recombine the salt buckets. This distributes the hot partition at the cost of two aggregation stages.",
    rubric: ["Correctly identifies data skew as the root cause: hot advertiser keys cause one shuffle partition to receive disproportionately many rows, creating a straggler task", "Notes that `spark.sql.shuffle.partitions=200` is too low for 2 TB of data and quantifies an appropriate target partition size (128–256 MB each)", "Names AQE (`spark.sql.adaptive.enabled`, `spark.sql.adaptive.skewJoin.enabled`) and explains it detects skew at runtime from shuffle statistics", "Describes salting as the Spark-2.x / manual alternative: append a random bucket, aggregate, then de-aggregate", "Distinguishes AQE (zero code change, runtime) from salting (code change, static N, requires two-phase aggregation)"],
  },
  {
    id: "cr-sql-avg-null-skew",
    category: "codereview",
    executes: false,
    mode: "text",
    free: true,
    level: "junior",
    title: "SQL: AVG and COUNT ignoring NULLs",
    company: "Analytics startup · Phone screen",
    difficulty: "easy",
    prompt: "Review this query. The analyst wants the average response-time score and the total number of tickets for each support agent. Point out any bugs, correctness risks, and how you would make it correct.\n\n```sql\nSELECT\n    agent_id,\n    COUNT(response_time_seconds) AS total_tickets,\n    AVG(response_time_seconds)   AS avg_response\nFROM support_tickets\nGROUP BY agent_id;\n```\n\nAssume `response_time_seconds` is NULL for tickets that were never responded to (open tickets). The product team wants open tickets counted in `total_tickets` and treated as the worst possible score (e.g., the column max) for the average.",
    hints: ["What does COUNT(column) do when the column value is NULL for a row?", "What does AVG() do when some values in the group are NULL?", "How would you include NULLs in both the count and the average calculation?"],
    starter: "",
    idealAnswer: "**Bug 1 — COUNT(response_time_seconds) silently skips NULLs.**\nPostgreSQL (and standard SQL) defines `COUNT(expression)` as counting the number of rows where the expression is *not* null. Open tickets — which have `response_time_seconds IS NULL` — are therefore excluded from `total_tickets`. The analyst will see a count lower than the real ticket volume. Fix: use `COUNT(*)` to count every row in the group regardless of column nullability.\n\n**Bug 2 — AVG(response_time_seconds) also silently skips NULLs, producing a biased average.**\nAVG computes `SUM(non-null values) / COUNT(non-null values)`. NULL rows are dropped from both numerator and denominator. This makes agents with many open (unanswered) tickets *look* better than they are, because the missing data is simply ignored rather than treated as a penalty. Per the requirement, open tickets should use the worst (maximum) value.\n\nFix — replace NULLs with the column maximum before averaging:\n```sql\nSELECT\n    agent_id,\n    COUNT(*)                                              AS total_tickets,\n    AVG(\n        COALESCE(response_time_seconds,\n                 MAX(response_time_seconds) OVER ())\n    )                                                     AS avg_response\nFROM support_tickets\nGROUP BY agent_id;\n```\nAlternatively, pre-compute the sentinel in a CTE:\n```sql\nWITH max_rt AS (\n    SELECT MAX(response_time_seconds) AS sentinel FROM support_tickets\n)\nSELECT\n    t.agent_id,\n    COUNT(*)                                        AS total_tickets,\n    AVG(COALESCE(t.response_time_seconds, m.sentinel)) AS avg_response\nFROM support_tickets t\nCROSS JOIN max_rt m\nGROUP BY t.agent_id;\n```\n\n**Correctness note:** If *all* tickets for an agent are open (all NULLs), `AVG()` returns NULL. With the COALESCE fix this is no longer an issue, but worth flagging in a review.\n\n**Performance:** No additional indexes are needed here; the GROUP BY scan is expected. The COALESCE fix adds negligible cost.",
    rubric: ["Correctly identifies COUNT(column) skips NULLs; recommends COUNT(*)", "Correctly identifies AVG ignores NULLs, explains sum/count denominator omission", "Understands the business impact: open tickets excluded makes agent KPIs look artificially good", "Proposes a semantically correct fix (COALESCE with a sentinel, or CASE WHEN NULL THEN max_value)", "Mentions edge case: all-NULL group returns NULL from AVG", "Distinguishes correctness bugs from performance concerns (both are correctness here)"],
  },
  {
    id: "cr-sql-nonsargable-date",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "mid",
    title: "SQL: Non-sargable DATE() predicate killing index",
    company: "E-commerce platform · Technical interview",
    difficulty: "medium",
    prompt: "Review this query used in a nightly report on a table with ~50 million rows. `orders.created_at` is a `TIMESTAMPTZ` column with a B-tree index. Identify any bugs or performance problems and explain how to fix them.\n\n```sql\nSELECT\n    DATE(created_at)   AS order_date,\n    COUNT(*)           AS order_count,\n    SUM(total_amount)  AS daily_revenue\nFROM orders\nWHERE DATE(created_at) = CURRENT_DATE - INTERVAL '1 day'\nGROUP BY DATE(created_at)\nORDER BY order_date;\n```",
    hints: ["Can PostgreSQL use a B-tree index on `created_at` when you wrap the column in DATE()?", "What does DATE(TIMESTAMPTZ) depend on beyond the stored value?", "How would you rewrite the WHERE clause to make it sargable (index-usable)?"],
    starter: "",
    idealAnswer: "**Bug 1 — Non-sargable predicate: DATE(created_at) defeats the index.**\nWrapping the indexed column in `DATE()` is a classic non-sargable pattern. PostgreSQL's query planner can only use a B-tree index if the column appears *bare* on one side of the comparison. Because `DATE()` is applied to `created_at`, the planner must invoke the function for every row in a full sequential scan — making this O(N) on 50 million rows every night.\n\nAdditionally, `DATE(TIMESTAMPTZ)` is timezone-sensitive: it converts the stored UTC value using the *session* timezone. The function is therefore not `IMMUTABLE`, which also prevents creating a plain function-based index on it.\n\n**Fix — rewrite as a half-open range on the bare column:**\n```sql\nSELECT\n    DATE(created_at)   AS order_date,\n    COUNT(*)           AS order_count,\n    SUM(total_amount)  AS daily_revenue\nFROM orders\nWHERE created_at >= (CURRENT_DATE - INTERVAL '1 day')\n  AND created_at <   CURRENT_DATE\nGROUP BY DATE(created_at)\nORDER BY order_date;\n```\nNow `created_at` is bare on the left of both comparisons; PostgreSQL can use an index range scan. The `DATE()` in SELECT and GROUP BY is fine — it only transforms *output* rows, not the index lookup predicate.\n\n**Bug 2 — Timezone ambiguity.**\n`CURRENT_DATE - INTERVAL '1 day'` gives yesterday in the *session* timezone. If sessions can run in different timezones (common in multi-region apps), the boundary drifts. The defensive fix is to pin the timezone explicitly:\n```sql\nWHERE created_at >= (CURRENT_DATE - INTERVAL '1 day') AT TIME ZONE 'UTC'\n  AND created_at <   CURRENT_DATE AT TIME ZONE 'UTC'\n```\nor use `date_trunc('day', NOW() AT TIME ZONE 'UTC')` as the boundary.\n\n**Performance summary:** Sequential scan on 50 M rows at O(N) vs index range scan at O(log N + matching rows). On spinning disks this can be the difference between seconds and minutes.",
    rubric: ["Names the non-sargable anti-pattern: function on indexed column prevents index use", "Explains the mechanism: planner cannot invert DATE() to get an index range", "Correctly rewrites the predicate as a half-open range (>= yesterday, < today) with bare column", "Identifies that DATE(TIMESTAMPTZ) is session-timezone-dependent (not IMMUTABLE)", "Distinguishes the SELECT/GROUP BY usage of DATE() (safe) from the WHERE usage (unsafe)", "Notes the timezone drift risk in multi-region or multi-session environments"],
  },
  {
    id: "cr-sql-having-vs-where",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "mid",
    title: "SQL: HAVING filtering a non-aggregate predicate",
    company: "SaaS analytics team · Onsite round",
    difficulty: "medium",
    prompt: "Review this query. The goal is to find active users (status = 'active') who placed more than 5 orders in the last 30 days. Point out any logic errors, misuse of SQL clauses, and performance concerns.\n\n```sql\nSELECT\n    u.id,\n    u.email,\n    COUNT(o.id) AS order_count\nFROM users u\nJOIN orders o ON o.user_id = u.id\nWHERE o.created_at >= NOW() - INTERVAL '30 days'\nGROUP BY u.id, u.email\nHAVING COUNT(o.id) > 5\n   AND u.status = 'active';\n```",
    hints: ["At what stage in query execution does HAVING filter rows — before or after GROUP BY?", "Is u.status an aggregate expression? Does it belong in HAVING or WHERE?", "What is the performance cost of evaluating u.status = 'active' in HAVING vs WHERE?"],
    starter: "",
    idealAnswer: "**Bug — u.status = 'active' belongs in WHERE, not HAVING.**\n\nSQL's logical order of operations is: FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY. The `HAVING` clause runs *after* full grouping and aggregation. Placing `u.status = 'active'` there forces the database to:\n1. Join every user (active or not) to their orders.\n2. Group all joined rows and compute COUNT(o.id) per user.\n3. Only *then* discard inactive users.\n\nMoving the filter to WHERE eliminates inactive users before the join fan-out and before aggregation, potentially scanning far fewer rows.\n\n**Correct rewrite:**\n```sql\nSELECT\n    u.id,\n    u.email,\n    COUNT(o.id) AS order_count\nFROM users u\nJOIN orders o ON o.user_id = u.id\nWHERE o.created_at >= NOW() - INTERVAL '30 days'\n  AND u.status = 'active'          -- moved here\nGROUP BY u.id, u.email\nHAVING COUNT(o.id) > 5;           -- only aggregate condition remains\n```\n\n**Why it also still works correctly:** `u.status` is a non-aggregate column (it's the same value for every row in a user's group). Allowing it in HAVING is technically legal in PostgreSQL because `u.id` is in GROUP BY and the planner can resolve `u.status` functionally, but it is wasteful and violates the intent of HAVING.\n\n**Performance impact:** If `users` has an index on `status`, putting the filter in WHERE allows the planner to consider an index scan or a filtered hash join, pruning inactive users from the start. In HAVING, that index is unused.\n\n**No correctness bug in the original (results are the same):** This is a performance and clarity issue, not a wrong-answer bug. A good review comment distinguishes between a broken query and an inefficient but correct one.",
    rubric: ["Correctly identifies that HAVING runs after GROUP BY, making it the wrong place for non-aggregate filters", "Moves u.status = 'active' to the WHERE clause in the fix", "Explains the execution-order mechanics (FROM→WHERE→GROUP BY→HAVING)", "Notes the performance implication: more rows aggregated before filter is applied", "Correctly states the original query produces the right result — this is a performance/style bug, not a logic error", "Mentions that an index on users.status can only be leveraged when the filter is in WHERE"],
  },
  {
    id: "cr-sql-correlated-subquery-window",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "senior",
    title: "SQL: Correlated subquery running once per row instead of window function",
    company: "Data engineering platform · System design / SQL round",
    difficulty: "hard",
    prompt: "Review this query. The goal is to label each sale record with its rank within the product category (1 = highest revenue sale in that category), and also tag any sale that falls within the top 3 for its category. The table `sales` has ~10 million rows. Identify correctness issues and performance problems, and provide a better implementation.\n\n```sql\nSELECT\n    s.id,\n    s.product_category,\n    s.revenue,\n    (\n        SELECT COUNT(*) + 1\n        FROM sales s2\n        WHERE s2.product_category = s.product_category\n          AND s2.revenue > s.revenue\n    ) AS revenue_rank,\n    CASE\n        WHEN (\n            SELECT COUNT(*) + 1\n            FROM sales s2\n            WHERE s2.product_category = s.product_category\n              AND s2.revenue > s.revenue\n        ) <= 3\n        THEN TRUE\n        ELSE FALSE\n    END AS is_top_3\nFROM sales s;\n```",
    hints: ["How many times does each correlated subquery execute relative to the number of rows in `sales`?", "What SQL feature is designed exactly for computing per-row rankings within a partition without re-scanning the table?", "The rank formula COUNT(*)+1 WHERE revenue > current — does it handle ties correctly? What happens if two rows have the same revenue?"],
    starter: "",
    idealAnswer: "**Performance bug — each correlated subquery executes O(N) times.**\nFor every row in the outer `sales` table (~10 M rows), PostgreSQL runs the inner `SELECT COUNT(*)+1 FROM sales s2 WHERE ...` as a nested-loop scan within the matching category partition. With two identical subqueries in the SELECT list, this is effectively *two full category scans per outer row*. On 10 M rows this can mean billions of row-evaluations — the query will be catastrophically slow without a covering index, and even with one it is unnecessarily expensive.\n\n**Correctness bug — tie-breaking is undefined.**\nThe formula `COUNT(*) + 1 WHERE s2.revenue > s.revenue` implements *dense* rank only when revenues are unique. When two rows share the same `revenue` in a category, both receive the same rank. This is actually `DENSE_RANK()` behaviour, but it might not be what the analyst intends. If standard competition ranking (`RANK()`) or row-number ranking is wanted, the semantics differ. The `is_top_3` flag can also be incorrect: if three rows are tied at rank 1, all three get `is_top_3 = TRUE` (arguably correct), but if the intent was exactly the top 3 unique rows, the flag is misleading.\n\n**Fix — rewrite using window functions:**\n```sql\nSELECT\n    id,\n    product_category,\n    revenue,\n    DENSE_RANK() OVER (\n        PARTITION BY product_category\n        ORDER BY revenue DESC\n    ) AS revenue_rank,\n    DENSE_RANK() OVER (\n        PARTITION BY product_category\n        ORDER BY revenue DESC\n    ) <= 3  AS is_top_3\nFROM sales;\n```\nWindow functions scan the table once (or once per partition pass), sort within partitions, and compute all ranks in a single pass — O(N log N) for the sort, not O(N²).\n\nIf `RANK()` (gaps on ties) or `ROW_NUMBER()` (unique sequential rank, arbitrary tiebreak) is actually wanted, use those instead and add an `ORDER BY` tiebreaker column (e.g., `id`) to make `ROW_NUMBER()` deterministic:\n```sql\nROW_NUMBER() OVER (\n    PARTITION BY product_category\n    ORDER BY revenue DESC, id ASC   -- tiebreaker for determinism\n) AS revenue_rank\n```\n\n**Supporting index:** Add a composite index `(product_category, revenue DESC)` to accelerate the window sort.\n\n**Summary table:**\n| Approach | Scans | Complexity | Tie handling |\n|---|---|---|---|\n| Correlated subquery (original) | 2 × N | O(N²) | same as DENSE_RANK |\n| DENSE_RANK() window | 1 | O(N log N) | ties share rank |\n| RANK() window | 1 | O(N log N) | gaps after ties |\n| ROW_NUMBER() window | 1 | O(N log N) | unique, needs tiebreaker |",
    rubric: ["Identifies the correlated subquery executes once per outer row — O(N) inner scans for O(N) outer rows = O(N²) total", "Notes the duplicate subquery: the same scan is done twice for revenue_rank and is_top_3", "Correctly rewrites using DENSE_RANK() OVER (PARTITION BY ... ORDER BY ...) window function", "Distinguishes RANK vs DENSE_RANK vs ROW_NUMBER and explains when each is appropriate", "Flags the tie-handling ambiguity in the original COUNT+1 formula", "Recommends a tiebreaker column in ROW_NUMBER() to ensure deterministic ordering", "Mentions a supporting composite index on (product_category, revenue DESC)"],
  },
  {
    id: "cr-python-large-csv-load",
    category: "codereview",
    executes: false,
    mode: "text",
    free: true,
    level: "junior",
    title: "Python: Loading a Large CSV Without Memory Guards",
    company: "Data Platform Team · Phone Screen",
    difficulty: "easy",
    prompt: "Review this snippet for correctness, performance, and reliability issues. Write a prose code review as if you are commenting on a pull request.\n\n```python\nimport pandas as pd\n\ndef compute_daily_revenue(filepath: str) -> float:\n    \"\"\"Sum the 'amount' column of a potentially huge transaction CSV.\"\"\"\n    df = pd.read_csv(filepath)\n    df = df[df['status'] == 'completed']\n    total = df['amount'].sum()\n    return float(total)\n```",
    hints: ["What happens to RAM if `filepath` points to a 10 GB file?", "What dtype does pandas assign to string columns and numeric columns by default, and why does that matter for memory?", "Is there a `pd.read_csv` parameter that lets you process the file without loading it all at once?"],
    starter: "",
    idealAnswer: "**Memory exhaustion on large files** — `pd.read_csv(filepath)` reads the *entire* file into a single DataFrame before any filtering occurs. If the CSV is multi-GB (common in data-engineering pipelines), this can exhaust available RAM and crash the process. The fix is to pass `chunksize=` so pandas returns a `TextFileReader` iterator; each chunk is filtered and aggregated individually, keeping peak memory proportional to chunk size rather than file size.\n\n```python\ntotal = 0.0\nfor chunk in pd.read_csv(filepath, chunksize=100_000,\n                          dtype={'amount': 'float32', 'status': 'category'}):\n    total += chunk.loc[chunk['status'] == 'completed', 'amount'].sum()\nreturn total\n```\n\n**No `dtype` specification** — Without explicit `dtype`, pandas infers types by reading the whole column first (defeating any low-memory strategy) and defaults to wide types: numeric columns with any NaN become `float64`, string columns become `object` (Python heap pointers — 8 bytes per entry plus the string itself). Specifying `dtype={'amount': 'float32', 'status': 'category'}` can cut memory 40–60% for typical transaction data, per the pandas scaling guide.\n\n**Performance classification** — This is a *performance* and *reliability* bug, not a logic bug; the function returns the correct answer for files that fit in RAM, but fails silently (OOM kill) for large ones. Correctness is preserved by the chunk-sum approach since summation is associative.",
    rubric: ["Identifies that pd.read_csv loads the full file into memory (correctness/reliability)", "Proposes chunksize as the fix and explains how the iterator works", "Identifies missing dtype specification as a secondary memory issue", "Mentions category dtype for low-cardinality string columns", "Notes the fix preserves correctness because sum is associative", "Distinguishes between performance bug vs logic bug"],
  },
  {
    id: "cr-python-bare-except-no-retry",
    category: "codereview",
    executes: false,
    mode: "text",
    free: true,
    level: "junior",
    title: "Python: Bare Except Swallowing Signals and No Retry Logic",
    company: "Analytics Engineering · Take-Home Review",
    difficulty: "easy",
    prompt: "Review this snippet for correctness, reliability, and best-practice concerns. Write a prose code review.\n\n```python\nimport psycopg2\n\ndef fetch_user_record(conn, user_id: int) -> dict | None:\n    \"\"\"Fetch a single user row from the database.\"\"\"\n    try:\n        cur = conn.cursor()\n        cur.execute(\"SELECT * FROM users WHERE id = %s\", (user_id,))\n        row = cur.fetchone()\n        if row is None:\n            return None\n        return dict(zip([desc[0] for desc in cur.description], row))\n    except:\n        print(\"something went wrong\")\n        return None\n```",
    hints: ["What exception classes does a bare `except:` catch that `except Exception:` does not?", "If this function is called inside a long-running pipeline and the DB is temporarily unreachable, what happens?", "The cursor is created inside the try block — how is it closed?"],
    starter: "",
    idealAnswer: "**Bare `except:` swallows `KeyboardInterrupt` and `SystemExit`** — A bare `except:` clause catches *all* exceptions, including `KeyboardInterrupt` (Ctrl-C) and `SystemExit`. This means pressing Ctrl-C while `fetch_user_record` is executing will be silently swallowed and `None` returned — the process continues running when the user intends to stop it. PEP 8 explicitly recommends against bare `except:` for exactly this reason. The fix is `except Exception:` which is limited to subclasses of `Exception` and lets `KeyboardInterrupt`/`SystemExit` propagate normally.\n\n**Errors are silently swallowed with no re-raise** — The handler prints a generic string and returns `None`. Callers cannot distinguish \"user does not exist\" from \"database is down\" from \"network timeout\". Real errors (e.g., `psycopg2.OperationalError`) should at minimum be re-raised after logging, or the function should raise a domain-specific exception. Swallowing errors hides bugs and makes production diagnosis very hard.\n\n**No retry / backoff on transient network failures** — Database connections are subject to transient failures (brief network hiccup, connection pool exhaustion). Without retry logic the first transient failure returns `None`, which callers may silently treat as \"user not found\". A library such as `tenacity` with `@retry(wait=wait_exponential(), retry=retry_if_exception_type(psycopg2.OperationalError), stop=stop_after_attempt(3))` handles transient failures without special-casing.\n\n**Cursor is never explicitly closed** — `cur` is opened but never closed via `cur.close()` or a `with conn.cursor() as cur:` block. If an exception fires before `fetchone()`, the cursor leaks. psycopg2 cursors are context managers; use `with conn.cursor() as cur:` to guarantee cleanup.\n\n**`SELECT *` is a minor style issue** — Selecting all columns is fragile; adding a column to the table changes the returned dict silently. Prefer explicit column lists.",
    rubric: ["Identifies that bare except catches KeyboardInterrupt and SystemExit", "Recommends except Exception: with explanation of the class hierarchy", "Notes that silently returning None conflates 'not found' with 'error'", "Recommends re-raising or wrapping in a domain exception", "Identifies lack of retry/backoff for transient DB errors", "Identifies the cursor resource leak / missing context manager", "Bonus: notes SELECT * fragility"],
  },
  {
    id: "cr-python-n-plus-one-query",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "mid",
    title: "Python: N+1 Queries in a Processing Loop",
    company: "Data Engineering Platform · Mid-Level Interview",
    difficulty: "medium",
    prompt: "Review this snippet for correctness, performance, and scalability issues. Write a prose code review.\n\n```python\nimport psycopg2\n\ndef enrich_events(conn, event_ids: list[int]) -> list[dict]:\n    \"\"\"\n    For each event ID, fetch its metadata from the DB and\n    return a list of enriched dicts.\n    \"\"\"\n    results = []\n    cur = conn.cursor()\n    for eid in event_ids:\n        cur.execute(\n            \"SELECT id, name, category, ts FROM events WHERE id = %s\",\n            (eid,)\n        )\n        row = cur.fetchone()\n        if row:\n            results.append({\n                \"id\": row[0],\n                \"name\": row[1],\n                \"category\": row[2],\n                \"ts\": row[3],\n            })\n    cur.close()\n    return results\n```",
    hints: ["How many round-trips to the database does this function make if `event_ids` has 10 000 elements?", "How could you fetch all the needed rows with a single SQL query?", "What happens to `cur` if an exception is raised before `cur.close()`?"],
    starter: "",
    idealAnswer: "**N+1 query anti-pattern — O(n) database round-trips** — The function executes one `SELECT` per event ID, so for `n` event IDs it makes `n` round-trips to the database. Each round-trip carries network latency, connection overhead, and PostgreSQL query planning cost. At 10 000 events this is 10 000 sequential round-trips; benchmarks on psycopg2 show per-execute overhead alone can make looped queries 13× slower than a single batched query for equivalent data. The fix is a single `SELECT … WHERE id = ANY(%s)` (PostgreSQL) or `WHERE id IN %s` query passing the entire list at once:\n\n```python\ncur.execute(\n    \"SELECT id, name, category, ts FROM events WHERE id = ANY(%s)\",\n    (event_ids,)   # psycopg2 adapts a Python list to a Postgres ARRAY literal\n)\nrows = cur.fetchall()\n```\n\nThis reduces N+1 round-trips to exactly 1.\n\n**Result ordering is not preserved** — The original loop preserves input order by construction. The batched query returns rows in arbitrary order (no `ORDER BY`), so the caller may receive results in a different order than `event_ids`. If order matters, add `ORDER BY id` or re-sort client-side by building a lookup dict keyed on `id`.\n\n**Cursor is not closed on exception** — `cur.close()` is only called at the happy-path end of the function. If an exception is raised mid-loop (e.g., a malformed row or a network drop), the cursor leaks. Use `with conn.cursor() as cur:` to guarantee it is closed in all code paths.\n\n**No handling for duplicate IDs in `event_ids`** — If the caller passes duplicates, the SQL `IN`/`ANY` query deduplicates silently (SQL sets), which may cause the returned list to be shorter than expected. The function should document this behaviour or deduplicate explicitly before querying.",
    rubric: ["Names the anti-pattern: N+1 queries / O(n) round-trips", "Quantifies the performance impact correctly", "Proposes a single WHERE id = ANY(%s) or IN %s query as the fix", "Notes that batched query changes result ordering", "Identifies cursor not closed on exception path", "Bonus: notes duplicate ID edge case"],
  },
  {
    id: "cr-python-fstring-sql-injection",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "senior",
    title: "Python: SQL Injection via f-String and Secret Leakage in Logs",
    company: "Data Platform · Senior / Security Review Round",
    difficulty: "hard",
    prompt: "Review this snippet for security, correctness, and operational concerns. Write a prose code review.\n\n```python\nimport psycopg2\nimport logging\n\nlogger = logging.getLogger(__name__)\n\ndef get_pipeline_runs(\n    conn,\n    pipeline_name: str,\n    status: str,\n    db_password: str,   # passed in for audit logging\n) -> list[dict]:\n    \"\"\"\n    Return pipeline run records matching the given name and status.\n    Logs full connection details for audit purposes.\n    \"\"\"\n    logger.info(\n        f\"Querying pipeline_runs: pipeline={pipeline_name}, \"\n        f\"status={status}, db_password={db_password}\"\n    )\n    cur = conn.cursor()\n    query = (\n        f\"SELECT id, pipeline_name, status, started_at, finished_at \"\n        f\"FROM pipeline_runs \"\n        f\"WHERE pipeline_name = '{pipeline_name}' AND status = '{status}'\"\n    )\n    cur.execute(query)\n    rows = cur.fetchall()\n    cur.close()\n    return [\n        {\n            \"id\": r[0],\n            \"pipeline_name\": r[1],\n            \"status\": r[2],\n            \"started_at\": r[3],\n            \"finished_at\": r[4],\n        }\n        for r in rows\n    ]\n```",
    hints: ["What SQL string would an attacker pass as `pipeline_name` to exfiltrate or delete data?", "How does psycopg2's parameterized query syntax prevent injection at the protocol level?", "What is the risk of logging `db_password` at INFO level — even in a 'secure' internal service?"],
    starter: "",
    idealAnswer: "**SQL injection via f-string query construction (critical security bug)** — The `pipeline_name` and `status` values are interpolated directly into the SQL string with an f-string. An attacker (or even a pipeline name containing a single-quote) can escape the string literal and inject arbitrary SQL. Example payload for `pipeline_name`:\n\n```\n' OR '1'='1'; DROP TABLE pipeline_runs; --\n```\n\nThis would produce:\n```sql\nWHERE pipeline_name = '' OR '1'='1'; DROP TABLE pipeline_runs; --' AND status = '...'\n```\n\nThe fix is mandatory: use psycopg2 parameterized queries, which send SQL structure and data separately so the database driver handles all escaping at the protocol level — injection is structurally impossible, not merely filtered:\n\n```python\ncur.execute(\n    \"SELECT id, pipeline_name, status, started_at, finished_at \"\n    \"FROM pipeline_runs \"\n    \"WHERE pipeline_name = %s AND status = %s\",\n    (pipeline_name, status)\n)\n```\n\nNote: column names and `ORDER BY` targets *cannot* be parameterized — if those are dynamic, use an explicit allowlist.\n\n**Database password logged in plaintext (critical secret leakage)** — `db_password` is logged at `INFO` level. Log lines are written to stdout, log aggregators (Datadog, Splunk, CloudWatch), and potentially cold storage, retained for months. A plaintext credential in logs is a credential breach — anyone with log read access has the password. The `db_password` parameter should be removed from the function signature entirely (passwords belong in environment variables or a secrets manager, not passed around as arguments), and must *never* appear in log output. If audit logging is required, log only a redacted token or connection host, never the secret.\n\n**Cursor not closed on exception** — Same resource-leak issue as simpler examples: `cur.close()` on the happy path only. Use `with conn.cursor() as cur:` for guaranteed cleanup.\n\n**`fetchall()` without LIMIT loads unbounded result sets** — A pipeline with millions of runs would load all rows into memory at once. Add a `LIMIT` clause or use server-side cursors (`conn.cursor(name='...')`) for streaming.\n\n**`db_password` in function signature is an API design smell** — Passing a database credential as a function argument means it can be stored in call stacks, tracebacks, and profiler output. Credentials should be retrieved from the environment or a secrets manager at connection time and should not flow as positional arguments through business-logic functions.",
    rubric: ["Identifies f-string SQL injection as a critical security vulnerability", "Provides a concrete attack payload example", "Explains parameterized queries fix at the protocol level (not just escaping)", "Identifies db_password in log output as a critical secret leakage", "Recommends removing db_password from the function signature", "Identifies cursor resource leak on exception path", "Bonus: notes unbounded fetchall() memory risk", "Bonus: notes db_password in function signature as API design issue"],
  },
  {
    id: "cr-ts-floating-promise-audit",
    category: "codereview",
    executes: false,
    mode: "text",
    free: true,
    level: "junior",
    title: "TypeScript: Floating Promise in Audit Logger",
    company: "Data Pipeline Startup · Technical Phone Screen",
    difficulty: "easy",
    prompt: "Review this audit-logging helper used in a Next.js API route. The reviewer says it 'seems to work in local tests.' Identify all bugs, type-safety issues, async correctness problems, and any performance concerns.\n\n```typescript\n// lib/audit.ts\nimport { db } from './db';\n\ninterface AuditEvent {\n  userId: string;\n  action: string;\n  metadata?: Record<string, unknown>;\n}\n\nexport function logAuditEvent(event: AuditEvent): void {\n  db.auditLogs.create({ data: event }); // fire-and-forget insert\n}\n\n// app/api/delete-dataset/route.ts\nexport async function DELETE(req: Request) {\n  const body = await req.json();\n  const datasetId = body.datasetId;\n\n  await db.datasets.delete({ where: { id: datasetId } });\n\n  logAuditEvent({\n    userId: body.userId,\n    action: 'DELETE_DATASET',\n    metadata: { datasetId },\n  });\n\n  return new Response(JSON.stringify({ success: true }), { status: 200 });\n}\n```",
    hints: ["What is the return type of `db.auditLogs.create(...)`, and does the caller do anything with it?", "What happens to the audit log if the database write throws — does any caller know about it?", "What guarantees do you have that `body.datasetId` and `body.userId` are non-null strings at runtime?"],
    starter: "",
    idealAnswer: "**1. Floating Promise — async correctness (critical)**\n`db.auditLogs.create(...)` returns a Promise, but `logAuditEvent` declares its return type as `void` and neither `await`s nor returns the Promise. The Promise is created and immediately discarded — a classic *floating promise*. If the insert rejects (e.g., DB constraint violation, network timeout), the rejection goes unhandled. In Node.js, an unhandled rejection triggers a warning and, in Node ≥ 15 / production configs, crashes the process. The fix is either to `await` the call inside an `async` function (and propagate or swallow the error explicitly) or at minimum attach `.catch(err => logger.error(err))`:\n\n```typescript\nexport async function logAuditEvent(event: AuditEvent): Promise<void> {\n  try {\n    await db.auditLogs.create({ data: event });\n  } catch (err) {\n    // never let audit failures crash the caller; but DO log them\n    console.error('Audit log failed', err);\n  }\n}\n```\nThe call site in `DELETE` must then `await logAuditEvent(...)` — or the same floating-promise bug re-appears at the call site.\n\n**2. Missing `await` at the call site — async ordering (critical)**\nEven if `logAuditEvent` is fixed to be `async`, the call in `DELETE` is not `await`ed. The response is returned before the audit log has a chance to complete, and any thrown error inside `logAuditEvent` will again be a floating promise at the call site. Always `await` async functions unless you genuinely intend fire-and-forget and have explicit error swallowing.\n\n**3. Unvalidated `req.json()` — type safety & correctness (high)**\n`body` is typed `any` (the return of `req.json()` in the Fetch API is `Promise<any>`). `body.datasetId` and `body.userId` are used without any runtime check — they could be `undefined`, `null`, or a completely wrong type. A malformed request body would silently delete the wrong dataset or write a null `userId` to the audit log. The fix is input validation (e.g., Zod):\n\n```typescript\nimport { z } from 'zod';\nconst schema = z.object({ datasetId: z.string().uuid(), userId: z.string() });\nconst { datasetId, userId } = schema.parse(await req.json());\n```\n\n**4. No try-catch around the delete — error handling (medium)**\nIf `db.datasets.delete` throws (e.g., record not found), the API route propagates an unhandled exception. Next.js will return a 500 but without a controlled error response. Wrap in try-catch and return a structured error body with an appropriate status code (404 vs 500).\n\n**Summary of categories:**\n- Correctness/Async: floating promise, missing await at call site\n- Type safety: unvalidated `any` body\n- Reliability: no error boundary around the delete",
    rubric: ["Identifies the floating promise inside `logAuditEvent` and explains why it silently swallows errors", "Identifies that the call site also lacks `await` after the fix, so the bug persists at both layers", "Notes that `req.json()` returns `any` and the body fields are unvalidated at runtime", "Suggests a concrete fix: make `logAuditEvent` async, await it, add try-catch or .catch()", "Mentions runtime consequences: unhandled rejection warning / process crash in Node ≥ 15", "Bonus: suggests Zod or equivalent for request body validation"],
  },
  {
    id: "cr-ts-n-plus-one-api-route",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "mid",
    title: "TypeScript: N+1 Query in Dataset Listing Route",
    company: "ML Platform Team · Backend Engineering Interview",
    difficulty: "medium",
    prompt: "Review this Next.js App Router route that returns a list of datasets with their latest pipeline run status. The team reports it is 'slow on large accounts.' Identify all bugs, type-safety problems, async correctness issues, and performance problems.\n\n```typescript\n// app/api/datasets/route.ts\nimport { db } from '@/lib/db';\n\ntype RunStatus = 'pending' | 'running' | 'success' | 'failed';\n\ninterface Dataset {\n  id: number;\n  name: string;\n  ownerId: number;\n}\n\ninterface EnrichedDataset extends Dataset {\n  latestRunStatus: RunStatus;\n}\n\nexport async function GET(req: Request) {\n  const { searchParams } = new URL(req.url);\n  const ownerId = parseInt(searchParams.get('ownerId')!);\n\n  const datasets: Dataset[] = await db.query(\n    'SELECT id, name, owner_id FROM datasets WHERE owner_id = $1',\n    [ownerId]\n  );\n\n  // Enrich each dataset with the latest pipeline run\n  const enriched = await Promise.all(\n    datasets.map(async (ds) => {\n      const runs = await db.query(\n        'SELECT status FROM pipeline_runs WHERE dataset_id = $1 ORDER BY created_at DESC LIMIT 1',\n        [ds.id]\n      );\n      const latestRunStatus = (runs[0]?.status ?? 'pending') as RunStatus;\n      return { ...ds, latestRunStatus };\n    })\n  );\n\n  return Response.json(enriched);\n}\n```",
    hints: ["How many database round-trips happen when the account has 200 datasets?", "What is `parseInt` doing with the `!` operator, and what could go wrong?", "Is the `as RunStatus` cast safe — what if the DB returns an unexpected status string?"],
    starter: "",
    idealAnswer: "**1. N+1 Database Query Pattern — performance (critical)**\nFor an account with N datasets, the route fires 1 query for the dataset list plus N individual queries for latest pipeline runs — N+1 round-trips total. With 200 datasets this is 201 sequential queries (Promise.all makes them concurrent, but each still pays a round-trip to the DB). The correct fix is a single joined query or a lateral join:\n\n```sql\nSELECT d.id, d.name, d.owner_id,\n       COALESCE(r.status, 'pending') AS latest_run_status\nFROM datasets d\nLEFT JOIN LATERAL (\n  SELECT status FROM pipeline_runs\n  WHERE dataset_id = d.id\n  ORDER BY created_at DESC LIMIT 1\n) r ON true\nWHERE d.owner_id = $1;\n```\nThis collapses N+1 queries into 1 round-trip regardless of dataset count.\n\n**2. Unbounded `Promise.all` — performance / resource risk (high)**\nUsing `Promise.all` over an arbitrarily large `datasets` array fires all N queries simultaneously. For an enterprise account with thousands of datasets this can exhaust the DB connection pool, causing timeouts for other requests. Fix: batch the queries (e.g., chunks of 50) or, better, eliminate the N+1 with the join above. If Promise.all must be retained for other reasons, enforce a concurrency limit (e.g., using `p-limit`).\n\n**3. `parseInt` Without Radix + Non-Null Assertion — type safety & correctness (high)**\n`parseInt(searchParams.get('ownerId')!)` has two compounded problems:\n- The non-null assertion `!` lies to TypeScript: `searchParams.get('ownerId')` returns `string | null`. If `ownerId` is absent, `!` suppresses the null check and `parseInt(null as any)` returns `NaN`, which is then passed to the query as `NaN`, likely returning 0 rows or causing a DB error.\n- `parseInt` without an explicit radix defaults to base-10 for most inputs but can behave unexpectedly with inputs prefixed by `0x`.\nFix: validate explicitly.\n```typescript\nconst raw = searchParams.get('ownerId');\nif (!raw || !/^\\d+$/.test(raw)) {\n  return Response.json({ error: 'Invalid ownerId' }, { status: 400 });\n}\nconst ownerId = parseInt(raw, 10);\n```\n\n**4. Unsafe `as RunStatus` Cast — type safety (medium)**\n`(runs[0]?.status ?? 'pending') as RunStatus` tells TypeScript to trust that whatever the DB returns is a valid `RunStatus`. If the `pipeline_runs` table ever contains a legacy status string (e.g., `'cancelled'`, `'queued'`) the cast silently passes and downstream consumers receive an invalid enum value. A type guard or validation step is safer:\n```typescript\nconst VALID_STATUSES = new Set<string>(['pending','running','success','failed']);\nfunction toRunStatus(s: string | undefined): RunStatus {\n  return VALID_STATUSES.has(s!) ? (s as RunStatus) : 'pending';\n}\n```\n\n**5. Missing Error Handling (medium)**\nNo try-catch around the database calls. A DB error returns an unhandled rejection and a 500 with no body. Wrap in try-catch and return a structured error response.\n\n**Summary of categories:**\n- Performance: N+1 queries, unbounded Promise.all concurrency\n- Type safety: non-null assertion on nullable, unsafe cast\n- Correctness: parseInt without radix, NaN passed to DB, unvalidated input",
    rubric: ["Correctly identifies the N+1 pattern: 1 + N DB queries for N datasets", "Proposes a SQL JOIN/lateral join to collapse to a single query", "Notes the risk of unbounded Promise.all hammering the connection pool", "Catches the non-null assertion `!` on a nullable `searchParams.get()` return", "Identifies `parseInt` without radix and the NaN propagation risk", "Flags the unsafe `as RunStatus` cast as providing no runtime validation", "Bonus: mentions p-limit or batching as mitigation for the Promise.all concurrency issue"],
  },
  {
    id: "cr-ts-json-parse-config-loader",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "mid",
    title: "TypeScript: Unsafe JSON Config Loader with Number Precision Bug",
    company: "AI Infrastructure Co. · Mid-Level Backend Review",
    difficulty: "medium",
    prompt: "Review this config-loader used to read per-tenant AI model configuration from a webhook payload. It is called during each inference request. Find all bugs, type-safety issues, and performance problems.\n\n```typescript\n// lib/config-loader.ts\n\ninterface ModelConfig {\n  tenantId: number;   // from a Snowflake / warehouse row — can exceed 2^53\n  modelName: string;\n  maxTokens: number;\n  allowedFeatures: string[];\n}\n\nlet cachedConfig: ModelConfig | null = null;\n\nexport function loadConfig(rawJson: string): ModelConfig {\n  const config = JSON.parse(rawJson) as ModelConfig;\n  cachedConfig = config;\n  return config;\n}\n\nexport function getMaxTokens(): number {\n  return cachedConfig!.maxTokens;\n}\n\nexport function isFeatureAllowed(feature: string): boolean {\n  return cachedConfig!.allowedFeatures.includes(feature);\n}\n```",
    hints: ["What happens if `rawJson` is not valid JSON — does the caller know?", "The `tenantId` is described as coming from a Snowflake warehouse. What does JavaScript's `number` type do with integers larger than 2^53 - 1?", "Both `getMaxTokens` and `isFeatureAllowed` use `cachedConfig!`. What is the risk at startup or after a reset?"],
    starter: "",
    idealAnswer: "**1. `JSON.parse` Without try-catch — correctness & reliability (critical)**\n`JSON.parse` throws a `SyntaxError` if the input string is malformed. This function calls it with no error handling. If a webhook delivers a truncated payload, the exception propagates uncaught and crashes the calling inference request. Every call to `loadConfig` with external input must be wrapped:\n```typescript\nexport function loadConfig(rawJson: string): ModelConfig {\n  let parsed: unknown;\n  try {\n    parsed = JSON.parse(rawJson);\n  } catch {\n    throw new Error(`Invalid config JSON: ${rawJson.slice(0, 80)}`);\n  }\n  // validate shape here (see point 2)\n  ...\n}\n```\n\n**2. `as ModelConfig` Type Assertion — no runtime validation (high)**\n`JSON.parse(rawJson) as ModelConfig` is a compile-time-only assertion. If the payload is missing `allowedFeatures`, has `maxTokens` as a string, or any other shape mismatch, TypeScript does not catch it at runtime. A downstream call to `cachedConfig.allowedFeatures.includes(...)` then throws `TypeError: Cannot read properties of undefined`. Use a runtime validator:\n```typescript\nimport { z } from 'zod';\nconst ModelConfigSchema = z.object({\n  tenantId: z.string(), // see point 3\n  modelName: z.string(),\n  maxTokens: z.number().int().positive(),\n  allowedFeatures: z.array(z.string()),\n});\n```\n\n**3. `tenantId: number` Loses Precision for Large IDs (critical)**\nSnowflake row IDs and other warehouse-generated IDs routinely exceed `Number.MAX_SAFE_INTEGER` (2^53 - 1 = 9,007,199,254,740,991). JavaScript's `number` type uses IEEE 754 double precision: any integer larger than MAX_SAFE_INTEGER is rounded to the nearest representable value. This means two distinct Snowflake IDs can silently collide (`9007199254740992 === 9007199254740993` is `true` in JS). The interface should use `string` for `tenantId` (or `BigInt` if arithmetic is needed), and the JSON payload should deliver it as a string:\n```typescript\ninterface ModelConfig {\n  tenantId: string; // keep as string; never convert to number\n  ...\n}\n```\n\n**4. Module-Level `cachedConfig` — Shared Mutable State / Request Bleed in Serverless (high)**\n`cachedConfig` is a module-level singleton. In a long-running Node.js server this is merely a design smell (stale config if multiple tenants share one process). In a serverless/edge environment (Vercel, AWS Lambda), warm instances reuse module state across requests from **different tenants**. A request for Tenant A calls `loadConfig`, and a concurrent request for Tenant B immediately reads `getMaxTokens()` — it may see Tenant A's config. This is a security-relevant request-bleed bug. Config should be passed explicitly through the call chain or scoped to the request context, not stored globally.\n\n**5. Non-Null Assertion `!` on `cachedConfig` — crash risk (medium)**\n`getMaxTokens()` and `isFeatureAllowed()` use `cachedConfig!.prop`. If either is called before `loadConfig` (e.g., during startup, in tests, or after a module reload), the `!` suppresses TS's null check and the call throws `TypeError: Cannot read properties of null`. Use an explicit guard:\n```typescript\nfunction requireConfig(): ModelConfig {\n  if (!cachedConfig) throw new Error('Config not loaded — call loadConfig first');\n  return cachedConfig;\n}\n```\n\n**Summary of categories:**\n- Correctness: JSON.parse without try-catch, non-null assertion\n- Type safety: `as` cast with no runtime validation, `number` for large IDs\n- Security/correctness: module-level mutable state bleeding between requests\n- Precision: Number.MAX_SAFE_INTEGER exceeded by Snowflake IDs",
    rubric: ["Identifies missing try-catch around JSON.parse and explains the SyntaxError crash path", "Flags `as ModelConfig` as providing no runtime shape validation and suggests Zod or a type guard", "Correctly explains Number precision loss for IDs > 2^53 - 1 and recommends string representation", "Identifies module-level `cachedConfig` as a request-bleed risk in serverless/multi-tenant environments", "Catches the non-null assertion on `cachedConfig` and its crash-on-first-call risk", "Bonus: mentions MAX_SAFE_INTEGER value or gives a concrete collision example"],
  },
  {
    id: "cr-ts-sequential-await-pipeline",
    category: "codereview",
    executes: false,
    mode: "text",
    free: false,
    level: "senior",
    title: "TypeScript: Sequential Awaits & Concurrency Cliff in Pipeline Orchestrator",
    company: "Data Engineering Platform · Senior Engineer System Design Round",
    difficulty: "hard",
    prompt: "Review this pipeline orchestrator that validates, enriches, and writes dataset records to a data warehouse. The team says it 'works correctly but is too slow for large batches.' Identify all bugs, type-safety issues, async correctness problems, and performance issues — including any that would appear only under load.\n\n```typescript\n// lib/pipeline-orchestrator.ts\nimport { validateRecord } from './validator';\nimport { enrichRecord } from './enricher';\nimport { warehouseClient } from './warehouse';\n\ninterface RawRecord {\n  id: string;\n  payload: unknown;\n}\n\ninterface ProcessedRecord {\n  id: string;\n  enrichedPayload: object;\n  validatedAt: Date;\n}\n\nasync function processOne(record: RawRecord): Promise<ProcessedRecord> {\n  await validateRecord(record);          // throws on invalid\n  const enriched = await enrichRecord(record);\n  return {\n    id: record.id,\n    enrichedPayload: enriched,\n    validatedAt: new Date(),\n  };\n}\n\nexport async function runPipeline(records: RawRecord[]): Promise<void> {\n  const processed: ProcessedRecord[] = [];\n\n  for (const record of records) {\n    const result = await processOne(record);  // sequential — one at a time\n    processed.push(result);\n  }\n\n  // Write all processed records to the warehouse in one batch\n  await warehouseClient.batchInsert(processed);\n\n  // Notify downstream consumers\n  records.forEach(r => {\n    warehouseClient.publishEvent('record.processed', { id: r.id });  // floating promise\n  });\n}\n```",
    hints: ["How would the total wall-clock time scale if each `processOne` call takes 200 ms and there are 500 records?", "What happens when `publishEvent` rejects — will the caller know?", "Suppose `records` contains 10,000 items and you switch from the for-loop to a bare `Promise.all`. What new problem appears?"],
    starter: "",
    idealAnswer: "**1. Sequential `await` in a for-loop — performance (critical)**\nEach iteration `await`s `processOne` before starting the next. If `processOne` takes 200 ms (network I/O for validate + enrich), 500 records take 500 × 200 ms = **100 seconds** end-to-end. Because `validateRecord` and `enrichRecord` are I/O-bound and independent, they can safely run concurrently. The naive fix is `Promise.all(records.map(processOne))`, but see point 2.\n\n**2. Unbounded `Promise.all` — performance cliff under load (critical)**\nReplacing the loop with `await Promise.all(records.map(processOne))` fires all N operations simultaneously. For 10,000 records this opens 10,000 concurrent HTTP/DB connections, exhausting connection pools, hitting rate limits, and causing cascading failures. The correct fix is **controlled concurrency** via batching or a semaphore library:\n```typescript\nimport PQueue from 'p-queue';\nconst queue = new PQueue({ concurrency: 50 });\nconst processed = await Promise.all(\n  records.map(record => queue.add(() => processOne(record)))\n);\n```\nAlternatively, process in chunks:\n```typescript\nconst BATCH = 50;\nconst processed: ProcessedRecord[] = [];\nfor (let i = 0; i < records.length; i += BATCH) {\n  const chunk = records.slice(i, i + BATCH);\n  const results = await Promise.all(chunk.map(processOne));\n  processed.push(...results);\n}\n```\nThis gives O(N/BATCH) waves of parallel I/O without overwhelming downstream services.\n\n**3. Floating Promises in `forEach` — async correctness (critical)**\n`records.forEach(r => { warehouseClient.publishEvent(...) })` creates N fire-and-forget Promises. `forEach` does not `await` them, so:\n- `runPipeline` returns (and its caller gets `void`) before any event is published.\n- If any `publishEvent` call rejects, the rejection is unhandled — in Node ≥ 15 this terminates the process.\n\nFix: collect and await the publish calls:\n```typescript\nawait Promise.all(\n  records.map(r => warehouseClient.publishEvent('record.processed', { id: r.id }))\n);\n```\nOr, if publish failures are non-critical, at minimum attach `.catch`:\n```typescript\nrecords.forEach(r =>\n  warehouseClient.publishEvent('record.processed', { id: r.id })\n    .catch(err => console.error('Publish failed', r.id, err))\n);\n```\n\n**4. First-Failure Aborts Entire Batch — error handling design (high)**\n`Promise.all` (and the sequential loop) short-circuits on the first rejection from `processOne` (which re-throws from `validateRecord`). A single invalid record cancels processing for all subsequent records. Depending on requirements, prefer `Promise.allSettled` to collect successes and failures separately:\n```typescript\nconst results = await Promise.allSettled(records.map(r => queue.add(() => processOne(r))));\nconst processed = results\n  .filter((r): r is PromiseFulfilledResult<ProcessedRecord> => r.status === 'fulfilled')\n  .map(r => r.value);\nconst failed = results.filter(r => r.status === 'rejected');\n// log / DLQ failed records\n```\n\n**5. `validatedAt: new Date()` — Date Precision / Timezone Ambiguity (low-medium)**\n`new Date()` returns the wall-clock time of the serverless execution environment, which may be UTC or a configured timezone. When records are processed over multiple async turns (different event loop ticks), each `validatedAt` reflects the moment that particular record finished — correct in intent, but if the warehouse stores this as a local-time string without timezone info, cross-region comparisons break. Prefer `new Date().toISOString()` (UTC string) stored as `string`, or a `TIMESTAMPTZ` column.\n\n**6. `enrichedPayload: object` Type — weak typing (low)**\n`object` is one of the weakest non-`any` types in TypeScript: it excludes primitives but accepts any object shape, including `{}`. If `enrichRecord` returns a known schema, define it. This prevents silent mismatches between what the enricher returns and what the warehouse insert expects.\n\n**Summary of categories:**\n- Performance: sequential await (100 s for 500 records), then unbounded Promise.all concurrency cliff\n- Async correctness: floating promises in forEach, unhandled rejections\n- Error handling: all-or-nothing abort vs. partial success with allSettled\n- Type safety: weak `object` type for payload\n- Correctness: Date timezone ambiguity in distributed execution",
    rubric: ["Quantifies the sequential-await cost (N × latency) and explains why it's avoidable for independent I/O", "Warns that naive Promise.all over large arrays creates a concurrency cliff (connection pool exhaustion)", "Proposes controlled concurrency: p-queue, p-limit, or manual chunking with a specific batch size", "Identifies all floating promises in forEach and explains the unhandled-rejection crash risk", "Distinguishes between Promise.all (first-fail abort) and Promise.allSettled (collect partial results) for batch pipelines", "Notes the weak `object` type on `enrichedPayload`", "Bonus: mentions a Dead Letter Queue pattern for failed records, or Date/timezone precision issues"],
  },
];
