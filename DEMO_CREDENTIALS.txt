FieldNotes — Test / Demo Login
================================

Use these credentials on the /login page (email + password):

  Email:     g@g.com
  Password:  test1234

This account has FULL access — every cheat sheet + all of Practice (Pro).

Notes
-----
- Works with no setup (no Supabase needed). It sets a demo session cookie.
- One-click alternative: visit  /api/demo?action=login  (no typing).
- Sign out: the "Sign out" button in the navbar, or visit  /api/demo?action=logout
- Without logging in you are anonymous = PREVIEW ONLY (free practice items +
  locked sheet previews). Sign in with the above to unlock everything.

Changing the credentials
------------------------
Override via environment variables (defaults are the values above):
  DEMO_EMAIL=you@example.com
  DEMO_PASSWORD=your-password
Defined in: lib/entitlements.ts  (DEMO_EMAIL / DEMO_PASSWORD)

⚠ This is a demo/test account, not real auth. Don't ship it as-is to production.
