[
  {
    "q": "What is the difference between `val`, `var`, and `def` in Scala? When should you prefer each?",
    "a": "`val` creates an immutable binding evaluated once at definition time — the reference cannot be reassigned, supporting referential transparency. `var` creates a mutable binding that can be reassigned, breaking functional guarantees and causing thread-safety issues. `def` defines a method or computed value that is re-evaluated every time it is called with no result caching. Prefer `val` by default; reach for `def` for parameterized computations; treat `var` as a code smell unless performance-critical local mutation is the only option.",
    "fj": "I use val for things that don't change and var for things that do — that's basically it.",
    "fs": "I default to `val` everywhere; it enables safe sharing across threads without synchronization. The moment I see a `var`, I ask whether an accumulator or fold achieves the same result immutably.",
    "level": "Jr",
    "asked": 30,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "mutable state leaks",
        "lines": [
          "var count = 0",
          "val fns = (1 to 3).map { i =>",
          "  () => { count += 1; count }",
          "}",
          "// all closures share 'count'",
          "fns.foreach(f => println(f()))",
          "// prints 1, 2, 3 but 'count'",
          "// escapes the loop"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "immutable idiom",
        "lines": [
          "val results = (1 to 3).toList",
          "// pure transform, no shared state",
          "val doubled = results.map(_ * 2)",
          "// [2, 4, 6] — no mutation"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is referential transparency and why do Scala devs care about it?",
        "a": "An expression is referentially transparent if you can replace it with its computed value anywhere in the program without changing behavior. Pure functions and `val` bindings guarantee this. It makes code easier to reason about, test in isolation, and safely parallelize — critical in data pipelines where the same transformation runs on many partitions."
      },
      {
        "q": "Can a `val` hold a mutable object?",
        "a": "Yes. `val buf = new StringBuilder` means `buf` cannot be reassigned, but `buf.append(\"x\")` is still legal because the object itself is mutable. Immutability of the binding is not the same as immutability of the object. Prefer immutable collections (`List`, `Map`) over mutable ones even with `val`."
      }
    ]
  },
  {
    "q": "What does Scala's `case class` give you automatically, and how does structural equality work?",
    "a": "The compiler auto-generates `equals` (field-by-field structural equality), `hashCode`, `toString`, a companion `apply` (so `new` is optional), and `unapply` (for pattern matching). It also provides a `copy` method for non-destructive updates. Two case class instances are `==` if all their fields compare equal, unlike plain classes where `==` delegates to reference identity. Never extend one case class from another — in Scala 3 the compiler rejects it as an error, and in Scala 2 it warns that the auto-generated `equals` would be inconsistent.",
    "fj": "Case classes are basically data holders that print nicely.",
    "fs": "I use case classes as immutable value objects in ADTs. The auto-generated `unapply` is what powers clean pattern matching, and `copy` is how I do non-destructive updates in pipeline transformations.",
    "level": "Jr",
    "asked": 22,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "case class User(id: Int, name: String)",
          "",
          "val u1 = User(1, \"Alice\")",
          "val u2 = User(1, \"Alice\")",
          "println(u1 == u2)    // true (structural)",
          "println(u1 eq u2)  // false (refs)",
          "",
          "val u3 = u1.copy(name = \"Bob\")",
          "// User(1, \"Bob\") — u1 unchanged"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is `eq` vs `==` in Scala?",
        "a": "`==` calls `equals`, which for case classes is structural equality. `eq` is reference equality — it checks whether two references point to the same JVM object. `null eq null` is `true`; `\"foo\" eq \"foo\"` may or may not be `true` depending on string interning. In practice, always use `==` for value comparison and avoid `eq` unless you explicitly need identity semantics."
      },
      {
        "q": "Why can't case classes extend other case classes?",
        "a": "Auto-generated `equals` only compares the fields declared in that class, so a subclass with extra fields would produce false positives. Scala 2 issues a deprecation warning for case-to-case inheritance but still compiles; Scala 3 promoted this to a hard compile error. Use a sealed trait hierarchy or composition instead."
      }
    ]
  },
  {
    "q": "Explain sealed traits and exhaustiveness checking. Why does sealing matter for pattern matching?",
    "a": "A `sealed trait` (or `sealed abstract class`) restricts all direct subclasses to the same compilation unit. This gives the compiler complete knowledge of possible subtypes, enabling exhaustiveness warnings: if your `match` misses a branch, the compiler warns at compile time instead of the program throwing a `MatchError` at runtime. In Scala 3 `enum` is the preferred syntax for simple sealed ADTs, automatically sealing all cases. Unsealed traits silently lose this safety guarantee — a library can add new subtypes and break all existing matches.",
    "fj": "I just put a wildcard `case _ =>` at the bottom to avoid warnings.",
    "fs": "A `_` wildcard silences the compiler but defeats the entire point of a sealed type — you lose compile-time proof of completeness. I keep branches exhaustive and let the compiler catch gaps when I add new variants.",
    "level": "Mid",
    "asked": 26,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "unsealed = silent runtime crash",
        "lines": [
          "trait Shape",
          "case class Circle(r: Double) extends Shape",
          "case class Rect(w: Double, h: Double)",
          "  extends Shape",
          "",
          "def area(s: Shape): Double = s match",
          "  case Circle(r) => Math.PI * r * r",
          "  // Rect missing — MatchError at runtime"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "sealed = compile-time safety",
        "lines": [
          "sealed trait Shape",
          "case class Circle(r: Double) extends Shape",
          "case class Rect(w: Double, h: Double)",
          "  extends Shape",
          "",
          "def area(s: Shape): Double = s match",
          "  case Circle(r) => Math.PI * r * r",
          "  case Rect(w, h) => w * h",
          "  // compiler verifies completeness"
        ]
      }
    ],
    "followups": [
      {
        "q": "How does Scala 3's `enum` improve on sealed traits for ADTs?",
        "a": "Scala 3 `enum` is syntax sugar that automatically seals and creates case objects/classes. It also provides `ordinal`, `values`, and `valueOf` for free, and each case can carry parameters: `enum Expr { case Num(n: Int); case Add(l: Expr, r: Expr) }`. This is more concise than the Scala 2 `sealed trait + case class` boilerplate."
      },
      {
        "q": "What are pattern match guards and what's a gotcha with them?",
        "a": "Guards add an `if` predicate to a case arm: `case x if x > 0 => ...`. The gotcha is that a guard makes the branch non-exhaustive — even if all cases are covered without guards, adding a guard means \"some values of this case go elsewhere\". The compiler will issue an exhaustiveness warning if no default catches the unguarded remainder."
      }
    ]
  },
  {
    "q": "How do Scala extractors and custom `unapply` work? Walk me through a non-case-class example.",
    "a": "`unapply` is the method the pattern-matching compiler calls to destructure a value. Case classes get it for free, but any object can define `unapply(x: T): Option[U]` to become usable as a pattern. Returning `Some(extracted)` signals a match; `None` signals failure. You can also return a `Boolean` for presence-only patterns, or return a tuple via `unapplySeq` for variable-length extraction. Extractors decouple data representation from pattern syntax — useful for parsing, validation, and domain-specific patterns.",
    "fj": "I just use case classes; I've never written a custom unapply.",
    "fs": "I use custom extractors when the match logic can't live in the data class itself — e.g., an `Email` extractor that validates format, keeping validation co-located with the pattern syntax rather than scattered across callers.",
    "level": "Mid",
    "asked": 15,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "object Email:",
          "  def unapply(s: String):",
          "      Option[(String, String)] =",
          "    s.split('@') match",
          "      case Array(user, domain)",
          "        if domain.contains('.') =>",
          "        Some((user, domain))",
          "      case _ => None",
          "",
          "\"alice@example.com\" match",
          "  case Email(u, d) =>",
          "    println(s\"$u at $d\")",
          "  case _ =>",
          "    println(\"invalid\")"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is `unapplySeq` and when would you use it?",
        "a": "`unapplySeq` returns `Option[Seq[T]]` and enables variable-length patterns like `case List(a, b, rest @ _*)`. The standard library's `List` extractor uses `unapplySeq`. You'd write your own for CSV rows, command-line args, or any variable-arity destructuring."
      }
    ]
  },
  {
    "q": "Walk me through `Option`, `Either`, and `Try` — when do you reach for each?",
    "a": "`Option[A]` models optional presence: `Some(value)` or `None`. Use it when absence is routine and expected (e.g., a lookup that may miss). `Either[L, R]` models a two-way result; by convention `Left` carries the error and `Right` the success value. Use it when the error type matters and you want to propagate it (validation, parsing). `Try[A]` is effectively `Either[Throwable, A]` but for exception-throwing code: `Success(value)` or `Failure(exception)`. All three are monadic — use `map`, `flatMap`, and `for`-comprehensions to chain operations without null checks or try-catch nesting.",
    "fj": "I use Option for nulls and wrap risky calls in try-catch.",
    "fs": "My default is `Option` for simple optionality and `Either[AppError, A]` for domain error channels. I reserve `Try` for wrapping Java APIs that throw exceptions, then immediately convert to `Either` at the boundary to keep the rest of the codebase exception-free.",
    "level": "Jr",
    "asked": 24,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "import scala.util.Try",
          "",
          "def parse(s: String): Either[String, Int] =",
          "  Try(s.toInt).toEither",
          "    .left.map(_.getMessage)",
          "",
          "val result = for",
          "  a <- parse(\"10\")",
          "  b <- parse(\"5\")",
          "yield a + b",
          "// Right(15)"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is the difference between `.getOrElse` and `.fold` on an Option?",
        "a": "`.getOrElse(default)` returns the wrapped value or a default. `.fold(ifEmpty)(f)` applies function `f` to the value if `Some`, or returns `ifEmpty` if `None` — both branches in one call. `fold` is more composable when you want to transform the value and handle absence in one expression without an intermediate `map`."
      },
      {
        "q": "Why is `Option.get` considered a code smell?",
        "a": "`.get` throws `NoSuchElementException` on `None`, defeating the entire purpose of `Option` which was to eliminate runtime null-like crashes. It's essentially a controlled `NullPointerException`. Use `getOrElse`, `fold`, `map`, or pattern matching instead. The only acceptable use of `.get` is after an explicit `isDefined` guard, which is still worse than pattern matching for clarity."
      }
    ]
  },
  {
    "q": "How does Scala's `for`-comprehension desugar? Show me the `flatMap`/`map`/`withFilter` expansion.",
    "a": "A `for`-comprehension is pure syntactic sugar. Each `<-` generator except the last becomes a `flatMap`; the last `<-` with `yield` becomes a `map`. Guards (`if` clauses) desugar to `withFilter`. The result is that any type implementing these methods — `Option`, `Either`, `Future`, custom types — works seamlessly in `for`. The key interview gotcha: `Either` in Scala 2.12+ is right-biased, so `for` chains go through `Right`; a `Left` short-circuits the whole comprehension, threading the error upward.",
    "fj": "for-comprehensions are just loops with yield.",
    "fs": "I reach for `for`-comprehensions whenever I'm chaining three or more monadic operations — the desugaring to `flatMap`/`map` is mechanical, but the readability gain over nested `flatMap` calls is significant, especially in validation pipelines.",
    "level": "Mid",
    "asked": 20,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "// Sugar:",
          "val r = for",
          "  x <- Some(10)",
          "  if x > 5",
          "  y <- Some(x * 2)",
          "yield y + 1",
          "",
          "// Desugars to:",
          "val r = Some(10)",
          "  .withFilter(_ > 5)",
          "  .flatMap(x =>",
          "    Some(x * 2).map(y => y + 1))"
        ]
      }
    ],
    "followups": [
      {
        "q": "Why can't you use `if` guards in a `for`-comprehension over `Either`?",
        "a": "`Either` does not implement `withFilter`, so the compiler rejects guard expressions in a `for` over `Either`. Use an explicit `if` inside a `map` or extract the guard into a helper that returns `Either[L, R]`. Cats provides `EitherT[F, L, R]` as the idiomatic workaround — wrap the `Either` in the monad transformer and guards work inside the `for`. Alternatively, extract the guard into a helper returning `Either[L, R]`."
      },
      {
        "q": "What happens if you put a `<-` over a `Future` inside a `for`?",
        "a": "Each `flatMap` in the desugared chain starts the next `Future` only after the previous completes — sequential execution. If you want concurrent execution, start all `Future`s *outside* the `for`-comprehension (so they're eagerly created), then zip them inside the `for`. Starting a `Future` inside a `flatMap` is a common concurrency footgun."
      }
    ]
  },
  {
    "q": "Explain Scala's higher-order functions, closures, and currying. What is partial application?",
    "a": "A higher-order function takes functions as parameters or returns functions. Closures are function values that capture variables from their enclosing scope. Currying transforms `f(a, b)` into `f(a)(b)` — a chain of single-argument functions. Partial application fixes some arguments of a curried function, producing a new function expecting the rest. In Scala you partially apply with `_`: `val double = multiply(2, _)`. A key Spark gotcha: a closure that captures a non-serializable outer class field will throw `NotSerializableException` when shipped to executors.",
    "fj": "Currying is when you pass a function to another function.",
    "fs": "I use currying to create reusable pipeline stages — for example a `def transform(config: Config)(row: Row): Row` lets me pre-bake the config once with `transform(cfg)` and use the resulting function as a DataFrame UDF without re-evaluating config on every row.",
    "level": "Mid",
    "asked": 18,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "def multiply(a: Int)(b: Int): Int =",
          "  a * b",
          "",
          "val triple = multiply(3) _",
          "// triple: Int => Int",
          "",
          "List(1, 2, 3).map(triple)",
          "// List(3, 6, 9)"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is the difference between a method and a function value in Scala?",
        "a": "Methods (`def`) belong to an object/class and are not first-class values themselves — they can't be passed as arguments directly. Function values (`val f: Int => Int = ...` or a lambda) are `FunctionN` instances and are first-class. Eta expansion converts a method into a function value; Scala 3 does this automatically in most contexts (explicit `_` is no longer required)."
      },
      {
        "q": "Why can capturing a `var` in a Spark closure cause silent wrong results?",
        "a": "The closure is serialized and shipped to each executor as a snapshot. If the driver mutates the `var` after shipping, executors have stale copies. Similarly, mutations on executors never propagate back to the driver. Use `Accumulator` for distributed counters, or restructure to pure transformations. This is a classic Spark interview trap."
      }
    ]
  },
  {
    "q": "Compare `List`, `Vector`, and `LazyList` in Scala's immutable collections. When does each win?",
    "a": "`List` is a singly-linked cons list: O(1) head/prepend, O(n) indexed access. Ideal for recursive functional patterns and short lists. `Vector` is a 32-way trie providing effectively O(1) (log₃₂) random access, append, and update — the go-to for large immutable sequences with mixed access patterns. `LazyList` (renamed from `Stream` in Scala 2.13) is lazy and memoizes: elements are computed on demand, enabling infinite sequences. The key `LazyList` gotcha: holding a reference to its head prevents GC of evaluated tails — a memory leak in long-running pipelines.",
    "fj": "I always use List because it's the default.",
    "fs": "For any sequence over ~1000 elements where I need random access, I use `Vector`. For truly lazy, potentially infinite pipelines — like reading a log feed — I use `LazyList` but ensure I don't hold a head reference across the entire traversal.",
    "level": "Mid",
    "asked": 16,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "// List: O(1) prepend, O(n) index",
          "val xs = 1 :: 2 :: 3 :: Nil",
          "",
          "// Vector: O(log32 n) all ops",
          "val v = Vector(1, 2, 3)",
          "val v2 = v.updated(1, 99)",
          "// Vector(1, 99, 3) — v unchanged",
          "",
          "// LazyList: infinite, memoized",
          "val nats: LazyList[Int] =",
          "  LazyList.from(1)",
          "nats.take(5).toList  // List(1,2,3,4,5)"
        ]
      }
    ],
    "followups": [
      {
        "q": "What are collection views and when do they help?",
        "a": "`.view` wraps a collection in a lazy view that fuses consecutive `map`/`filter`/`flatMap` operations, avoiding intermediate collections. Unlike `LazyList`, views are not memoized — each traversal recomputes. Use views for large collections where you'll apply several transformations and either take a prefix or convert once with `.toList`."
      },
      {
        "q": "What is the difference between `Stream` and `LazyList`?",
        "a": "`Stream` (Scala 2) has a strict head — the first element is always computed. `LazyList` (Scala 2.13+) is fully lazy: even the head is not computed until forced. This matters for recursive definitions that would loop forever with a strict head. `Stream` is deprecated; always use `LazyList` in modern code."
      }
    ]
  },
  {
    "q": "How do Scala traits work as mixins? What is trait linearization and how does it resolve the diamond problem?",
    "a": "Traits can contain concrete method implementations and be mixed into classes with `extends`/`with`. When multiple traits define the same method, linearization determines which wins. The algorithm builds a left-to-right, depth-first chain: for `class D extends A with B with C`, the order is `D → C → B → A → ...`, right-to-left-most-specific first, with duplicates removed keeping only the rightmost occurrence. The rightmost (last `with`) trait's implementation wins. `super` in a trait calls the next in line, enabling stackable-trait decorator patterns.",
    "fj": "The last trait's method wins when there's a conflict.",
    "fs": "Linearization gives a deterministic MRO. I exploit it intentionally with the stackable-traits pattern — each trait's `super` call delegates down the chain, composing behaviors like logging, validation, and retry without inheritance hierarchies.",
    "level": "Sr",
    "asked": 19,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "trait A { def greet = \"A\" }",
          "trait B extends A {",
          "  override def greet =",
          "    \"B->\" + super.greet",
          "}",
          "trait C extends A {",
          "  override def greet =",
          "    \"C->\" + super.greet",
          "}",
          "class D extends A with B with C",
          "// Linearization: D,C,B,A",
          "new D().greet",
          "// \"C->B->A\""
        ]
      }
    ],
    "followups": [
      {
        "q": "What is the trait initialization order pitfall?",
        "a": "If a `val` in a trait is read during the construction of a mixing class before the subclass's `val` is initialized, you get `null` (or 0 for primitives). The fix is to use `lazy val` or `def` in the trait definition so the value is computed after full initialization. This is the classic 'initialization order problem' that often surfaces in test frameworks and DSLs."
      }
    ]
  },
  {
    "q": "Explain Scala 2 implicits — implicit parameters, implicit conversions, and implicit resolution scope.",
    "a": "Implicit parameters are method arguments the compiler fills in automatically from in-scope implicit values. Implicit conversions automatically wrap a value in another type when a method call doesn't type-check directly. The resolution order: (1) local scope and imports, (2) companion objects of the involved types. The critical gotcha: if two equal-priority implicits match, the compiler raises 'ambiguous implicits' — a compile error. Implicit conversions are powerful but dangerous and were restricted in Scala 3 (you must now import `scala.language.implicitConversions`).",
    "fj": "Implicits are like automatic function parameters the compiler fills in.",
    "fs": "I use implicit parameters for type class evidence and execution contexts, but I treat implicit conversions as a last resort — they silently change types and make call sites misleading. In Scala 3 I prefer `given`/`using` and explicit `extension` methods instead.",
    "level": "Mid",
    "asked": 23,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "ambiguous implicits",
        "lines": [
          "implicit val s1: String = \"hello\"",
          "implicit val s2: String = \"world\"",
          "",
          "def greet(implicit s: String) =",
          "  println(s)",
          "",
          "greet",
          "// Error: ambiguous implicit values"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "unique implicit in scope",
        "lines": [
          "implicit val greeting: String = \"hello\"",
          "",
          "def greet(implicit s: String) =",
          "  println(s)",
          "",
          "greet // prints \"hello\""
        ]
      }
    ],
    "followups": [
      {
        "q": "What is the implicit search order in Scala 2 and why does companion object placement matter?",
        "a": "Search order: (1) local scope / explicit imports, (2) imports in enclosing scopes, (3) companion objects of the type being sought and its supertypes. Libraries (like Cats/Circe) put default instances in companion objects so users don't need imports. If you define a conflicting instance locally, it wins over the companion-object instance due to higher priority."
      }
    ]
  },
  {
    "q": "How does Scala 3 replace implicits with `given`/`using`? What changed and why?",
    "a": "Scala 3 splits the overloaded `implicit` keyword into intent-specific syntax. `given` declares a contextual instance (replaces `implicit val/def`). `using` marks a parameter as contextual (replaces `implicit` parameter). `summon[T]` replaces `implicitly[T]` to retrieve a `given` instance. Extension methods are now a first-class `extension` block rather than an implicit class. The motivation was that Scala 2 `implicit` served too many roles (conversions, parameters, evidence), making code harder to read and tooling harder to analyze.",
    "fj": "given/using is just new syntax for implicits.",
    "fs": "The key improvement is intent clarity: `given` is unambiguously an instance for contextual dispatch, `extension` is unambiguously a method on an existing type, and `using` at the call site makes the context visible. I now catch implicit-conversion bugs earlier because the compiler requires an explicit import to use them.",
    "level": "Mid",
    "asked": 21,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "// Scala 2",
          "implicit val ord: Ordering[Int] =",
          "  Ordering.Int",
          "def sort[T](xs: List[T])",
          "    (implicit o: Ordering[T]) = ...",
          "",
          "// Scala 3",
          "given Ordering[Int] = Ordering.Int",
          "",
          "def sort[T](xs: List[T])",
          "    (using Ordering[T]): List[T] = ...",
          "",
          "val inst = summon[Ordering[Int]]"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is a context function type in Scala 3?",
        "a": "A context function type `A ?=> B` is a function that takes an implicit/given `A` as context. It lets you define DSLs where a given is automatically threaded through without explicit `using` everywhere. For example `type Txn[A] = DbSession ?=> A` lets all functions in a transaction block share the session without passing it explicitly."
      },
      {
        "q": "How do extension methods differ between Scala 2 and Scala 3?",
        "a": "Scala 2 uses `implicit class` wrappers that box the receiver and require a wrapper type in scope. Scala 3 `extension` is a first-class construct: `extension (s: String) def shout: String = s.toUpperCase + \"!\"`. No wrapper allocation, no implicit class clutter, and tooling can find extensions directly. In Scala 3 you can also add multiple methods in one `extension` block."
      }
    ]
  },
  {
    "q": "What are type classes in Scala and how do context bounds implement them?",
    "a": "A type class is a trait parameterized over a type that defines behavior for that type without inheritance: `trait Show[A] { def show(a: A): String }`. Instances are provided as `given` (Scala 3) or `implicit val` (Scala 2) values. Context bound syntax `[A: Show]` is shorthand for `(using Show[A])`, enabling ad-hoc polymorphism — adding behavior to types you don't own. This is how Cats, Circe, and Spark Encoders all work. `summon[Show[A]]` or `implicitly[Show[A]]` retrieves the instance inside the method.",
    "fj": "Type classes are like interfaces that you implement for your class.",
    "fs": "I use type classes whenever I need to add behavior to third-party types or create principled abstractions like `Encoder`, `Codec`, or `Monoid`. Context bounds keep call sites clean while the compiler proves at compile time that an instance exists — a missing instance is a compile error, not a runtime failure.",
    "level": "Sr",
    "asked": 17,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "trait Show[A]:",
          "  def show(a: A): String",
          "",
          "given Show[Int] with",
          "  def show(n: Int) = s\"Int($n)\"",
          "",
          "def display[A: Show](a: A): String =",
          "  summon[Show[A]].show(a)",
          "",
          "display(42) // \"Int(42)\""
        ]
      }
    ],
    "followups": [
      {
        "q": "What is coherence and why is it important in type class design?",
        "a": "Coherence means there is at most one instance of a type class for a given type in scope. Without coherence, different parts of a program could use different `Ordering[Int]` instances, producing inconsistent sorted results. Scala doesn't enforce global coherence like Haskell, but the convention is one canonical instance per type, placed in the companion object, overridable locally with explicit imports."
      }
    ]
  },
  {
    "q": "Explain variance in Scala: covariance (`+A`), contravariance (`-A`), and invariance. Give the intuitive model.",
    "a": "Variance describes how subtyping of a generic type relates to subtyping of its type parameter. `List[+A]` is covariant: if `Dog <: Animal` then `List[Dog] <: List[Animal]` — read-only producers can be covariant. `Function1[-A, +B]` is contravariant in input: a function that handles `Animal` can stand in wherever a `Dog`-handler is expected. Mutable containers must be invariant: if `Array[Dog] <: Array[Animal]` you could write a `Cat` into an `Array[Dog]`, causing heap pollution. The mnemonic: producers are covariant (+), consumers are contravariant (-), mutable is invariant.",
    "fj": "`+A` means you can use a subtype where a supertype is expected.",
    "fs": "I always ask: does this type produce values or consume them? Producers covariant, consumers contravariant — same as Haskell's functor/contravariant split. Covariant mutable containers are a soundness hole, which is why `Array` and `mutable.Buffer` are invariant.",
    "level": "Sr",
    "asked": 22,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "class Cage[+A](val animal: A)",
          "// Cage[Dog] <: Cage[Animal] ✓",
          "",
          "trait Vet[-A]:",
          "  def treat(a: A): Unit",
          "// Vet[Animal] <: Vet[Dog] ✓",
          "// (a vet for any animal can treat a dog)",
          "",
          "// Mutable = invariant:",
          "// Array[Dog] is NOT Array[Animal]",
          "// avoids: arr(0) = new Cat"
        ]
      }
    ],
    "followups": [
      {
        "q": "Why can't you put a value of type `A` in a covariant `F[+A]` position as a method parameter?",
        "a": "If `F[+A]` allowed `def put(a: A)`, and `F[Dog] <: F[Animal]`, then through the subtype reference you could call `put(new Cat)` on what is actually an `F[Dog]`, corrupting it. The compiler enforces that covariant type parameters appear only in output (return type / field) positions, and contravariant parameters only in input positions."
      },
      {
        "q": "What is the relationship between `Nothing` and variance?",
        "a": "`Nothing` is a subtype of every type in Scala. Because `List` is covariant, `List[Nothing]` is a subtype of `List[Any]`. This is why `Nil` (typed as `List[Nothing]`) can be assigned to a `List[String]` — `Nothing <: String` so `List[Nothing] <: List[String]`. The same logic makes `None` (typed `None.type <: Option[Nothing] <: Option[A]`) assignable to any `Option[A]`."
      }
    ]
  },
  {
    "q": "How do Scala `Future`s work, and what is the `ExecutionContext` pitfall with blocking operations?",
    "a": "`Future[T]` starts executing immediately when created — it is eager, not lazy. It runs on threads from the given `ExecutionContext`. The global pool (`ExecutionContext.global`) is sized for CPU-bound work (number of CPUs). Blocking a thread in this pool (JDBC call, `Thread.sleep`, `Await.result`) starves other futures of threads and can cause a deadlock under load. The fix: use `scala.concurrent.blocking { ... }` which signals the pool to spawn a compensating thread, or use a dedicated blocking `ExecutionContext` backed by a cached thread pool.",
    "fj": "I just call Await.result to get the value out of a Future.",
    "fs": "I never call `Await.result` in production code — that's a thread block that poisons the executor pool. I chain with `flatMap`/`for`, and for truly blocking I/O I use a dedicated thread pool with `blocking { }` to signal the pool manager.",
    "level": "Sr",
    "asked": 20,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "blocks global thread pool",
        "lines": [
          "import scala.concurrent._",
          "import ExecutionContext.Implicits.global",
          "",
          "Future {",
          "  // JDBC — blocks a global thread",
          "  db.query(\"SELECT ...\")  ",
          "}"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "dedicated blocking pool",
        "lines": [
          "import scala.concurrent.blocking",
          "",
          "val ioEc =",
          "  ExecutionContext.fromExecutorService(",
          "    Executors.newCachedThreadPool())",
          "",
          "Future {",
          "  blocking { db.query(\"SELECT ...\") }",
          "}(ioEc)"
        ]
      }
    ],
    "followups": [
      {
        "q": "Why is `Future` eager in Scala and what's the implication for retry logic?",
        "a": "`Future { expr }` evaluates `expr` immediately when the line executes, not when the `Future` is composed. Storing a `Future` in a `val` then using it multiple times does NOT re-execute the computation — it reuses the single result. To build retry logic you need a `def` that returns a new `Future` each time it's called, not a shared `val`."
      },
      {
        "q": "How would you sequence multiple Futures in parallel vs. sequentially?",
        "a": "Create all `Future`s before entering the `for`-comprehension so they start in parallel, then use `for { a <- fa; b <- fb } yield (a, b)`. If you create `fb` inside `flatMap` of `fa`, they're sequential. For a list of futures, `Future.sequence(list)` runs them all and collects results, failing fast on the first failure."
      }
    ]
  },
  {
    "q": "What is a by-name parameter (`=> T`) in Scala, and how does it differ from `lazy val`?",
    "a": "A by-name parameter `f: => A` wraps the passed expression as a thunk and re-evaluates it every time `f` is referenced inside the method — like calling a `def`. A `lazy val` is evaluated at most once: computed on first access, then cached. The combined pattern `def memoize(block: => A): A = { lazy val v = block; v }` gives you a by-name parameter that evaluates exactly once. By-name enables control-flow abstractions like custom `while` loops, retry combinators, and logging DSLs that only evaluate messages when logging is enabled.",
    "fj": "By-name is for lazy evaluation — same as lazy val.",
    "fs": "The key distinction is once vs. many times. `lazy val` = memoized; by-name = re-evaluated on each reference. I use by-name for combinators where I want to defer evaluation conditionally, and `lazy val` when I want initialization deferred but idempotent.",
    "level": "Mid",
    "asked": 14,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "var counter = 0",
          "def byName(x: => Int): Unit =",
          "  println(x) // evaluates once",
          "  println(x) // evaluates again",
          "",
          "byName({ counter += 1; counter })",
          "// prints 1 then 2",
          "",
          "// lazy val — computed once:",
          "lazy val computed = {",
          "  println(\"init\")",
          "  42",
          "}",
          "println(computed) // \"init\" then 42",
          "println(computed) // 42 (no re-init)"
        ]
      }
    ],
    "followups": [
      {
        "q": "Give a real-world use case for a by-name parameter.",
        "a": "A logging helper: `def debug(msg: => String): Unit = if (log.isDebug) log.debug(msg)`. The string interpolation `s\"expensive $calc\"` is only evaluated when debug is actually enabled. Without by-name, the string is built on every call regardless. This pattern is common in Spark's internal logging and in test assertion libraries."
      }
    ]
  },
  {
    "q": "What are Scala 3 `opaque type` aliases? How do they improve on Scala 2 value classes?",
    "a": "An `opaque type UserId = Int` creates a distinct type at the type level but compiles to plain `Int` at runtime — no boxing overhead. Outside the defining object/package, callers cannot see the underlying `Int` representation; only operations explicitly exported work. This provides a zero-cost newtype pattern. Scala 2 `AnyVal` value classes also avoid boxing in simple cases, but they leak the underlying type in a number of situations (generics, arrays, pattern matching) causing unexpected boxing and loss of abstraction.",
    "fj": "Opaque types are just type aliases with a new name.",
    "fs": "I use `opaque type` for domain IDs, email addresses, and monetary amounts. It catches accidental mix-ups (`UserId` vs `ProductId`) at compile time with zero runtime cost. Scala 2 value classes were fragile — you'd box in a `List[UserId]` — but opaque types are rock-solid.",
    "level": "Sr",
    "asked": 12,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "object Domain:",
          "  opaque type UserId = Int",
          "  opaque type ProductId = Int",
          "",
          "  def userId(n: Int): UserId = n",
          "  def productId(n: Int): ProductId = n",
          "",
          "import Domain.*",
          "val uid: UserId = userId(1)",
          "val pid: ProductId = productId(1)",
          "",
          "// Compile error — types are distinct:",
          "// val x: UserId = pid"
        ]
      }
    ],
    "followups": [
      {
        "q": "When would you still use a value class instead of an opaque type?",
        "a": "In Scala 2 codebases where opaque types are unavailable, `AnyVal` value classes are the best option. Also when you need to extend a trait (value classes can extend a trait with no abstract methods, enabling polymorphism), which opaque types don't support directly."
      }
    ]
  },
  {
    "q": "Explain Scala 3 union types (`A | B`). How do they differ from sealed traits for modeling alternatives?",
    "a": "Union types let you write `def accept(x: Int | String): Unit = ...` without any pre-planned hierarchy. The compiler tracks the type and enforces exhaustive pattern matching. Sealed traits require you to create marker traits and extend them before you can have a disjoint sum type. Union types are open — types from separate codebases can be unioned without modification — while sealed traits are closed. The tradeoff: sealed ADTs carry structured methods and can be extended with behavior; union types are pure type-level constructs best for small, ad-hoc alternatives.",
    "fj": "Union types are like Either but cleaner.",
    "fs": "I use union types for public API parameters that accept a few unrelated types — avoids forcing callers to wrap values. For domain models with rich behavior I still prefer sealed traits/enums because they're self-documenting and can carry methods.",
    "level": "Sr",
    "asked": 14,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "type StringOrInt = String | Int",
          "",
          "def process(v: StringOrInt): String =",
          "  v match",
          "    case s: String => s\"str:$s\"",
          "    case n: Int    => s\"int:$n\"",
          "",
          "// No sealed hierarchy needed",
          "process(\"hi\")  // \"str:hi\"",
          "process(42)    // \"int:42\""
        ]
      }
    ],
    "followups": [
      {
        "q": "What is an intersection type in Scala 3?",
        "a": "`A & B` denotes a type that is both `A` and `B` simultaneously. It replaces `with` in type position for compound types: `def merge(x: Comparable & Serializable)`. Intersection types are commutative (`A & B` = `B & A`) unlike `with` in Scala 2 which was order-dependent. They're used for type-safe mixin constraints without creating a new named type."
      }
    ]
  },
  {
    "q": "How does significant indentation (optional braces) work in Scala 3, and when should you use it?",
    "a": "Scala 3 introduces indentation-sensitive parsing as an option: a colon at the end of certain keywords (`if`, `for`, `match`, `class`, `def`, etc.) opens an indented region instead of requiring curly braces. The rule is that statements in the same indented block must align at the same column. Braces still work and can be freely mixed; the style is chosen per file or block. The official guidance is to use significant indentation for new Scala 3 code, especially for conciseness in DSLs, but the Scala team allows either in a mixed codebase.",
    "fj": "You just remove curly braces and use spaces like Python.",
    "fs": "The parser resolves indentation via the `<indent>/<outdent>` token model — it's not whitespace-sensitive everywhere, only after specific keywords. I enable it in new Scala 3 modules but keep braces in shared codebases with Scala 2 contributors to avoid training friction.",
    "level": "Jr",
    "code": [
      {
        "lang": "scala",
        "lines": [
          "// Scala 3 — optional braces",
          "def abs(n: Int): Int =",
          "  if n >= 0 then n",
          "  else -n",
          "",
          "class Point(x: Int, y: Int):",
          "  def distSq =",
          "    x * x + y * y"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is the `then` keyword in Scala 3?",
        "a": "`then` disambiguates `if` expressions without braces: `if cond then expr else expr`. It replaces the need for a brace or newline to separate the condition from the then-branch, making the intent explicit in one-liner form. In Scala 2 you'd write `if (cond) expr else expr` with mandatory parentheses around the condition; Scala 3 drops both."
      }
    ]
  },
  {
    "q": "How does Scala 3's `enum` improve algebraic data type (ADT) modeling over Scala 2?",
    "a": "Scala 3 `enum` is first-class syntax for sealed ADTs. Simple cases become `case` entries without boilerplate. Parameterized cases work like case classes: `enum Expr { case Num(n: Int); case Add(l: Expr, r: Expr) }`. The compiler auto-generates `ordinal`, `fromOrdinal`, and `values` for simple enums, enabling serialization. Enums can implement traits and have methods. Compared to Scala 2's `sealed trait + case class + companion object` triple, Scala 3 enums reduce the ADT ceremony by roughly 60%.",
    "fj": "Scala 3 enums are like Java enums but nicer.",
    "fs": "I use `enum` for every domain sum type in Scala 3. The `ordinal`/`values` generation is a gift for JSON codecs — Circe's automatic derivation works cleanly with enums. For recursive ADTs like expression trees, enum parameterized cases are as expressive as the Scala 2 approach with far less noise.",
    "level": "Jr",
    "asked": 11,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "// Scala 3",
          "enum Color:",
          "  case Red, Green, Blue",
          "",
          "enum Shape:",
          "  case Circle(r: Double)",
          "  case Rect(w: Double, h: Double)",
          "",
          "def area(s: Shape): Double = s match",
          "  case Shape.Circle(r) => Math.PI*r*r",
          "  case Shape.Rect(w,h) => w * h"
        ]
      }
    ]
  },
  {
    "q": "What is the Dataset vs DataFrame distinction in Spark Scala, and when does compile-time type safety matter?",
    "a": "`DataFrame` is `Dataset[Row]` — schema is known only at runtime; column name typos and type mismatches surface as runtime exceptions. `Dataset[T]` is typed: `T` is a case class or primitive, and the compiler checks transformations at compile time. Encoders (implicit `Encoder[T]`) bridge JVM objects to Spark's Tungsten binary format. The tradeoff: typed Datasets pay a small serialization overhead and make some optimizations harder for Catalyst; DataFrames are more dynamically flexible. For production pipelines with many transformations, the compile-time safety of `Dataset[T]` pays off significantly in catching bugs before deployment.",
    "fj": "Dataset is just DataFrame with generics.",
    "fs": "I use `Dataset[T]` at pipeline boundaries where the schema is the contract, and switch to `DataFrame` operations for complex analytics where Catalyst needs freedom to optimize. The encoder is the linchpin — if your case class contains a non-Encoder-supported type (like a custom wrapper), Spark will fail at runtime rather than compile time unless you write a custom encoder.",
    "level": "Sr",
    "asked": 25,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "case class Event(id: Long,",
          "  ts: Long, kind: String)",
          "",
          "// Compile-time safe:",
          "val ds: Dataset[Event] =",
          "  spark.read.parquet(\"..\")",
          "    .as[Event]  // needs Encoder[Event]",
          "",
          "ds.filter(_.id > 0)  // typed lambda",
          "// TypeError at compile if field wrong",
          "",
          "// DataFrame — runtime schema only:",
          "val df = spark.read.parquet(\"..\")",
          "df.filter(col(\"id\") > 0)"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is an Encoder in Spark and how is it derived?",
        "a": "An `Encoder[T]` describes how to serialize a JVM object to Spark's Tungsten binary row format and back, enabling on-the-fly access to individual fields without full deserialization. For case classes, `Encoders.product[T]` (available implicitly via `spark.implicits._`) derives it via reflection/macro. For primitives, predefined encoders exist. Custom encoders are needed for third-party types like `java.sql.Timestamp` or wrapper types."
      },
      {
        "q": "What performance difference exists between typed Dataset operations and DataFrame operations?",
        "a": "DataFrame/SQL operations run entirely through Catalyst optimizer and Tungsten codegen — Spark can reorder, push-down, and fuse operations freely. Typed `Dataset` lambdas (`.map`, `.filter` with Scala functions) are opaque to Catalyst: they run as black-box JVM code, losing query planning benefits. For complex analytics, prefer DataFrame column expressions or SQL. Use typed Dataset for row-level domain logic where type safety outweighs query optimization."
      }
    ]
  },
  {
    "q": "Why does a Spark closure that captures a non-serializable object field throw `NotSerializableException`?",
    "a": "Spark serializes the entire closure (the function and all its captured references) via Java serialization before shipping it to executors. If the closure captures a reference to an outer `class` instance (e.g., a field access like `this.config.value`), Spark tries to serialize the entire enclosing object. If that class is not `Serializable`, Spark throws `NotSerializableException` at job submission time. The fix is to extract the needed values into local `val`s inside the closure so only primitives/strings are captured, not the object reference.",
    "fj": "Make the class implement Serializable.",
    "fs": "I always extract driver-side values to local vals before the lambda: `val threshold = this.config.threshold; ds.filter(_.value > threshold)`. That captures only the `Int`, not `this`. Implementing `Serializable` on a class with database connections or SparkContext fields causes worse problems — those objects must never be shipped to executors.",
    "level": "Sr",
    "asked": 24,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "captures non-serializable this",
        "lines": [
          "class Pipeline(config: Config) {",
          "  // Config is not Serializable",
          "  def run(ds: Dataset[Row]) =",
          "    ds.filter(_.getLong(0) >",
          "      config.threshold)",
          "  // Captures 'this' -> crash!",
          "}"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "capture only the value",
        "lines": [
          "class Pipeline(config: Config) {",
          "  def run(ds: Dataset[Row]) = {",
          "    val t = config.threshold",
          "    // captures Int, not Config",
          "    ds.filter(_.getLong(0) > t)",
          "  }",
          "}"
        ]
      }
    ],
    "followups": [
      {
        "q": "How do you handle driver-side configuration that executors need, such as database credentials?",
        "a": "Use broadcast variables for large read-only objects: `val bcCfg = sc.broadcast(config)` then access `bcCfg.value` inside the closure. Broadcast variables are serialized once per executor rather than once per task, and Spark serializes only the broadcast wrapper. For credentials specifically, also consider Spark's secret management or environment variables so secrets never appear in lineage logs."
      }
    ]
  },
  {
    "q": "What is the difference between `==` and `eq` in Scala, and when does `==` call `equals` vs. `eq`?",
    "a": "`==` in Scala is defined as null-safe `equals` — it delegates to the `equals` method and handles `null` without throwing NPE (unlike Java's `==`). For case classes, `equals` is auto-generated as structural equality. `eq` is reference equality, checking JVM object identity. `null == null` is `true` via Scala's null-safe `==`; `null eq null` is also `true`. The common gotcha: comparing two `new String(\"a\")` instances with `==` returns `true` (structural), while `eq` returns `false` (different heap objects).",
    "fj": "== is structural equality and eq is reference equality — that's all.",
    "fs": "The subtle part is that `==` calls `equals` which is overridable. For collection-heavy data work I verify that custom domain types correctly override `equals`/`hashCode` as a pair — a broken `hashCode` can make case class instances disappear silently from `Set`s and `Map`s.",
    "level": "Jr",
    "asked": 16,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "val a = new String(\"hello\")",
          "val b = new String(\"hello\")",
          "",
          "println(a == b)   // true (equals)",
          "println(a eq b)   // false (refs)",
          "",
          "case class Id(n: Int)",
          "val x = Id(1)",
          "val y = Id(1)",
          "println(x == y)   // true",
          "println(x eq y)   // false"
        ]
      }
    ]
  },
  {
    "q": "How does pattern matching work with guards, nested patterns, and tuple destructuring?",
    "a": "Pattern matching in Scala is an expression (returns a value). Guards add `if` predicates to a case: `case x if x > 0 =>`. Nested patterns destructure recursively: `case Some(List(h, _*)) =>`. Tuple destructuring uses positional patterns: `case (a, b) =>`. The compiler checks exhaustiveness for sealed types and warns about unreachable cases if a more specific pattern appears after a general one. Unlike Java `switch`, Scala patterns bind variables, destructure, and guard in one unified syntax.",
    "fj": "Pattern matching is like a switch statement with extra syntax.",
    "fs": "I use nested patterns aggressively in ETL to decode nested JSON structures into domain types in one match expression, letting the compiler prove all cases are handled. The `@` binding operator (`case h @ Head(_) =>`) lets me capture the whole matched structure while still destructuring — essential for logging the raw value on error.",
    "level": "Mid",
    "asked": 19,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "def describe(x: Any): String = x match",
          "  case 0             => \"zero\"",
          "  case n: Int if n>0 => s\"pos $n\"",
          "  case n: Int        => s\"neg $n\"",
          "  case (a, b)        => s\"pair $a,$b\"",
          "  case Some(v)       => s\"some $v\"",
          "  case None          => \"none\"",
          "  case _             => \"other\""
        ]
      }
    ],
    "followups": [
      {
        "q": "What does the `@` operator do in a pattern?",
        "a": "`case full @ Some(v) =>` binds `full` to the entire matched `Some(v)` and `v` to the inner value. This lets you use both the whole structure and its parts without extracting twice. It's especially useful when you want to log the raw input for debugging while also working with the destructured value."
      }
    ]
  },
  {
    "q": "What is the `collect` method on Scala collections and how does it differ from `filter` + `map`?",
    "a": "`collect` takes a partial function and applies it only to elements for which it is defined — combining filter and map atomically in one pass. It avoids the two-pass cost of `filter` followed by `map` and produces cleaner code. The partial function's `isDefinedAt` performs the filtering, and `apply` does the transformation. A common use is extracting and transforming a specific subtype from a mixed collection: `list.collect { case Right(v) => v }` — no explicit `filter(_.isRight).map(_.right.get)` needed.",
    "fj": "collect is like filter but with a transformation.",
    "fs": "I reach for `collect` whenever I'm filtering on type or shape while simultaneously extracting a field. It reads as a single intent rather than two sequential operations, and on large in-memory collections the single-pass character matters for cache friendliness.",
    "level": "Mid",
    "code": [
      {
        "lang": "scala",
        "lines": [
          "val mixed: List[Either[String, Int]] =",
          "  List(Right(1), Left(\"e\"),",
          "       Right(3), Left(\"x\"))",
          "",
          "// collect: one pass, clean intent",
          "val nums = mixed.collect {",
          "  case Right(n) => n * 2",
          "}",
          "// List(2, 6)"
        ]
      }
    ]
  },
  {
    "q": "Explain Scala's `Try` and how to recover from failures using `recover` and `recoverWith`.",
    "a": "`Try[A]` is either `Success(value)` or `Failure(exception)`. `recover` takes a partial function from `Throwable` to `A` and converts matching failures to successes — useful for providing default values on specific exceptions. `recoverWith` takes a partial function returning `Try[A]`, enabling fallback computations that may themselves fail. Chaining: `Try(parse(s)).recover { case _: NumberFormatException => 0 }`. For non-exceptional expected failures, convert early to `Either` with `.toEither` for richer error typing.",
    "fj": "Just wrap the whole thing in a try-catch.",
    "fs": "I use `Try` only at Java API boundaries, then immediately `.toEither.left.map(mapException)` to get a typed error domain. Scattered `Try`/`recover` chains across business logic are a smell — they mix exception semantics with functional flow control.",
    "level": "Mid",
    "asked": 13,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "import scala.util.Try",
          "",
          "def safeDiv(a: Int, b: Int): Try[Int] =",
          "  Try(a / b)",
          "",
          "val r = safeDiv(10, 0)",
          "  .recover {",
          "    case _: ArithmeticException => -1",
          "  }",
          "// Success(-1)"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is the difference between `recover` and `recoverWith`?",
        "a": "`recover` maps a `Failure` to a plain value, producing `Success(value)`. `recoverWith` maps a `Failure` to another `Try`, enabling a fallback that may itself fail (e.g., trying an alternative source). The name mirrors `flatMap` vs `map`: `recoverWith` is the flatMap equivalent for the failure channel."
      }
    ]
  },
  {
    "q": "What is the difference between `map` and `flatMap` on `Option`, and why does nesting arise without `flatMap`?",
    "a": "`map` applies a function `A => B` to the `Some(a)` value, producing `Option[B]`. If the function itself returns an `Option`, you get `Option[Option[B]]` — a nested wrapper. `flatMap` applies `A => Option[B]` and flattens one layer, producing `Option[B]` directly. This flattening is what makes monadic chaining work: multiple nullable lookups compose with `flatMap` or `for` without accumulating layers. The same principle applies to `Either`, `Future`, and `List`.",
    "fj": "I use map and if I get a double-wrapped result I just call .get.get or flatten after.",
    "fs": "Double-wrapping is the signal to switch from `map` to `flatMap`. In pipeline code I watch for `Option[Option[_]]` return types as a code smell — it means a `map` was used where `flatMap` was needed, and the outer layer will swallow the inner `None` silently on subsequent operations.",
    "level": "Jr",
    "asked": 18,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "nested Option",
        "lines": [
          "def findUser(id: Int): Option[User] =",
          "  db.get(id)",
          "def findEmail(u: User): Option[String] =",
          "  u.email",
          "",
          "// map -> Option[Option[String]]",
          "val bad = findUser(1).map(findEmail)"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "flatMap flattens",
        "lines": [
          "// flatMap -> Option[String]",
          "val good =",
          "  findUser(1).flatMap(findEmail)",
          "",
          "// or with for:",
          "val good2 = for",
          "  u <- findUser(1)",
          "  e <- findEmail(u)",
          "yield e"
        ]
      }
    ]
  },
  {
    "q": "What is tail recursion and the `@tailrec` annotation in Scala? Why does the JVM require it explicitly?",
    "a": "Tail recursion is a recursive call that is the very last action in a function, allowing the compiler to replace the call with a `GOTO` — reusing the current stack frame instead of allocating a new one. Scala's `@tailrec` annotation instructs the compiler to either optimize the call or emit a compile error if the tail-call optimization is impossible (e.g., the recursive call is not in tail position). The JVM has no native tail-call opcode, so Scala performs this transformation at the bytecode level — without `@tailrec`, a recursive function on a large input will throw `StackOverflowError`.",
    "fj": "I use recursion and trust the stack is big enough.",
    "fs": "I annotate every recursive function with `@tailrec` as a correctness assertion — if I refactor the recursion out of tail position the code won't compile, rather than crashing at runtime on production data. For mutual recursion, `@tailrec` doesn't apply; I use a trampoline via `scala.util.control.TailCalls`.",
    "level": "Mid",
    "asked": 15,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "not tail-recursive",
        "lines": [
          "def sum(n: Int): Long =",
          "  if n <= 0 then 0",
          "  else n + sum(n - 1)",
          "  // + is after recursive call",
          "  // StackOverflow on large n"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "tail-recursive with accumulator",
        "lines": [
          "import scala.annotation.tailrec",
          "",
          "@tailrec",
          "def sum(n: Int, acc: Long = 0): Long =",
          "  if n <= 0 then acc",
          "  else sum(n - 1, acc + n)",
          "  // recursive call in tail position"
        ]
      }
    ]
  },
  {
    "q": "How does Scala's type inference work, and where does it fail requiring explicit annotations?",
    "a": "Scala uses bidirectional local type inference (Hindley-Milner extended). It infers types of `val` bindings, lambda parameters when the expected type is known, and most expression types. It fails and requires explicit annotations in: (1) recursive methods — the return type of a recursive `def` must be stated; (2) overloaded methods where the argument type must disambiguate; (3) `_` wildcards in complex expressions; (4) implicit/given resolution where the expected type drives which instance is selected. Scala 3 improves inference for polymorphic lambdas and reduces annotation burden vs. Scala 2.",
    "fj": "Scala infers almost everything, you rarely need to annotate.",
    "fs": "I annotate public API return types explicitly even when inference could handle it — it's documentation and prevents accidental type widening from breaking call sites when the implementation changes. I let inference do its job for local vals and lambdas where the context is clear.",
    "level": "Mid",
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "recursive def needs return type",
        "lines": [
          "// Compile error:",
          "def fib(n: Int) =  // missing type",
          "  if n <= 1 then n",
          "  else fib(n-1) + fib(n-2)"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "explicit return type",
        "lines": [
          "def fib(n: Int): Int =",
          "  if n <= 1 then n",
          "  else fib(n-1) + fib(n-2)"
        ]
      }
    ]
  },
  {
    "q": "What is a companion object in Scala, and how does it differ from `static` in Java?",
    "a": "A companion object is an `object` with the same name as a class, defined in the same file. It shares access to the class's private members. Methods and vals on the companion are accessed without an instance (`ClassName.method`), similar to Java `static`. The key difference: the companion is a proper object with its own type (`ClassName.type`), can extend traits and be passed as a value, and participates in implicit/given resolution. This makes factory methods, type class instances, and default arguments far more flexible than Java statics.",
    "fj": "Companion object is just like static methods in Java.",
    "fs": "The companion-as-trait-instance pattern is powerful: placing a type class `given` instance in the companion means it's always in scope without imports. I use companions for `apply` factory methods that enforce invariants (e.g., returning `Option[T]` when construction can fail) — much cleaner than constructors that throw.",
    "level": "Jr",
    "asked": 14,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "class Email private(val address: String)",
          "",
          "object Email:",
          "  def of(s: String): Option[Email] =",
          "    if s.contains('@')",
          "      then Some(new Email(s))",
          "      else None",
          "",
          "Email.of(\"x@y.com\") // Some(Email)",
          "Email.of(\"invalid\")  // None"
        ]
      }
    ]
  },
  {
    "q": "How do Scala's `foldLeft` and `foldRight` differ? What are the stack implications?",
    "a": "`foldLeft` processes elements left-to-right, accumulating in tail-recursive fashion — it is stack-safe on standard collections. `foldRight` processes right-to-left; on `List` the standard library implementation reverses and uses `foldLeft`, so it is also stack-safe. On `LazyList` and custom structures, `foldRight` may stack-overflow for large inputs because the recursive call is not in tail position. `foldLeft` is generally preferred for deterministic, efficient accumulation. `reduceLeft`/`reduceRight` skip the initial accumulator but throw on empty collections — always use `fold` variants for safety.",
    "fj": "foldLeft goes left-to-right and foldRight goes right-to-left — that's the only difference.",
    "fs": "Stack safety is the critical distinction in production. I default to `foldLeft` for all aggregations and only reach for `foldRight` when the right-to-left traversal order is semantically needed (e.g., building a `List` without reversing). For recursive structures like trees I implement explicit trampolined folds.",
    "level": "Mid",
    "code": [
      {
        "lang": "scala",
        "lines": [
          "val xs = List(1, 2, 3, 4)",
          "",
          "// foldLeft: (((0+1)+2)+3)+4 = 10",
          "xs.foldLeft(0)(_ + _)",
          "",
          "// Build list in original order",
          "// foldRight preserves order:",
          "xs.foldRight(List.empty[Int])",
          "  (_ :: _)",
          "// List(1,2,3,4) — no reverse needed"
        ]
      }
    ]
  },
  {
    "q": "What are Scala's `implicit class` (Scala 2) and `extension` (Scala 3) methods? Show a practical data-engineering example.",
    "a": "Both let you add methods to existing types without modifying them. In Scala 2, `implicit class RichDF(df: DataFrame)` wraps the type and defines extension methods — every call boxes the `DataFrame` into the wrapper. In Scala 3, `extension (df: DataFrame) def addAuditCols: DataFrame = ...` is zero-overhead and does not create a wrapper type. Extensions are looked up from `given` instances in scope, making them composable with type classes. For data engineering, extension methods on `DataFrame` for audit columns, schema assertions, or pipeline chaining produce dramatically cleaner code.",
    "fj": "I'd just create a utility object with static methods.",
    "fs": "Utility objects scatter call sites and break fluent chaining. Extension methods let me write `df.validate.dedup.addAuditCols` instead of `addAuditCols(dedup(validate(df)))`. In Scala 3 I group all DataFrame pipeline extensions in one `extension` block, keeping the DSL co-located with its tests.",
    "level": "Mid",
    "asked": 16,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "// Scala 3 extension on DataFrame",
          "extension (df: DataFrame)",
          "  def addAuditCols: DataFrame =",
          "    df",
          "      .withColumn(\"_ingest_ts\",",
          "        current_timestamp())",
          "      .withColumn(\"_run_id\",",
          "        lit(runId))",
          "",
          "// Usage — reads like a method:",
          "raw.transform(clean).addAuditCols"
        ]
      }
    ]
  },
  {
    "q": "Explain the difference between `class`, `abstract class`, `trait`, and `object` in Scala. When do you choose each?",
    "a": "`class` is a concrete type that can be instantiated; `abstract class` cannot be instantiated and may have abstract members — use when you need a constructor parameter in the base type or Java interop (Java can extend abstract classes but not traits). `trait` is the Scala interface/mixin; supports multiple inheritance and can carry concrete implementations. `object` is a singleton — use for companion objects, module-level utilities, and entry points. Scala 3 adds `opaque type`, `enum`, and `given` as first-class alternatives for patterns that previously required creative class/object combinations.",
    "fj": "trait is like an interface and abstract class is like an abstract class in Java.",
    "fs": "I default to `trait` for shared behavior. I use `abstract class` only when I need constructor parameters in the base (traits can't have class-style constructor params in Scala 2, though Scala 3 `trait` parameters change this) or for Java interop. `object` is my module boundary — it's a value, can extend traits, and participates in the type system unlike Java static classes.",
    "level": "Jr",
    "asked": 20
  },
  {
    "q": "What is Scala's `Either.sequence` / `traverse` pattern and why is it important for batch validation?",
    "a": "Standard library `Either` has no built-in `sequence`. Libraries like Cats provide `Traverse[List].sequence(List[Either[E, A]]): Either[E, List[A]]` which fails fast on the first `Left`. For accumulating multiple errors (fail slow), use `Validated[NonEmptyList[E], A]` from Cats — it gathers all failures rather than short-circuiting. In batch validation (e.g., validating 1000 CSV rows) fail-fast is usually wrong — you want all errors reported at once. `ValidatedNel` is the canonical Cats pattern for this.",
    "fj": "I'd fold over the list checking each Either and stop on the first error.",
    "fs": "Fail-fast is the wrong default for user-facing validation — users want to see all errors, not fix one and re-submit. I use `Cats ValidatedNel` for multi-field and multi-row validation, accumulating a `NonEmptyList[Error]`, then convert to `Either` only at the service boundary where I need monadic chaining.",
    "level": "Sr",
    "asked": 13,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "import cats.data.ValidatedNel",
          "import cats.syntax.all.*",
          "",
          "type VNel[A] = ValidatedNel[String, A]",
          "",
          "def checkAge(n: Int): VNel[Int] =",
          "  if n >= 0 then n.validNel",
          "  else \"negative age\".invalidNel",
          "",
          "def checkName(s: String): VNel[String] =",
          "  if s.nonEmpty then s.validNel",
          "  else \"empty name\".invalidNel",
          "",
          "(checkName(\"\"), checkAge(-1))",
          "  .mapN((n, a) => s\"$n:$a\")",
          "// Invalid(NonEmptyList(",
          "//   \"empty name\", \"negative age\"))"
        ]
      }
    ]
  },
  {
    "q": "What is the stackable traits pattern in Scala and how does `super` work in a trait chain?",
    "a": "Stackable traits use linearization to compose behaviors: each trait's implementation calls `super.method`, delegating to the next trait in the linearization order. You build a stack by mixing multiple behavior-modifying traits onto a base class. Each layer wraps the previous without knowing the concrete class. This pattern is how Scala achieves aspect-like programming (logging, retry, caching) without frameworks. The key constraint: traits that call `super.method` must `abstract override` the method, signaling that they are meant to be stacked onto a class providing a concrete implementation.",
    "fj": "I'd use a decorator pattern with separate wrapper classes.",
    "fs": "The stackable pattern beats wrapper classes for behavior composition because the layering is defined at the `class D extends Base with Logging with Retry` use-site, not buried in wrapper constructors. Adding a new behavior is one `with Trait` addition. I use it extensively for test doubles — stack a `RecordingTrait` onto the real service to capture calls.",
    "level": "Sr",
    "code": [
      {
        "lang": "scala",
        "lines": [
          "abstract class Writer:",
          "  def write(s: String): Unit",
          "",
          "trait UpperCase extends Writer:",
          "  abstract override def write(s: String) =",
          "    super.write(s.toUpperCase)",
          "",
          "trait Trimming extends Writer:",
          "  abstract override def write(s: String) =",
          "    super.write(s.trim)",
          "",
          "class StdOut extends Writer:",
          "  def write(s: String) = println(s)",
          "",
          "val w = new StdOut",
          "  with Trimming with UpperCase",
          "w.write(\" hello \") // \"HELLO\""
        ]
      }
    ]
  },
  {
    "q": "How does Spark handle UDFs in Scala, and what are the type-safety and performance pitfalls?",
    "a": "A Spark UDF wraps a Scala function: `val upper = udf((s: String) => s.toUpperCase)`. UDFs are black boxes — Catalyst cannot optimize through them, they disable predicate pushdown, and serialization/deserialization overhead applies on every row. Type safety is lost: the UDF's return type must be declared explicitly or Spark infers it incorrectly. In Spark 3.x, typed `Dataset` transformations and built-in `functions.*` are always preferable. Use UDFs only for logic that has no built-in equivalent. Prefer `mapPartitions` over per-row UDFs when initialization cost (e.g., model loading) is high.",
    "fj": "UDFs are great for custom logic that SQL can't do.",
    "fs": "UDFs are a last resort. I scan `org.apache.spark.sql.functions._` first — almost all common transforms are there as Catalyst-optimized expressions. When I must write a UDF, I always specify the return type explicitly, add null handling, and benchmark against a `Dataset[T].map` to choose the lesser evil.",
    "level": "Sr",
    "asked": 18,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "UDF — no optimization",
        "lines": [
          "val parseDate =",
          "  udf((s: String) =>",
          "    LocalDate.parse(s))",
          "// Catalyst can't push filters",
          "// past this opaque function"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "built-in expression",
        "lines": [
          "import org.apache.spark.sql.functions.*",
          "",
          "df.withColumn(\"date\",",
          "  to_date(col(\"dateStr\"),",
          "    \"yyyy-MM-dd\"))",
          "// Catalyst-optimizable"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is `mapPartitions` and when does it outperform `map` with a UDF?",
        "a": "`mapPartitions(iter => ...)` receives the full partition iterator, letting you initialize expensive resources once per partition rather than once per row. Loading an ML model, opening a DB connection, or reading a lookup file inside `mapPartitions` is O(partitions) initialization cost vs O(rows) for per-row `map`/UDF. The tradeoff is that all rows of a partition stay in memory simultaneously, so watch partition size."
      }
    ]
  },
  {
    "q": "Explain Scala's `lazy val` thread-safety guarantee and its double-checked locking implementation.",
    "a": "A `lazy val` in Scala is thread-safe by default: the JVM-level implementation uses a `volatile` flag and synchronized block to guarantee single initialization under concurrent access. The first thread to evaluate it acquires a monitor lock, computes the value, sets the volatile flag; subsequent threads see the flag and skip evaluation. This is the double-checked locking (DCL) pattern — safe on JVM because `volatile` ensures memory visibility. The cost: each access reads a `volatile` field. For hot code paths this overhead can be measurable; use an eager `val` if the object is always needed.",
    "fj": "lazy val is thread-safe because Scala makes it synchronized.",
    "fs": "The volatile flag read is the per-access cost of `lazy val`. In a tight loop over millions of rows I've seen `lazy val` fields cause measurable slowdowns from repeated volatile reads. For initialization-heavy singletons in Spark transformations I initialize eagerly in the enclosing scope once and use a plain `val`.",
    "level": "Sr",
    "asked": 12,
    "code": [
      {
        "lang": "scala",
        "lines": [
          "object Config:",
          "  // Initialized once, thread-safe",
          "  lazy val schema: StructType =",
          "    buildExpensiveSchema()",
          "",
          "// First caller computes,",
          "// subsequent callers read flag.",
          "// Safe across multiple Spark tasks",
          "// accessing the driver object."
        ]
      }
    ]
  },
  {
    "q": "How do you handle resource management in Scala? What does `scala.util.Using` provide over try-finally?",
    "a": "`scala.util.Using` (added in 2.13) implements the loan pattern for any `AutoCloseable`: it opens the resource, passes it to the block, and always closes it — returning a `Try[A]` so cleanup errors are not swallowed. `Using.Manager` handles multiple resources in order, closing all even if one close fails, accumulating exceptions via suppression. Compared to manual try-finally, `Using` is composable, avoids exception-swallowing when both the body and close fail, and integrates with `Try` chains. For functional effect systems (ZIO, Cats Effect), use `ZIO.acquireRelease` or `Resource[F, A]` for composable lifecycle management.",
    "fj": "I use try-finally to close resources the same as in Java.",
    "fs": "Manual try-finally swallows the original exception if `close()` also throws. `Using` surfacing both via suppressed exceptions is the correct behavior. For complex pipelines I prefer Cats Effect `Resource` which composes arbitrary cleanup actions in `flatMap` chains without nesting.",
    "level": "Mid",
    "code": [
      {
        "lang": "scala",
        "lines": [
          "import scala.util.Using",
          "",
          "val result: Try[Int] =",
          "  Using(new FileInputStream(\"f.bin\")) {",
          "    stream => stream.read()",
          "  }",
          "",
          "// Multiple resources:",
          "Using.Manager { use =>",
          "  val in = use(new FileInputStream(\"a\"))",
          "  val out = use(new FileOutputStream(\"b\"))",
          "  out.write(in.readAllBytes())",
          "} // both closed in reverse order"
        ]
      }
    ]
  },
  {
    "q": "What is Scala's `PartialFunction` type and how does it differ from `Function1`?",
    "a": "`PartialFunction[A, B]` extends `A => B` but adds `isDefinedAt(a: A): Boolean` — it is only defined for a subset of inputs. The compiler builds `PartialFunction` instances from `{ case ... }` literal blocks. Key operations: `orElse` chains two partial functions (try first, fall back), `andThen` composes with a total function, `lift` converts to `A => Option[B]`. The `collect` method on collections uses `isDefinedAt` internally. Never call `.apply` on a `PartialFunction` without checking `isDefinedAt` — you'll get a `MatchError`.",
    "fj": "A PartialFunction is just a function that doesn't handle all inputs.",
    "fs": "I use `PartialFunction` with `orElse` for building extensible command dispatchers and event routers — each handler covers its own cases and the chain composes. The `lift` method is indispensable for converting legacy `PartialFunction` APIs to safe `Option`-returning code.",
    "level": "Mid",
    "code": [
      {
        "lang": "scala",
        "lines": [
          "val div: PartialFunction[Int, Int] =",
          "  { case n if n != 0 => 100 / n }",
          "",
          "div.isDefinedAt(0) // false",
          "div.lift(0)        // None",
          "div.lift(5)        // Some(20)",
          "",
          "val fallback: PartialFunction[Int, Int] =",
          "  { case _ => -1 }",
          "",
          "(div orElse fallback)(0) // -1"
        ]
      }
    ]
  },
  {
    "q": "Describe how Spark's Catalyst optimizer interacts with Scala Dataset operations. What breaks optimization?",
    "a": "Catalyst is Spark's query planner. DataFrame and SQL column expressions are represented as an AST that Catalyst can inspect, rewrite (predicate pushdown, constant folding, projection pruning), and codegen. When you use `Dataset[T].map(f)` with a Scala lambda, the lambda is an opaque JVM function — Catalyst cannot see inside it, so it wraps it as `MapPartitions` with no optimization. Mixing typed `Dataset` lambdas with DataFrame column operations in the same query plan forces a serialization boundary (deserialize to JVM objects, apply lambda, re-serialize). Minimize typed Dataset lambda chains for analytics; prefer column expressions.",
    "fj": "Catalyst just makes everything faster automatically.",
    "fs": "The serialization boundary between typed Dataset and DataFrame operations is the subtlety most engineers miss. In a mixed plan, Spark inserts `DeserializeToObject` and `SerializeFromObject` nodes — visible in `df.queryExecution.optimizedPlan`. I use `.toDF()` to cross back to DataFrame mode after typed operations, giving Catalyst a chance to optimize the analytical portion.",
    "level": "Sr",
    "asked": 16,
    "followups": [
      {
        "q": "What is predicate pushdown and how does it interact with Parquet?",
        "a": "Predicate pushdown means Catalyst rewrites a filter so it is applied at the data-source level before data enters Spark. For Parquet, row-group statistics (min/max per column) allow Spark to skip entire row groups without reading them. This requires the filter to be expressed as a column expression (`col(\"year\") === 2024`), not a Scala lambda, because Catalyst can serialize column expressions to the Parquet reader but cannot serialize opaque lambdas."
      }
    ]
  },
  {
    "q": "How do you write a Scala type class instance for a third-party type you don't control, and what is the orphan instance problem?",
    "a": "You define a `given` (Scala 3) or `implicit val` (Scala 2) in a dedicated object or package object for the third-party type: `given Show[JavaLocalDate] with { def show(d: LocalDate) = d.toString }`. The orphan instance problem: if you define this instance in a file that is not the companion of `Show` nor of `LocalDate`, the instance is not automatically in scope — users must import it explicitly. Libraries like Cats deliberately place third-party instances in importable modules (e.g., `cats.instances.java.*`) for this reason. Coherence issues arise if two libraries both provide an orphan instance for the same type — users must pick one.",
    "fj": "I'd extend the interface and add the method I need.",
    "fs": "Third-party types can't be extended, and that's the whole point of type classes. I package orphan instances in a dedicated `instances` object, document the import clearly, and treat it as a contract — if a downstream library adds a conflicting instance, the user sees an ambiguous implicit error that surfaces the conflict rather than silently picking the wrong behavior.",
    "level": "Sr"
  },
  {
    "q": "How do you architect a large Scala FP codebase using tagless final? What are the trade-offs vs. free monads?",
    "a": "Tagless final encodes effects as abstract type constructor `F[_]` constrained by type classes (`Monad[F]`, `Concurrent[F]`, etc.). Algebras are traits parameterized on `F`: `trait UserRepo[F[_]] { def find(id: UserId): F[Option[User]] }`. Interpreters provide concrete `F` at the wiring layer (`IO`, `Future`, or a test `Id`). Trade-offs vs. free monads: tagless final has no intermediate AST — you cannot inspect or transform the program before running it, which makes introspection and batching harder but avoids the overhead of AST construction and interpretation. Free monads excel when you need to serialize or analyze the effect description; tagless final excels when composition and testability are the goals. In early 2026, tagless final with Cats Effect 3 `IO` is the dominant production pattern; free monads are largely niche.",
    "fj": "I'd use tagless final because it's what Cats uses.",
    "fs": "The architectural payoff of tagless final is that the test suite runs with a pure `Id` interpreter — no threading, no mocking frameworks. I structure the codebase so each module declares only the capability it needs (`Sync[F]`, `Logger[F]`), never `IO` directly. This keeps modules honest about their effects and lets us swap interpreters: production uses `IO`, integration tests use a `WriterT`-based logger that captures output for assertions.",
    "level": "Staff",
    "code": [
      {
        "accent": "fix",
        "lang": "scala",
        "label": "tagless final algebra + interpreter",
        "lines": [
          "// Algebra — F is abstract",
          "trait EventStore[F[_]]:",
          "  def append(e: Event): F[Unit]",
          "  def read(id: StreamId): F[List[Event]]",
          "",
          "// Production interpreter",
          "class KafkaEventStore[F[_]: Async](",
          "    producer: KafkaProducer[F])",
          "  extends EventStore[F]:",
          "  def append(e: Event) =",
          "    producer.send(e.toRecord)",
          "  def read(id: StreamId) = ???",
          "",
          "// Test interpreter",
          "class InMemoryEventStore[F[_]: Sync]",
          "  extends EventStore[F]:",
          "  private val store =",
          "    scala.collection.mutable.Map.empty",
          "  def append(e: Event) =",
          "    Sync[F].delay { store(e.id) = e }",
          "  def read(id: StreamId) =",
          "    Sync[F].delay { store.values.toList }"
        ]
      }
    ],
    "followups": [
      {
        "q": "How do you handle cross-cutting concerns like tracing in tagless final?",
        "a": "Define a `Trace[F[_]]` algebra with `span(name: String)(fa: F[A]): F[A]` and thread it through as a capability. Interpreters can wrap calls with OpenTelemetry spans for production and use a no-op for tests. Avoid passing `Tracer` objects directly — keep the capability abstract so you don't leak observability library types into your domain modules."
      },
      {
        "q": "When would you choose free monads over tagless final in a Scala system?",
        "a": "Free monads are justified when you need to interpret or transform the effect description before executing it — e.g., query deduplication by inspecting the AST, converting database calls to batches, or compiling to a different backend entirely. They're also useful for embedded DSLs where the program is data that can be serialized or replayed. If you just need testability and effect polymorphism, tagless final has lower overhead."
      }
    ]
  },
  {
    "q": "What are the sbt build standards you enforce on a multi-module Scala project at scale, and how do you manage dependency conflicts?",
    "a": "At scale, a multi-module sbt build should define a root `project/` with shared settings in a `commonSettings` `Def.settings` sequence applied to every subproject. Key standards: pin Scala version and major library versions in one place (`ThisBuild / scalaVersion`); use `dependencyOverrides` sparingly but deterministically for convergence conflicts — document every override with a comment explaining which transitive dependency caused it; enable `evictionWarningOptions` or `dependencyCheck` in CI to catch new conflicts early. Use `sbt-assembly` or `sbt-native-packager` for fat-jar/Docker builds; `sbt-dependency-graph` to visualize the tree. For large teams, a shared `plugins.sbt` managed as a submodule keeps plugin versions consistent across repositories. Avoid `%%` wildcard version ranges — pin everything.",
    "fj": "I add dependencies and let sbt resolve conflicts automatically.",
    "fs": "Automatic eviction is silent in sbt — it picks the highest version it sees without telling you why. I run `sbt evicted` in CI and fail the build on any eviction where the selected version differs from what I pinned. Every `dependencyOverride` gets a comment: `// forced by jackson-databind 2.15.3 pulled in via aws-sdk-v2`. That comment becomes the paper trail when an upgrade breaks something.",
    "level": "Staff",
    "code": [
      {
        "accent": "fix",
        "lang": "scala",
        "label": "structured multi-module build.sbt",
        "lines": [
          "// build.sbt",
          "ThisBuild / scalaVersion := \"3.4.2\"",
          "ThisBuild / organization := \"com.example\"",
          "",
          "lazy val commonSettings = Seq(",
          "  scalacOptions ++= Seq(",
          "    \"-Wunused:all\",",
          "    \"-Xfatal-warnings\"",
          "  ),",
          "  Test / fork := true",
          ")",
          "",
          "lazy val core = project",
          "  .settings(commonSettings)",
          "  .settings(",
          "    libraryDependencies ++= Seq(",
          "      \"org.typelevel\" %% \"cats-effect\"",
          "        % \"3.5.4\"",
          "    )",
          "  )",
          "",
          "lazy val api = project",
          "  .dependsOn(core)",
          "  .settings(commonSettings)"
        ]
      }
    ],
    "followups": [
      {
        "q": "How do you handle Scala 2 vs Scala 3 cross-compilation in a library used by multiple teams?",
        "a": "Use `crossScalaVersions := Seq(\"2.13.14\", \"3.4.2\")` and publish artifacts with the `_2.13` / `_3` suffix via `sbt +publish`. Source incompatibilities go in `src/main/scala-2` and `src/main/scala-3` directories which sbt merges automatically per version. Macro code requires separate implementations — Scala 2 macros (`scala.reflect`) don't compile under Scala 3; use `inline` and `transparent inline` equivalents. Keep the cross-build scope narrow: shared utilities cross-compile easily; effect-system code that touches Cats Effect 3 only needs Scala 2.13+."
      }
    ]
  },
  {
    "q": "How do you design a Scala 2→3 migration strategy for a 200k-line production codebase?",
    "a": "A safe migration follows three phases. Phase 1 — compatibility: upgrade to Scala 2.13.x, enable `-Xsource:3` compiler flag to surface Scala 3 syntax conflicts early, fix all warnings-as-errors. Phase 2 — cross-compilation: use `crossScalaVersions` to build on both 2.13 and 3.x simultaneously; fix any source-incompatible patterns (implicit conversions requiring explicit import, `given`/`using` syntax via `scalacOptions += \"-source:future\"`). Phase 3 — Scala 3 only: flip `scalaVersion`, migrate macro code (Scala 2 macros must be rewritten as `inline` functions or Scala 3 macros via `quotes.reflect`), drop Scala 2 workarounds. The hardest part is macros — audit every `scala.reflect` dependency and either find a Scala 3-compatible replacement library or budget rewrite time. `scala-rewrites` and Scalafix rules automate ~60% of mechanical changes.",
    "fj": "Bump the Scala version in build.sbt and fix compile errors.",
    "fs": "The macro surface is the migration's critical path. I start by listing every library that uses macros — Circe, Magnolia, Shapeless, Enumeratum — and checking their Scala 3 support matrix. Libraries that haven't migrated block the entire upgrade. I negotiate with team leads to replace unmaintained libraries before starting, not during. The `-Xsource:3` flag on Scala 2.13 is underrated — it surfaces real incompatibilities six months before you flip the version, when you still have runway to fix them incrementally.",
    "level": "Staff",
    "followups": [
      {
        "q": "What does `Scalafix` do and which rules are most useful for a Scala 2→3 migration?",
        "a": "`Scalafix` is a linting and rewriting tool that applies semantic and syntactic transformations via rules. For a 2→3 migration the most useful rules are: `ProcedureSyntax` (converts `def f(): Unit` procedure style), `ExplicitResultTypes` (adds inferred types to `def`s that will need them in Scala 3), `RemoveUnused` (cleans up unused imports that become errors), and the `scala/scala-rewrites` rule set which handles `Varargs`, `SymbolLiterals`, and `ExplicitNullaryEtaExpansion`. Run Scalafix in CI on the cross-compiled branch to keep the codebase clean on both versions simultaneously."
      }
    ]
  },
  {
    "q": "How do you achieve reliable exactly-once semantics in a Scala + Kafka pipeline, and what does idempotent production look like in code?",
    "a": "Kafka provides exactly-once semantics (EOS) through transactional producers and idempotent delivery. On the producer side: set `enable.idempotence=true` (retries without duplicates per partition-epoch), `acks=all`, `transactional.id` (unique per producer instance for cross-partition atomicity). On the consumer side: set `isolation.level=read_committed` so consumers only see committed messages from transactional producers. In Scala with `fs2-kafka` or `kafka4s`: wrap produce+consume in a `commitBatch`/`transaction` block that commits the Kafka offset and produces the output atomically — if the transaction aborts, the consumer rewinds to the last committed offset. The critical gotcha: transactional producers must call `initTransactions()` before use and `abortTransaction()` on any exception, or the coordinator will hold the epoch and block recovery.",
    "fj": "I'd use at-least-once delivery and deduplicate downstream.",
    "fs": "Deduplication downstream is acceptable when the sink supports it cheaply (upsert by primary key), but it's a leaky abstraction — every consumer must implement its own dedup logic. I prefer transactional EOS at the pipeline layer so consumers receive clean, committed messages. With `fs2-kafka` I scope each transaction to a `Stream` chunk: produce the transformed records and commit the source offsets in one `transaction.commit`, then move to the next chunk. If the app crashes mid-chunk, the uncommitted offsets rewind automatically.",
    "level": "Staff",
    "code": [
      {
        "accent": "fix",
        "lang": "scala",
        "label": "fs2-kafka transactional produce",
        "lines": [
          "// Producer settings for EOS",
          "val producerSettings =",
          "  TransactionalProducerSettings(",
          "    bootstrapServers,",
          "    transactionalId = \"pipeline-1\"",
          "  ).withAcks(Acks.All)",
          "   .withEnableIdempotence(true)",
          "",
          "TransactionalKafkaProducer",
          "  .stream(producerSettings)",
          "  .flatMap { producer =>",
          "    KafkaConsumer",
          "      .stream(consumerSettings)",
          "      .partitionedStream",
          "      .flatMap { partStream =>",
          "        partStream",
          "          .map(transform)",
          "          .groupWithin(500, 1.second)",
          "          .evalMap { chunk =>",
          "            producer.produce(",
          "              TransactionalProducerRecords(",
          "                chunk))",
          "          }",
          "      }",
          "  }"
        ]
      }
    ],
    "followups": [
      {
        "q": "What is the difference between idempotent producer and transactional producer in Kafka?",
        "a": "An idempotent producer (`enable.idempotence=true`) guarantees no duplicates on retries for a single producer session to a single topic partition — Kafka assigns a producer ID and sequence number, discarding redeliveries. A transactional producer adds cross-partition and cross-topic atomicity: all messages in a transaction are visible to `read_committed` consumers atomically, including the offset commit. Transactional implies idempotent. Use idempotent-only when producing to a single topic without needing offset atomicity; use transactional for read-process-write pipelines where the consume offset and produced output must commit together."
      }
    ]
  },
  {
    "q": "How do you tune JVM GC and heap settings for a long-running Scala Spark driver or Akka service, and what does GC pressure look like in a Scala FP codebase?",
    "a": "For Spark drivers in early 2026: G1GC is the default from Java 9+; ZGC is preferable for latency-sensitive drivers (sub-millisecond pauses) — set `-XX:+UseZGC -Xms4g -Xmx4g` (equal min/max prevents resize pauses). For Akka services with high actor message throughput, tune G1GC with `-XX:MaxGCPauseMillis=100 -XX:G1HeapRegionSize=16m`. Scala FP code generates significant short-lived garbage: `Option`, `Either`, case class allocations, and lambda `FunctionN` instances. Escape analysis in HotSpot eliminates many of these on the stack, but large-scale pipelines still create GC pressure. Mitigations: prefer `while` loops over `map`/`filter` for hot inner paths; use `AnyVal` / opaque types to avoid boxing; profile with JFR (Java Flight Recorder) before optimizing. In Spark, executor GC tuning in `spark.executor.extraJavaOptions` is separate from driver — tune both, but executor GC matters more for throughput.",
    "fj": "I use default JVM settings and tune if I see slowness.",
    "fs": "I attach JFR to production Spark drivers during a representative job and look for two signals: long GC pauses (>500ms) indicating heap pressure, and high allocation rate with short object lifetimes indicating the young gen is thrashing. The fix for Scala FP GC pressure is almost never 'stop using FP' — it's right-sizing the young gen (`-XX:NewRatio`), enabling string deduplication for string-heavy workloads, and removing unnecessary boxing in the hot path identified by the profiler.",
    "level": "Staff",
    "followups": [
      {
        "q": "How does ZGC differ from G1GC for Akka-based Scala services?",
        "a": "G1GC is a generational, stop-the-world collector for major collections — pauses grow with heap size. ZGC is a concurrent, non-generational (as of Java 15, generational ZGC added in Java 21) collector targeting sub-millisecond pauses regardless of heap size by doing almost all work concurrently. For Akka services handling time-sensitive messages (latency SLA < 10ms), ZGC is preferred because actor scheduling and message processing should not stall for GC. The trade-off: ZGC uses more CPU for concurrent work and may reduce throughput by ~5-10% vs G1GC in CPU-bound scenarios."
      }
    ]
  },
  {
    "q": "How do you build a Scala build-vs-buy decision framework for a new platform capability — e.g., a streaming compute layer — and what signals push toward Flink vs. Spark Structured Streaming vs. a custom Scala solution?",
    "a": "The decision matrix has three axes: operational complexity, feature fit, and team Scala fluency. Spark Structured Streaming fits teams already running Databricks/EMR, needing micro-batch latency (1–60s), SQL-heavy analytics, and Delta Lake integration — the Scala API is mature and Catalyst handles optimization. Apache Flink fits true event-time streaming with sub-second latency, complex stateful windowing (session windows, pattern detection via CEP), or exactly-once with per-record processing; its Scala API is solid though the DataStream API is lower-level. A custom Scala solution using fs2 + Kafka is justified when the data volume is moderate, the team wants full control of backpressure and resource use, external library dependencies are a compliance concern, or the streaming logic is simple enough that a full streaming engine is operational overhead. Signals to avoid custom: stateful aggregations across partitions, event-time watermarking, or replayable checkpoint recovery — all are solved problems in Flink/Spark that take months to rebuild correctly.",
    "fj": "I'd use Spark because we already have it.",
    "fs": "Incumbent bias toward Spark is the most common mistake I see at Staff level. The right question is: what are the latency, state, and exactly-once requirements? If the answer is sub-second with complex state, Spark Structured Streaming will fight you at every turn — its stateful operators are limited and micro-batch introduces inherent latency floors. I've migrated pipelines from Spark SS to Flink when event-time correctness was non-negotiable, and the operational gain from Flink's native checkpoint recovery outweighed the retraining cost within two quarters.",
    "level": "Staff",
    "followups": [
      {
        "q": "What is the operational cost of running Apache Flink on Kubernetes compared to Spark on Databricks?",
        "a": "Flink on Kubernetes (using the Flink Kubernetes Operator) requires managing JobManager/TaskManager pod specs, checkpoint storage (S3/GCS), HA via ZooKeeper or Kubernetes leader election, and rolling upgrades that preserve state. Databricks handles cluster lifecycle, Delta log management, and auto-scaling opaquely. The Flink operational burden is real — plan for 1-2 engineer-weeks of platform work upfront and ongoing on-call for checkpoint recovery and savepoint management. The payoff is lower cost per compute-hour and finer-grained control over resource allocation, which matters at 100+ TB/day scale."
      }
    ]
  },
  {
    "q": "As a principal engineer, how do you set the org-wide Scala/JVM language strategy across 10+ teams, and what governance process ensures the codebase doesn't fragment?",
    "a": "Language strategy at org scale requires three pillars. First, a living Architecture Decision Record (ADR) for every major language/library choice, owned by a principal-level working group with representatives from each team. ADRs capture context, alternatives considered, and known trade-offs — they are the institutional memory that prevents the same debate recurring every 18 months. Second, a shared internal library catalog (an internal Artifactory/Nexus with a curated BOM — Bill of Materials) that all teams import. The BOM pins Cats Effect, Circe, http4s, Spark, and testing library versions; teams cannot override them without a working-group review. This prevents the diamond dependency problem across service boundaries. Third, a Scala style guide enforced by Scalafmt and Scalafix rules checked in CI for every repository. The guide is versioned and changed through a pull-request process, not individual team preference. Governance anti-patterns to avoid: a 'rewrite it all in Scala 3' edict without migration support (creates hidden tech debt); allowing teams to adopt competing effect systems (Cats Effect vs. ZIO) without a clear boundary (makes shared library development impossible).",
    "fj": "I'd let each team choose their own libraries and establish best practices over time.",
    "fs": "Laissez-faire library choice creates an invisible tax: shared libraries become impossible, on-call engineers can't read each other's code, and dependency resolution fails at the service boundary. The BOM is the highest-leverage governance tool I've deployed — one PR to bump Cats Effect in the BOM upgrades 12 services simultaneously, and the CI of each service proves compatibility. The working-group model distributes ownership without creating a bottleneck; each team has a voice, but the decision is recorded and durable.",
    "level": "Principal",
    "followups": [
      {
        "q": "How do you handle a team that wants to adopt ZIO when the org standard is Cats Effect?",
        "a": "The right response is not a blanket ban but a structured evaluation. Charter the team to write a spike with ZIO, then present findings against defined criteria: interop with existing Cats Effect shared libraries (using `zio-interop-cats`), operational familiarity (on-call rotation can debug ZIO fibers), and migration cost if the team is later disbanded and merged. If the evaluation is favorable and the team commits to owning the interop layer, issue an ADR approving ZIO as an approved alternative with a documented boundary. If not, the ADR captures why it was rejected — preventing the conversation from restarting every six months."
      },
      {
        "q": "How do you prevent library version drift across 10+ Scala microservices in a mono-org but polyrepo setup?",
        "a": "Publish an internal BOM artifact (`com.example:platform-bom`) with `dependencyManagement` entries for all approved libraries. Services import it via `dependsOn(bom)` in sbt or `import platform-bom` in build.gradle. Renovate Bot (or Dependabot) is configured at the org level to open PRs against the BOM, not individual services — one review approves the version for all. A nightly CI job runs all service builds against the HEAD of the BOM in draft mode, catching conflicts before the BOM is published. Services that fail must update or negotiate an exception, keeping drift observable and bounded."
      }
    ]
  },
  {
    "q": "How do you evaluate the multi-year language bet of Scala vs. Kotlin for a data engineering org, and what makes Scala uniquely defensible in 2026?",
    "a": "The evaluation must separate use cases: pure data engineering (Spark/Flink pipelines) vs. backend services vs. ML systems. In 2026, Scala's defensible moats are: (1) Spark's native Scala API — the DataFrame/Dataset/Catalyst surface is authored in Scala and the Scala API is always first-class; Python APIs have a serialization layer; (2) the functional ecosystem — Cats Effect, ZIO, fs2, and http4s form a coherent, type-safe effect system with no Kotlin equivalent at the same rigor level; (3) Scala 3's type system (union types, intersection types, opaque types, match types) enables domain modeling precision that Kotlin's type system cannot replicate. Kotlin's moats are: JVM interop simplicity (no implicit/given overhead for Java teams), better tooling experience (IntelliJ IDEA's Kotlin support outpaces Metals in some workflows), and adoption in Android/backend services. For a pure data engineering org, Scala wins on the Spark axis. For a full-stack JVM org building backend services + data, a hybrid strategy is defensible: Kotlin for services, Scala for data — with a shared data model library cross-compiled if needed. The risk of Scala: hiring pipeline is narrower than Kotlin or Python; factor in 3-6 month ramp time for engineers coming from Java/Python.",
    "fj": "Scala is harder to hire for, so I'd lean toward Python or Kotlin.",
    "fs": "Hiring difficulty is real but often overstated — the Scala engineer pool is smaller but far more homogeneous in quality. The median Scala hire understands FP, type systems, and JVM internals; the median Python hire does not. For data engineering, that depth pays for itself in production reliability. My argument to the CTO: Scala's Spark-native advantage is a 5-year moat that compounds — every Spark version ships Scala features first, and Catalyst's pushdown/codegen improvements are only accessible via column expressions authored in Scala or SQL. Python UDFs forgo most of that. If the org is building a serious data platform, betting against Scala is betting against Databricks and against Spark's own development trajectory.",
    "level": "Principal",
    "followups": [
      {
        "q": "How do you quantify the hiring and onboarding cost of Scala vs. Kotlin when presenting to non-technical leadership?",
        "a": "Frame it as total cost of ownership over a 3-year horizon. Inputs: time-to-hire (Scala averages 60-90 days vs. 30-45 for Kotlin/Java in most markets, 2025 data), ramp time to first PR in production (6-12 weeks for Scala FP vs. 2-4 for Kotlin given Java background), attrition risk (Scala engineers are often more senior and expect technical challenges; if the work becomes CRUD services they leave). Offset: Scala engineers often replace 1.5x the headcount equivalent in Python for complex data work. The honest answer is that the break-even is 18-24 months — if leadership needs ROI faster, Kotlin or Python is the honest recommendation."
      }
    ]
  },
  {
    "q": "How do you design and enforce cross-team Scala API contracts so that a breaking change in a shared library is caught before it reaches production in any of 15 dependent services?",
    "a": "Three-layer contract enforcement. Layer 1 — binary compatibility checking with MiMa (Migration Manager for Artifacts): every shared library PR runs `mimaReportBinaryIssues` against the last published version; any method removal, signature change, or class rename fails CI automatically. MiMa distinguishes `@deprecated` (backward compatible) from actual removal. Layer 2 — semantic versioning automation: use `sbt-dynver` or `sbt-release` to enforce that binary-incompatible changes bump the major version, so dependent services that pin to `2.x` never silently pull a breaking change. Layer 3 — consumer-driven contract tests: each consuming service contributes a thin test module that imports the shared library and exercises the exact API surface it uses. These tests live in the shared library's repo and run on every library PR. A breakage in any consumer test is a hard stop. The governance wrapper: a 'shared library change' ADR template that requires listing all consumers, running their contract tests locally, and a 2-week deprecation notice for any public API removal. Emergency patches skip the 2-week window but still require MiMa green.",
    "fj": "I'd publish a new version and ask teams to upgrade when they have time.",
    "fs": "Silent breakage is the most expensive failure mode in a polyrepo org — a library upgrade in service A works, the same upgrade in service B crashes in production three weeks later because B uses a method that was moved. MiMa is the non-negotiable first line of defense. Consumer-driven contract tests are the second — they shift the detection to library PR time, not service deployment time. I've seen orgs run 18 months without MiMa, accumulate dozens of undocumented breaking changes, and then spend a quarter in a 'Scala upgrade freeze' trying to untangle the mess. The governance cost of contract tests is days; the recovery cost of skipping them is months.",
    "level": "Principal",
    "followups": [
      {
        "q": "What is MiMa and how does it detect binary incompatibility in Scala?",
        "a": "MiMa (Migration Manager) compares the public API of two JAR versions at the bytecode level. It detects: removed classes/methods/fields, changed method signatures, changed access modifiers, and changed generic type parameters. For Scala specifically it understands Scala-specific encoding (traits compiled to interfaces + impl classes, case class `copy`, `unapply`, etc.) and reports violations in terms of Scala source concepts, not raw JVM descriptors. Run it via the `sbt-mima-plugin`: set `mimaPreviousArtifacts := Set(organization.value %% name.value % lastReleasedVersion)` and `mimaReportBinaryIssues` becomes a standard task."
      }
    ]
  },
  {
    "q": "How do you diagnose and resolve ambiguous or diverging implicit/given resolution in a large Scala codebase?",
    "a": "The compiler error 'ambiguous implicit values' or 'diverging implicit expansion' is the symptom of one of three root causes: multiple equal-priority instances in scope, a recursive implicit that matches its own search, or a type class derivation loop (e.g., Shapeless `Generic` triggering itself). Diagnosis steps: (1) add `-Xlog-implicits` (Scala 2) or `-explain` (Scala 3) to the failing file — the compiler prints the full search tree, showing every candidate and why each was accepted or rejected; (2) use the `sourcecode` library or `implicitly[Show[T]]` directly at the call site to force the compiler to reveal what it found; (3) for derivation loops, `reify { showInstance }` in Scala 2 or `Expr.summon` in Scala 3 macros to inspect the derived instance. Resolution: the correct fix is almost always narrowing scope — move one instance behind an explicit import, give one instance higher priority via inheritance depth, or switch from a blanket `given` to a more specific one. Avoid 'fix' by adding more implicits; each addition increases resolution time and the chance of future ambiguity.",
    "fj": "I just add another import until it compiles.",
    "fs": "In a large codebase implicit resolution time compounds: sbt incremental compilation can spend 30-40% of its time on implicit search in heavily-derived code. I audit with `-Yprofile-enabled -Yprofile-destination profile.txt` (Scala 3) to find which files and phases are slow, then check if the slowness comes from derivation (`given` derivation with many alternatives is O(n) in the number of candidates). Moving derivations to explicit `derives` clauses or pre-deriving into companion objects speeds up compilation and makes the resolution path transparent.",
    "level": "Staff",
    "asked": 10,
    "code": [
      {
        "accent": "bug",
        "lang": "scala",
        "label": "ambiguous given instances",
        "lines": [
          "// Two equally-ranked instances:",
          "given Show[Int] = n => s\"Int($n)\"",
          "given Show[Int] = n => n.toString",
          "",
          "def display[A: Show](a: A) =",
          "  summon[Show[A]].show(a)",
          "",
          "display(42)",
          "// Error: ambiguous given"
        ]
      },
      {
        "accent": "fix",
        "lang": "scala",
        "label": "single canonical instance",
        "lines": [
          "// One instance in companion object:",
          "object Show:",
          "  given showInt: Show[Int] =",
          "    n => s\"Int($n)\"",
          "",
          "// Local override via explicit import:",
          "import Show.showInt",
          "display(42) // unambiguous"
        ]
      }
    ],
    "followups": [
      {
        "q": "How does implicit resolution priority work via inheritance in Scala 2 and given prioritization in Scala 3?",
        "a": "In Scala 2, an implicit defined in a subtype beats one in a supertype — libraries exploit this: place a low-priority fallback in a trait (`LowPriority`), extend it in the companion, and put the specific instance in the companion. The specific instance wins because the companion is a 'closer' scope. In Scala 3 the same principle applies to `given` definitions: a `given` in the companion object has higher priority than one in a supertrait of the companion. When debugging, the compiler's implicit search always starts at the innermost scope and walks outward — the first match wins."
      },
      {
        "q": "What causes 'diverging implicit expansion' and how do you fix it?",
        "a": "Diverging expansion means the compiler entered an infinite search loop — typically a type class derivation where instance A needs instance B which needs instance A again. The fix is to add an explicit base-case `given` that terminates the recursion before the loop starts, or to break the cycle by using a `def`-based alias given or placing `lazy val` inside the given body so the forward reference resolves after all instances are registered. In Shapeless-heavy codebases, divergence often signals a missing `Generic` or `LabelledGeneric` instance for a type — adding `derives Generic` resolves it."
      }
    ]
  },
  {
    "q": "How do you choose between Cats Effect, ZIO, and plain Futures as the effect system for a new Scala service team?",
    "a": "The decision has four dimensions: team FP maturity, ecosystem fit, operational complexity, and long-term maintainability. Plain `Future`: appropriate when the team has strong Java/Kotlin backgrounds and FP adoption is incremental; the immediate hire pool is larger, but `Future` is eagerly evaluated, lacks structured concurrency, and makes cancellation and resource finalization manual — production bugs from unhandled thread leaks are common at scale. Cats Effect 3 (`IO`): mature, battle-tested in the Typelevel ecosystem; `IO` is lazy and referentially transparent; structured concurrency via fibers; `Resource[F, A]` handles lifecycle; enormous library support (http4s, fs2, Doobie, Skunk). The learning curve is real for non-FP engineers but amortizes over 6-12 months. ZIO 2: similar guarantees to Cats Effect with a different ergonomics philosophy — ZIO's error channel is type-parameterized (`ZIO[R, E, A]`), which forces explicit error modeling but adds type noise; ZIO has strong built-in dependency injection via `ZLayer`; the ZIO ecosystem (ZIO HTTP, ZIO Kafka, ZIO Schema) is growing but less mature than Typelevel. Recommendation: for teams with FP experience already using Cats, default to Cats Effect 3; for greenfield teams that want ZIO's explicit error types and DI layer, ZIO 2 is viable but requires committing to the ZIO ecosystem; for teams that must interop heavily with existing Java code and have short ramp budgets, plain `Future` with `scala.concurrent.blocking` discipline is the pragmatic choice.",
    "fj": "I'd use Futures because they're simpler and everyone knows them.",
    "fs": "The hidden cost of plain `Future` is that every production incident with async code becomes an archaeology project — no structured cancellation, no fiber traces, no guaranteed resource finalization. I've migrated two services from `Future` to Cats Effect `IO` after production incidents: in both cases the bug was a `Future` that started but was never awaited, leaking a connection indefinitely. `IO` makes this impossible by construction. My default for any new service in 2026 is Cats Effect 3 — the ecosystem support alone (http4s, fs2-kafka, Doobie) means we're never integrating libraries with incompatible effect types.",
    "level": "Staff",
    "asked": 9,
    "followups": [
      {
        "q": "How do you interoperate between a Cats Effect IO codebase and a ZIO-based library?",
        "a": "Use the `zio-interop-cats` library, which provides `CatsEffect` instances for ZIO's `Task` type, making it a valid `Async[F]` for Cats Effect type classes. You can call ZIO code from a Cats Effect codebase via `.toEffect[IO]` and vice versa via `ZIO.fromEffect`. The interop layer works well but has overhead — avoid crossing the boundary in hot paths. Long-term, standardize on one effect system per service; interop is a migration bridge, not a permanent architecture."
      },
      {
        "q": "What is structured concurrency in Cats Effect 3 and why does it prevent resource leaks?",
        "a": "Structured concurrency means every fiber has a parent, and when the parent is cancelled or completed, all child fibers are also cancelled before the parent exits. In practice: `Concurrent[F].both(fa, fb)` starts two fibers and guarantees both are cancelled if either fails or if the outer scope is cancelled. This prevents the 'fire and forget Future' pattern where a launched computation outlives its expected lifetime. `Resource[F, A]` extends this to lifecycle: acquire and release are a unit — releasing always happens, even on cancellation."
      }
    ]
  },
  {
    "q": "How do you standardize Spark platform versions and cluster configurations across a multi-team data org to prevent pipeline fragmentation?",
    "a": "Fragmentation happens when teams independently pin Spark versions, Scala versions, and library sets in their own build files. The outcome: incompatible serialization formats, conflicting Delta Lake protocol versions, and on-call engineers unable to debug a colleague's job because the execution model differs. Standardization requires: (1) a central Platform BOM (Bill of Materials) published as an sbt/Maven artifact pinning Spark, Scala, Delta Lake, and approved connector versions — all team `build.sbt` files import it; (2) a single cluster image (AMI or Docker image for EMR/Databricks) built and tested by the platform team, versioned, and promoted through environments — teams submit jobs against the platform image, not their own; (3) a Spark configuration reference (`spark-defaults.conf` shared repository) with baseline settings for shuffle partitions, adaptive query execution, memory fractions, and serializer (Kryo vs Java) — teams can override in their SparkSession for job-specific tuning but document the override in a PR comment; (4) a breaking-change policy: Spark major-version upgrades are platform team-driven, announced 6 weeks ahead, with a two-sprint migration window where both old and new platform versions run in parallel for teams to test. The governance anti-pattern: letting teams run 'just this one job on Spark 3.4 while the platform is on 3.2' — Delta Lake protocol upgrades between Spark versions make tables written on 3.4 unreadable on 3.2, silently corrupting cross-team data contracts.",
    "fj": "I'd let teams use whichever Spark version works for their job.",
    "fs": "The Delta Lake protocol version trap is the killer. Delta tables written with writer version 3 features (row-level deletes, column mapping) cannot be read by Spark versions that only support writer version 2. Once one team upgrades and writes to a shared table, every downstream consumer either upgrades or loses access — with no warning in the logs other than a confusing ProtocolChangedException. I enforce platform BOM with a CI check: any `build.sbt` that overrides the Spark version triggers a mandatory platform-team review. The overhead is one review per quarter; the cost of not doing it was a 6-hour production incident at a previous employer.",
    "level": "Principal",
    "asked": 7,
    "followups": [
      {
        "q": "How do you handle the Delta Lake protocol upgrade problem when multiple teams share tables?",
        "a": "Delta Lake protocol upgrades are irreversible on a table — once a writer uses a newer protocol, older readers fail. The safe pattern: run `DESCRIBE DETAIL` on shared tables before any Spark/Delta upgrade to record current `minReaderVersion` and `minWriterVersion`. In the platform upgrade window, upgrade readers first (all consumers), then upgrade writers (producers). Use Delta's `delta.minReaderVersion` table property to explicitly cap the protocol if you need to maintain backward compatibility during the migration window. For cross-team tables governed by a data contract, treat a protocol upgrade as a breaking schema change — require consumer sign-off before upgrading."
      }
    ]
  },
  {
    "q": "How do you manage the hiring and training risk of advanced Scala FP styles, and what should a principal-level Scala style guide enforce vs. leave to team discretion?",
    "a": "The core tension is expressiveness vs. approachability. Advanced patterns — tagless final, free monads, optics (Monocle), type-level programming with Shapeless/Magnolia — make expert engineers highly productive but create a knowledge cliff: a mid-level engineer from a Java background reads the codebase and sees an alien language. Style guide philosophy at principal level: standardize patterns where inconsistency creates bugs or interop costs; leave room for team autonomy where the choice is aesthetic. Mandatory standards: (1) effect system choice (IO vs Future — one per service, no mixing); (2) error modeling convention (`Either[AppError, A]` vs exceptions — choose one and enforce via Scalafix rule banning `throw` outside explicitly designated boundaries); (3) tagless final algebra naming and module structure (consistent `Algebra[F[_]]` trait + companion with `make` factory); (4) no macro usage in application code (only in designated library modules, reviewed by principal); (5) Scalafmt config is non-negotiable — code style debates are banned. Discretionary: which lens library (Monocle vs manual `copy`), whether to use `cats.data.State` vs explicit state threading, test framework (MUnit vs ScalaTest). Hiring practice: in interviews, probe for understanding of variance, type classes, and for-comprehension desugaring — these are teachable-in-weeks concepts; require demonstrated understanding as a filter, not mastery. Reserve advanced tagless final questions for senior+ roles.",
    "fj": "I'd just hire good engineers and let them learn FP on the job.",
    "fs": "The 'learn FP on the job' approach fails in high-throughput orgs because new engineers write Java-in-Scala — `var`, mutable maps, `null` checks, `throw` — which then propagates because no style guide rejects it in review. The style guide is the answer: a Scalafix rule that bans `throw` in application modules and flags `var` outside explicitly annotated blocks catches the Java habits at PR time, before they become production code. I've run two style guide rollouts: the engineers who initially resist always come around after one production incident that a linter would have caught.",
    "level": "Principal",
    "asked": 6,
    "followups": [
      {
        "q": "How do you level-set FP training for a cohort of 10 engineers joining from Java/Python backgrounds?",
        "a": "A 4-week structured onboarding, not self-directed: Week 1 covers `Option`/`Either`/`Try`, `for`-comprehensions, and case classes — the 90% of production Scala that can be learned from Java intuitions. Week 2 introduces type classes and implicits/given with worked examples from the actual codebase (not textbook Cats). Week 3 focuses on the effect system in use (Cats Effect IO or Future): fiber model, `Resource`, `Ref` for shared state. Week 4 is a pairing sprint where each new engineer refactors a real piece of code reviewed by a senior. The key measure: first PR merged without major FP-related review comments. Most engineers hit this in 3-5 weeks; outliers who don't by week 8 need a direct conversation about fit."
      }
    ]
  },
  {
    "q": "How do you manage library ecosystem governance after a major Scala version churn — specifically the fragmentation that occurred during the Scala 2→3 transition period?",
    "a": "The 2020-2024 Scala 2→3 transition created a three-year window where ecosystem libraries were in one of four states: Scala 2 only, cross-compiled but experimental on Scala 3, Scala 3 only (e.g., Scala 3 macro rewrites), or abandoned with no Scala 3 roadmap. At the principal level, managing this requires an explicit library lifecycle policy. Step 1: audit every library dependency and tag it as 'migrated', 'cross-published', 'blocked-on-macro', or 'abandoned'. 'Blocked-on-macro' means the library uses Scala 2 macros (Shapeless 2, Scala 2 macros for Circe derivation) and cannot compile under Scala 3 without a rewrite. These are the migration blockers — find replacement libraries (Shapeless 3, Circe with `derives` in Scala 3) or fund the port. Step 2: categorize dependencies by business criticality and replaceability. An abandoned library used only for testing (e.g., a custom matcher library) can be replaced quickly; an abandoned library in a core serialization path is a multi-sprint effort. Step 3: establish a sunset policy for libraries not publishing Scala 3 artifacts after a given date — typically 12 months after the org's Scala 3 target date. Libraries past sunset are replaced in the next sprint cycle, not deferred. Step 4: for libraries that are cross-compiled but have known behavioral differences under Scala 3 (e.g., macro-derived codecs behaving differently due to `given` derivation order), create regression test suites run against both Scala 2.13 and Scala 3 builds in CI. This catches behavioral divergence before the Scala 2 build is retired.",
    "fj": "I'd upgrade libraries as they release Scala 3 versions and fix issues as they come up.",
    "fs": "Reactive library upgrades create the worst possible upgrade experience: teams upgrade piecemeal, discover behavioral differences (usually in codec derivation or macro-generated code) in production, and spend debugging time that should have been caught in a controlled migration window. The audit and lifecycle policy converts a reactive fire-drill into a planned migration. The most important governance lever I've found: requiring every library in the org's BOM to have a documented Scala 3 upgrade path before the org sets a Scala 3 migration date. Setting the date without the path is how organizations miss their target by 12+ months.",
    "level": "Principal",
    "asked": 5,
    "followups": [
      {
        "q": "How does Shapeless 3 differ from Shapeless 2 and why does the difference matter for a Scala 2→3 migration?",
        "a": "Shapeless 2 is built on Scala 2 macros (`scala.reflect`) for generic derivation — it cannot compile under Scala 3. Shapeless 3 is a ground-up rewrite using Scala 3's `Mirror` type class and `inline` metaprogramming, providing similar `Generic`/`HList`-style capabilities via `scala3-migration`-compatible code. The practical difference: libraries that use Shapeless 2 for automatic codec derivation (some versions of Circe, Doobie's generic module, PureConfig) must either adopt Shapeless 3, switch to `Mirror`-based derivation directly, or remain Scala 2 only. This is the macro surface audit that blocks most real migrations."
      }
    ]
  },
  {
    "q": "How do you sequence a Scala 2→3 fleet migration across 30+ services in a large engineering org, and how do you manage rollback risk?",
    "a": "A fleet migration at 30+ services cannot be done service-by-service without a coordination framework — ad-hoc upgrades create version islands where shared libraries must publish for both Scala versions indefinitely, doubling release overhead. The coordinated approach has five phases. Phase 0 — audit: inventory all services, classify each by migration complexity (no macros, macro libraries only, custom macros — each tier is a different complexity class). Phase 1 — platform readiness: the shared library BOM publishes Scala 3 artifacts; all tier-0 libraries are cross-compiled; macro-heavy libraries have documented Scala 3 equivalents. Phase 2 — pioneer cohort: 3-5 volunteer services (chosen for low macro surface) migrate first and run in production for 60 days, surfacing unknown issues. Pioneer findings feed a public migration playbook. Phase 3 — wave rollout: services migrate in cohorts of 8-10, sequenced by complexity tier. Platform team embeds one migration-experienced engineer per cohort for the first sprint. Phase 4 — cutover: all services migrated; cross-published shared libraries drop the Scala 2 build, halving CI overhead. Rollback risk: because each service has its own deployment artifact, rollback is per-service — a Scala 3 service that fails in production reverts its own deployment. The risk is cross-service: if service A (Scala 3) writes a shared Kafka topic with a Scala 3-compiled Avro schema, and service B (still Scala 2) reads it with the old schema registry, there may be a deserialization mismatch. Mitigate by running schema compatibility checks in the schema registry before migration; treat schema evolution as a separate concern from language version.",
    "fj": "I'd upgrade services one by one and fix problems as they appear.",
    "fs": "Service-by-service without coordination means shared libraries stay cross-published for 2+ years — that is 2x CI overhead, 2x release testing, and doubled library author burden. The cohort model with a published playbook drops the average service migration from 3 sprints (first-mover) to 1 sprint (late cohort) because every gotcha is already documented. The hardest coordination problem is the shared schema: Avro and Protobuf schemas compiled by Scalapb or Avro4s have slightly different default derivations between Scala 2 and 3 — audit every shared schema contract before migrating either the producer or consumer, not after.",
    "level": "Principal",
    "asked": 6,
    "code": [
      {
        "accent": "fix",
        "lang": "scala",
        "label": "cross-publish BOM for both versions",
        "lines": [
          "// shared-bom/build.sbt",
          "ThisBuild / crossScalaVersions :=",
          "  Seq(\"2.13.14\", \"3.4.2\")",
          "",
          "// Publish both artifacts:",
          "// platform-bom_2.13-1.0.0.jar",
          "// platform-bom_3-1.0.0.jar",
          "",
          "// Pioneer services use _3 artifact;",
          "// legacy services still use _2.13.",
          "// Both supported until Phase 4 cutover."
        ]
      }
    ],
    "followups": [
      {
        "q": "How do you handle a service that cannot migrate because it depends on a third-party library with no Scala 3 artifact?",
        "a": "Three options in priority order. First, check whether the library's Java artifact (plain JAR, no Scala version suffix) can be used — many Java libraries work fine as Scala 3 dependencies. Second, check if a compatible Scala 3 replacement library exists (e.g., Enumeratum → Scala 3 native `enum`, PureConfig → `ciris` or PureConfig Scala 3 build). Third, if the library is genuinely unmaintained and irreplaceable, the service stays on Scala 2.13 until the library is replaced — document this as a 'migration blocker' in the fleet audit and track it as a tech debt item with an owner and deadline. Never force-migrate a service past a hard blocker by patching the library locally — that creates a fork you will have to maintain forever."
      }
    ]
  },
  {
    "q": "When should a data engineering org converge new services onto Kotlin or Java instead of continuing to invest in Scala, and what are the concrete tipping points?",
    "a": "Converging away from Scala is a rational choice under four concrete conditions. First, the team's Spark workloads are predominantly Python: if >70% of pipeline work is in PySpark notebooks or dbt, adding a Scala service layer adds language context-switching cost with no productivity gain — Kotlin or Python services that call Spark via REST (Databricks Connect, Livy) or produce Avro/Parquet files are simpler. Second, the hire pipeline is consistently taking >90 days for Scala engineers: at that pace a 10-person team has a 2-person average vacancy, which means existing engineers are overloaded; switching the service layer to Kotlin (familiar to Android/Java engineers) immediately opens a 3x larger candidate pool. Third, the majority of new services are CRUD APIs with no complex type-level requirements: for an event-sourced microservice calling Postgres, the ceremony of tagless final adds no correctness guarantee that a well-tested Kotlin coroutines service does not also provide; the simpler code is the correct choice. Fourth, the org is standardizing JVM tooling around Gradle + IntelliJ IDEA: sbt's unique build model adds friction when the rest of the org uses Gradle; if the org is consolidating build infrastructure, sbt is an outlier. The Scala moat that survives all four conditions: Spark-native optimization. If any team is writing Catalyst extensions, custom Spark data sources, or Spark internals-adjacent code, Scala is non-negotiable — those APIs are Scala-first and the Python layer has meaningful limitations. The honest principal-level answer: 'keep Scala for Spark, evaluate Kotlin for service layer' is a defensible and increasingly common architecture in 2026.",
    "fj": "Scala is better so I'd keep using it unless the team really struggles.",
    "fs": "Personal preference for Scala is not a principal-level argument. The argument must be framed as total cost of ownership over a 3-year horizon: hire cost, ramp time, library maintenance burden, and operational complexity. I've made the recommendation to converge two teams onto Kotlin for service APIs — not because Scala is worse, but because those teams' work was 90% REST endpoint + Postgres, and the Scala FP ceremony was creating bugs, not preventing them. The Spark pipelines stayed Scala. The boundary between 'Scala data layer' and 'Kotlin service layer' with a shared Avro schema contract is a clean architecture that lets each language do what it does best.",
    "level": "Principal",
    "asked": 8,
    "followups": [
      {
        "q": "How do you share data models between a Scala Spark pipeline and a Kotlin service layer in the same org?",
        "a": "The cleanest boundary is a schema-first contract: define data models in Avro or Protobuf IDL (`.avsc` / `.proto` files), committed to a shared schema repository, and generate case classes for Scala and data classes for Kotlin using avro4s/ScalaPB on the Scala side and avro-tools/protoc-gen-kotlin on the Kotlin side. Both generated artifacts are published from the same IDL version, so the schema is the single source of truth. Avoid sharing a Scala library of case classes directly with a Kotlin service — Kotlin can consume Scala JARs but the ergonomics are poor (Scala collections, Option types, and implicit-heavy APIs don't translate cleanly to idiomatic Kotlin)."
      },
      {
        "q": "What is Databricks Connect and how does it change the Scala-vs-Python tradeoff for data services?",
        "a": "Databricks Connect (v2, available from DBR 13.x) allows a local Python or Scala process to submit DataFrame operations to a remote Databricks cluster, receiving results as Arrow batches. This means a Python service can run Spark-quality analytical queries without embedding a Spark runtime. It shifts the language tradeoff: previously, 'I need Spark performance' meant 'I need Scala or PySpark in a cluster'. With Databricks Connect, a Kotlin/Python REST service can delegate heavy computation to the cluster while keeping the service layer in the team's preferred language. The limitation: only DataFrame/SQL operations are supported remotely; Scala RDD-level or Catalyst extension code must still run on the cluster, in Scala."
      }
    ]
  }
]
