[
  {
    "q": "Your S3-backed pipeline starts getting 503 SlowDown errors at scale. What is actually happening and how do you fix it?",
    "a": "S3 partitions request capacity by key prefix, giving you at least 3,500 PUT/POST/DELETE and 5,500 GET requests per second per prefix. SlowDown/503 means one prefix is hot and S3 hasn't finished auto-creating new partitions yet. Spread keys across more prefixes (e.g. a hashed or date-based prefix) so the load fans out, and add jittered retries while S3 scales the partitioning.",
    "fs": "Talks in terms of per-prefix request rate, not per-bucket, and fixes it by spreading key prefixes.",
    "fj": "Says 'S3 has a bucket-wide limit, request a quota increase' or blames file size.",
    "level": "Jr",
    "code": [
      {
        "label": "hot prefix vs fanned-out",
        "lang": "python",
        "lines": [
          "# hot: all writes share one prefix",
          "s3://bucket/data/file_001.parquet",
          "",
          "# fanned out across prefixes",
          "s3://bucket/data/dt=2026-06-04/",
          "  hash=a1/file_001.parquet"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "If a date-prefix layout is needed for partition pruning but it concentrates today's writes on one hot prefix, how do you reconcile that?",
        "a": "Add a hash/bucket segment under the date so writes fan out while the date prefix still prunes: `dt=2026-06-04/bucket=07/...`. The query engine filters on `dt`, but the request load spreads across the bucket sub-prefixes. You keep read-time pruning and write-time parallelism instead of trading one for the other."
      },
      {
        "q": "When does throwing more prefixes at the problem stop helping?",
        "a": "When the bottleneck isn't S3 but the client: a single-threaded or low-concurrency writer can't drive enough parallel requests to even reach per-prefix limits. It also stops helping once you hit account-level S3 request quotas or saturate NAT/network egress. At that point you scale client concurrency or consider `S3 Express One Zone` directory buckets for far higher TPS."
      },
      {
        "q": "Why use jittered exponential backoff specifically, not just fixed retries, on SlowDown?",
        "a": "Fixed-interval retries from many clients synchronize and re-hit the still-hot prefix in lockstep, prolonging the throttle. Exponential backoff with random jitter de-correlates retries so requests arrive smeared over time, letting S3 finish splitting the partition. The AWS SDKs implement this adaptive retry mode by default."
      }
    ]
  },
  {
    "q": "Why does putting a high-cardinality timestamp directly as an S3 partition key often hurt more than it helps?",
    "a": "Each distinct partition value becomes a separate folder and a catalog/metastore entry, so partitioning on a raw second/millisecond timestamp explodes into millions of tiny partitions. Query planning then spends more time listing partitions than scanning data, and you get many small files that kill read throughput. Partition on coarse columns that match query filters (year/month/day) and keep file sizes near 128MB-1GB.",
    "fs": "Reasons about partition cardinality and small-file overhead, picks coarse keys matching the WHERE clause.",
    "fj": "Thinks 'more partitions = always faster' and partitions on a column queries never filter on.",
    "level": "Mid",
    "code": [
      {
        "label": "good partition layout",
        "lang": "sql",
        "lines": [
          "-- partition on coarse, queried cols",
          "PARTITIONED BY (",
          "  dt date,        -- day grain",
          "  region string",
          ")",
          "-- NOT on raw event_ts (ms)"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "If you truly need sub-day pruning on a huge fact table, what's better than partitioning on hour?",
        "a": "Keep a coarse `dt` partition and let the file format do fine-grained skipping: Parquet/ORC store per-row-group min/max stats, so a `WHERE event_ts BETWEEN ...` predicate prunes row groups within the day's files. A table format like `Iceberg` goes further with hidden partitioning and metadata-level file pruning. You get intra-day skipping without the partition explosion."
      },
      {
        "q": "How does over-partitioning specifically slow down a Glue/Hive metastore beyond just listing?",
        "a": "Each partition is a row in the catalog, so millions of partitions bloat `GetPartitions` calls, which are paginated and rate-limited, and query planning serializes on fetching them. Crawlers and `MSCK REPAIR` also degrade. Partition projection or `Iceberg` metadata avoids storing per-partition rows entirely, which is why heavily-partitioned tables move off the classic metastore."
      },
      {
        "q": "Coarse partitions risk huge daily files. How do you keep file sizes in the sweet spot then?",
        "a": "Control output parallelism at write time: in Spark, `repartition` or set `maxRecordsPerFile`/target file size so each task emits ~128MB-1GB objects rather than one giant file or thousands of tiny ones. Run periodic compaction (or `OPTIMIZE` in a table format) to coalesce small files from streaming/incremental writes. Aim for fewer, fatter files inside each coarse partition."
      }
    ]
  },
  {
    "q": "In AWS Glue, when would you use a DynamicFrame instead of a Spark DataFrame, and what do you lose?",
    "a": "DynamicFrames are Glue's self-describing abstraction that tolerates inconsistent or unknown schemas, integrate with job bookmarks for incremental loads, and support pushdown predicates against the Data Catalog. You convert to a Spark DataFrame when you need Spark SQL, window functions, or the broader optimized Spark API, since DynamicFrames are slower and more limited. A common pattern is read as DynamicFrame, toDF() for the heavy transforms, then back for the catalog-aware write.",
    "fs": "Names bookmarks, pushdown predicates, and ResolveChoice as the DynamicFrame wins; converts to DataFrame for real compute.",
    "fj": "Treats them as interchangeable, or claims DynamicFrames are faster than DataFrames.",
    "level": "Mid",
    "code": [
      {
        "label": "read, transform, write",
        "lang": "python",
        "lines": [
          "dyf = glueContext.create_dynamic_frame",
          "  .from_catalog(database=db, table_name=t)",
          "df = dyf.toDF()        # Spark API",
          "out = DynamicFrame.fromDF(",
          "  df, glueContext, 'out')"
        ]
      }
    ],
    "followups": [
      {
        "q": "What does ResolveChoice actually solve that a plain DataFrame can't express cleanly?",
        "a": "When a column has mixed types across files (e.g. a field that's sometimes `int`, sometimes `string`), a DataFrame forces one rigid schema and may fail or null out rows. `ResolveChoice` lets you keep both as a struct (`make_struct`), cast, or project a single type per ambiguous column. It encodes the schema-conflict decision explicitly instead of crashing on read."
      },
      {
        "q": "Why might toDF() then fromDF() silently break job bookmarks?",
        "a": "Bookmarks track progress via the Glue source's `transformation_ctx`. The DataFrame round-trip itself is fine, but if you read with raw Spark instead of `create_dynamic_frame.from_catalog`, there's no source context to bookmark, so the job reprocesses everything. Bookmarks bind to the Glue read, not to the Spark transforms in the middle."
      },
      {
        "q": "When is converting to a DataFrame the wrong call even for heavy logic?",
        "a": "If the work is mostly schema reconciliation, drops, or catalog-aware partitioned writes on messy data, staying in DynamicFrames avoids re-deriving a strict schema and keeps catalog pushdown. Converting materializes the schema, which can be expensive or lossy on deeply nested, inconsistent JSON. Convert only when you genuinely need Spark SQL/windows."
      }
    ]
  },
  {
    "q": "A Glue job is meant to be incremental but keeps reprocessing all of S3. What's the most likely cause?",
    "a": "Job bookmarks are either disabled or being defeated. Bookmarks track what was already processed using a transformation_ctx on each source/sink, so if you omit transformation_ctx, don't call job.commit(), or read with plain Spark instead of a DynamicFrame, the bookmark never advances. Enable bookmarks in the job config, give every source a stable transformation_ctx, and commit at the end.",
    "fs": "Immediately cites transformation_ctx + job.commit() and that bookmarks only work via Glue sources, not raw Spark reads.",
    "fj": "Suggests manually tracking processed files in a side table without knowing bookmarks exist.",
    "level": "Sr",
    "code": [
      {
        "label": "bookmark requirements",
        "lang": "python",
        "lines": [
          "dyf = glueContext.create_dynamic_frame",
          "  .from_catalog(",
          "    database=db, table_name=t,",
          "    transformation_ctx='src')  # required",
          "# ... transforms ...",
          "job.commit()   # advances bookmark"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "How does a Glue bookmark decide a given S3 object is 'new' — and where does that assumption break?",
        "a": "Bookmarks key off the object's last-modified timestamp and path, processing files newer than the last commit. It breaks when upstream rewrites/overwrites existing files in place (same path, new content) or backfills with older timestamps — those get skipped. For mutable or out-of-order data you need a table format with proper change tracking, not bookmarks."
      },
      {
        "q": "A bad deploy processed corrupt data and committed the bookmark. How do you reprocess just that window?",
        "a": "Use job bookmark controls: rewind to a prior run with `--job-bookmark-from`/`-to` run IDs, or reset the bookmark and re-run with a narrowed source predicate. Resetting wholesale reprocesses everything, so prefer pause + a `pushDownPredicate` scoped to the affected partitions. The point is bookmarks are seekable, not just on/off."
      },
      {
        "q": "Why is calling job.init() with the right args as important as job.commit()?",
        "a": "`job.init(args['JOB_NAME'], args)` loads the existing bookmark state for that named job before processing; without the matching name/args the run starts from a fresh state and reprocesses all data even though `commit()` is called. Init reads the bookmark, commit advances it — you need both halves wired correctly."
      }
    ]
  },
  {
    "q": "Explain Redshift distribution styles and how a wrong DISTKEY shows up in query plans.",
    "a": "KEY co-locates rows with the same value on the same slice so a join on that column needs no redistribution; ALL replicates a small table to every node (great for stable dimensions); EVEN/AUTO round-robins when there's no clear join key. A wrong DISTKEY surfaces as DS_BCAST_INNER or DS_DIST_BOTH in EXPLAIN, meaning Redshift is broadcasting or redistributing data across the network for every join. The goal is DS_DIST_NONE on your biggest, most frequent joins.",
    "fs": "Reads DS_DIST_NONE vs DS_BCAST_INNER from EXPLAIN and ties DISTKEY choice to the dominant join.",
    "fj": "Picks DISTKEY to 'spread data evenly' without thinking about joins, or confuses it with the sort key.",
    "level": "Sr",
    "code": [
      {
        "label": "EXPLAIN tells on you",
        "lang": "sql",
        "lines": [
          "EXPLAIN SELECT ...",
          "  FROM fact f JOIN dim d",
          "  ON f.dim_id = d.id;",
          "-- BAD:  DS_BCAST_INNER",
          "-- GOOD: DS_DIST_NONE"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "DISTKEY KEY fixes the join but now one slice is overloaded. What happened and what do you do?",
        "a": "If the DISTKEY column is skewed (a few values dominate), all those rows land on one slice, causing data skew so that one slice's queries lag while others idle. Check `SVV_TABLE_INFO.skew_rows`. Pick a higher-cardinality, evenly-distributed join column, or fall back to `EVEN`/`ALL` if no good key exists — co-location isn't worth a hot slice."
      },
      {
        "q": "When does DISTSTYLE ALL backfire instead of eliminating redistribution?",
        "a": "`ALL` copies the whole table to every node, so it only pays off for small, slowly-changing dimensions. On a large or frequently-written table it multiplies storage by the node count and makes every write fan out to all nodes, inflating load and VACUUM time. Use it for a small dim, never for a fact table."
      },
      {
        "q": "Why does AUTO distribution sometimes start as ALL and switch to EVEN/KEY later?",
        "a": "With `DISTSTYLE AUTO` Redshift begins small tables as `ALL` for join efficiency, then automatically converts to `EVEN` (or a chosen KEY) once the table grows past a size threshold, doing the redistribution in the background. So a plan can change as data lands. It's convenient but for hot, well-understood joins an explicit DISTKEY gives more predictable `DS_DIST_NONE`."
      }
    ]
  },
  {
    "q": "What does a Redshift SORTKEY actually buy you, and when is a compound key the wrong choice?",
    "a": "A sort key keeps blocks physically ordered so Redshift can use zone maps (per-block min/max) to skip blocks that can't match a range filter, which is the main I/O reduction lever. A compound sort key only prunes well when you filter on the leading column(s) in order; if queries filter on different columns unpredictably, the trailing columns give little benefit. For multi-column unpredictable filtering an interleaved key spreads weight evenly, but it costs more to VACUUM, so most teams default to compound on the common time/range column.",
    "fs": "Explains zone-map block skipping and the leading-column rule; mentions VACUUM to maintain sortedness.",
    "fj": "Says sort key 'just sorts the output' or thinks interleaved is always better.",
    "level": "Sr",
    "followups": [
      {
        "q": "After heavy inserts, zone maps stop helping even though the sort key is right. Why?",
        "a": "New rows land in an unsorted region at the end of the table, so their blocks have wide, overlapping min/max ranges that defeat zone-map pruning. The table's `unsorted` percentage climbs and scans read blocks they used to skip. Running `VACUUM SORT` (or `VACUUM FULL`) re-sorts those rows back into order, restoring tight zone maps."
      },
      {
        "q": "Why can a low-cardinality leading sort column waste the zone map?",
        "a": "Zone maps store min/max per ~1MB block; if the leading column has few distinct values, many blocks share the same min and max, so a filter can't decide which to skip and reads them all. A high-cardinality, monotonically-increasing column (like an event date) gives each block a distinct narrow range, maximizing skip. Lead with the column that has good range separation."
      },
      {
        "q": "Given AUTO sort keys exist, why would a senior still set one explicitly?",
        "a": "`AUTO` lets Redshift Advisor learn from query patterns and apply a sort key over time, which is great for unknown or evolving workloads. But for a known dominant access pattern (e.g. time-range scans) an explicit compound key on that column gives immediate, predictable pruning from day one instead of waiting for the advisor to converge. Explicit wins when you already know the query shape."
      }
    ]
  },
  {
    "q": "Walk me through choosing the number of shards for a Kinesis Data Stream and what the partition key controls.",
    "a": "Each shard handles up to 1MB/s or 1,000 records/s for writes and 2MB/s for reads, so you size shard count to peak ingest throughput, not average. The partition key is MD5-hashed to map a record to a shard, and ordering is only guaranteed within a shard, so records sharing a key (e.g. a user_id) stay ordered. A low-cardinality or skewed key creates a hot shard that throttles even though total capacity looks fine.",
    "fs": "Sizes by per-shard MB/s and record/s limits and flags hot-shard skew from a bad partition key.",
    "fj": "Says 'just pick a few shards' or thinks Kinesis guarantees global ordering across the stream.",
    "level": "Mid",
    "code": [
      {
        "label": "shard math",
        "lang": "python",
        "lines": [
          "peak = 8_000   # records/sec",
          "per_shard = 1_000",
          "shards = -(-peak // per_shard)  # ceil",
          "# also check MB/s: 1 MB/s per shard",
          "# pick max(record_shards, mb_shards)"
        ]
      }
    ],
    "followups": [
      {
        "q": "Traffic is spiky and you don't want to manage resharding. What changes the calculus?",
        "a": "Use Kinesis Data Streams On-Demand mode: it auto-scales shard capacity based on observed throughput (doubling to a recent peak) so you stop sizing shards manually. You trade the per-shard cost model for pay-per-GB and a higher unit price. For unpredictable or bursty workloads On-Demand removes the resharding toil; for steady high volume Provisioned is cheaper."
      },
      {
        "q": "Five consumers on one stream and read throttling appears. What's the fix?",
        "a": "Standard consumers share the shard's 2MB/s read budget, so N consumers divide it and lag. Enhanced Fan-Out gives each registered consumer its own dedicated 2MB/s per shard via a push (HTTP/2 `SubscribeToShard`) model with lower latency. Use EFO when multiple independent apps read the same stream; it costs more per consumer-shard-hour."
      },
      {
        "q": "How do you reshard a hot shard without breaking per-key ordering for in-flight records?",
        "a": "Use `SplitShard` to divide the hot shard's hash-key range; the parent shard stays readable until drained, and the KCL processes the parent fully before its children, preserving order across the split. New records for the same key route to whichever child owns that hash range. Avoid blindly increasing total shards — split the specific hot range."
      }
    ]
  },
  {
    "q": "When would you reach for Kinesis Data Firehose over Kinesis Data Streams?",
    "a": "Firehose is fully managed delivery: no shards to size, it buffers and batches into S3/Redshift/OpenSearch with optional format conversion to Parquet, and it auto-scales. Choose it when you just need durable near-real-time delivery to a sink and can tolerate a buffer delay (down to ~60s or a size threshold). Choose Data Streams when you need sub-second latency, multiple independent consumers, custom processing, replay, or strict per-key ordering.",
    "fs": "Frames it as managed delivery vs custom low-latency processing; mentions buffering and Parquet conversion.",
    "fj": "Thinks Firehose is real-time like Streams, or doesn't know Firehose can't replay or fan out custom consumers.",
    "level": "Jr",
    "followups": [
      {
        "q": "You need light transforms before Firehose lands data in S3. How without standing up a stream processor?",
        "a": "Attach a transformation Lambda to the Firehose delivery stream: Firehose invokes it per buffered batch to filter, enrich, or reshape records before delivery, and you can also enable record format conversion to Parquet/ORC using a Glue schema. Failed records route to an S3 error prefix. It's inline transformation without a separate consumer app."
      },
      {
        "q": "Firehose to Redshift is delivering duplicates. Why, and how do you dedup?",
        "a": "Firehose delivery is at-least-once, and its Redshift path stages to S3 then issues `COPY`, so a retried batch can re-COPY the same file, duplicating rows. Dedup downstream: load into a staging table and `MERGE`/upsert on a natural key, or include an idempotency key and dedup in a transform. Don't assume exactly-once delivery."
      },
      {
        "q": "Can you feed Firehose from a Data Stream, and why would you?",
        "a": "Yes — set a Kinesis Data Stream as Firehose's source to get the best of both: Streams gives you low-latency custom consumers, replay, and per-key ordering, while Firehose handles durable batched archival to S3/Parquet off the same data. It's a common 'hot path + cold archive' pattern: real-time consumers read the stream, Firehose tees everything to the lake."
      }
    ]
  },
  {
    "q": "A Lambda triggered by S3 events is occasionally processing the same file twice. How do you reason about it?",
    "a": "S3-to-Lambda (and most event sources) deliver at-least-once, so retries and rare duplicate events mean your function must be idempotent, not assume exactly-once. Make the work idempotent with a dedup key (object key + version/ETag) tracked in DynamoDB with a conditional write, or design downstream writes as upserts. Also bound blast radius: set sane retries/DLQ so a poison object doesn't loop forever.",
    "fs": "States at-least-once delivery and designs for idempotency with a conditional dedup key + DLQ.",
    "fj": "Assumes events fire exactly once and adds no dedup, or 'just catch the exception'.",
    "level": "Mid",
    "code": [
      {
        "label": "idempotent dedup",
        "lang": "python",
        "lines": [
          "table.put_item(",
          "  Item={'id': f'{key}:{etag}'},",
          "  ConditionExpression=",
          "    'attribute_not_exists(id)')",
          "# raises if already processed -> skip"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "Two duplicate events arrive almost simultaneously. Does the conditional put still protect you?",
        "a": "Yes — DynamoDB conditional writes are atomic per item, so even concurrent `put_item` calls with `attribute_not_exists` serialize: exactly one succeeds and the other throws `ConditionalCheckFailedException`. That's the whole point of doing the dedup gate as a conditional write rather than a read-then-write, which would race. The loser skips the work."
      },
      {
        "q": "What if the function dies after the dedup write but before the actual work finishes?",
        "a": "Then the dedup key exists but the work never completed, and the retry sees the key and wrongly skips — a lost update. Fix by making the dedup record a state machine (`PENDING` then `DONE`) so a retry can resume `PENDING` items, or do the work and the dedup commit in one transaction/upsert. The key should reflect completion, not just receipt."
      },
      {
        "q": "Why prefer S3 EventBridge notifications over direct S3-to-Lambda for this?",
        "a": "EventBridge adds filtering, multiple targets, archive/replay, and a DLQ at the bus level, and gives a cleaner at-least-once contract with retry policies you control. Direct S3-to-Lambda can also silently drop on throttling without an SQS buffer. Routing S3 events through EventBridge (or S3 to SQS to Lambda) gives durable buffering and a real DLQ for poison objects."
      }
    ]
  },
  {
    "q": "What are the real limits that make Lambda a poor fit for some data jobs, and what do you use instead?",
    "a": "Lambda caps at 15 minutes runtime, 10GB memory, 10GB ephemeral /tmp, and a 6MB synchronous payload, so large shuffles, long batch jobs, or big in-memory datasets don't fit. It shines for event-driven, short, parallelizable glue work (trigger a Glue/EMR job, transform a single object, fan out per-partition). For heavy transforms use Glue or EMR; for long orchestration use Step Functions to coordinate many short Lambdas.",
    "fs": "Knows the 15-min/10GB/payload ceilings and routes heavy work to Glue/EMR with Step Functions.",
    "fj": "Tries to run a full ETL inside one Lambda and hits the 15-minute wall in prod.",
    "level": "Mid",
    "followups": [
      {
        "q": "A job needs ~30 min but is otherwise Lambda-shaped. Cheapest way to keep it serverless?",
        "a": "Move it to AWS Fargate (ECS task) or Glue Python Shell/Spark — both are serverless-ish, run far longer than 15 minutes, and you pay per-second of compute. For event-triggered orchestration, have Lambda just launch the Fargate task and return. Don't fight the 15-minute ceiling by chunking a single logical job across many Lambda invocations if a longer-running container fits cleanly."
      },
      {
        "q": "The 6MB sync payload is blocking a Lambda that returns large results. What's the pattern?",
        "a": "Don't return the payload — write the result to S3 and return a pointer (the S3 key) instead; downstream reads it from S3. For async/event invocations the limit is 256KB, so the same claim-check pattern applies. This decouples result size from the invocation contract and is standard for big-output Lambdas."
      },
      {
        "q": "When is more Lambda memory the right lever even if you're not memory-bound?",
        "a": "Lambda allocates CPU proportionally to configured memory, so bumping memory can cut a CPU-bound function's wall-clock time enough to lower total cost (price = GB-seconds, and fewer seconds can offset more GB). Use Lambda Power Tuning to find the cost/latency sweet spot. Memory is effectively your CPU dial, not just a heap-size setting."
      }
    ]
  },
  {
    "q": "Two Athena queries scan the same table but one costs 50x more. Athena bills per TB scanned, so what design decisions drive that gap?",
    "a": "Athena charges by bytes scanned, so columnar formats (Parquet/ORC) let it read only needed columns, compression shrinks bytes, and partition pruning skips whole prefixes when you filter on a partition column. The expensive query is likely scanning row-based CSV/JSON, doing SELECT *, or missing a partition filter so it reads the full dataset. Convert to partitioned compressed Parquet, select only needed columns, and always filter on partition keys.",
    "fs": "Ties cost directly to bytes scanned: Parquet + compression + partition pruning + column projection.",
    "fj": "Thinks Athena cost depends on rows returned or query runtime, not bytes scanned.",
    "level": "Mid",
    "code": [
      {
        "label": "cut bytes scanned",
        "lang": "sql",
        "lines": [
          "-- expensive",
          "SELECT * FROM events;",
          "",
          "-- cheap: projection + partition",
          "SELECT user_id, amount",
          "FROM events",
          "WHERE dt = DATE '2026-06-04';"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "A query filters on a partition column but Athena still scans everything. What silently broke pruning?",
        "a": "Usually a function or cast wrapped around the partition column (e.g. `WHERE CAST(dt AS varchar) = ...` or `date(dt) = ...`) prevents partition pruning because Athena can't match it to a literal partition value. Compare the partition column to a literal of the same type. Type mismatches and expressions on the partition key are the classic pruning killers."
      },
      {
        "q": "How would you cap a runaway team's Athena spend regardless of query quality?",
        "a": "Set per-query and per-workgroup data-scanned limits in Athena workgroups: a per-query control cancels any single query exceeding the byte threshold, and aggregate controls cap a workgroup over a period. Route teams into separate workgroups with their own limits and CloudWatch metrics. It's a guardrail against `SELECT *` on an unpartitioned table draining the budget."
      },
      {
        "q": "For a hot repeated query, what beyond Parquet+partitions cuts cost further?",
        "a": "Bucketing (`CLUSTERED BY`) on a high-selectivity join/filter column lets Athena read only matching buckets, and result reuse caches identical query results so a repeat scans zero bytes within the cache window. For dashboard-style repetition, materializing a pre-aggregated table cuts the scan to the rollup size. Stack these on top of columnar + partitioning."
      }
    ]
  },
  {
    "q": "Your Athena table has thousands of daily partitions and MSCK REPAIR is painfully slow. What's the senior fix?",
    "a": "MSCK REPAIR and per-partition ALTER ... ADD don't scale because they round-trip the catalog for every partition. Use partition projection: you declare the partition value ranges/format in table properties and Athena computes partitions at query time from the WHERE clause, with zero catalog entries to manage. This gives partition pruning for highly partitioned, predictably-laid-out data without any partition-loading job.",
    "fs": "Names partition projection and that it computes partitions from table properties, skipping the metastore.",
    "fj": "Schedules MSCK REPAIR more often or adds partitions one ALTER at a time.",
    "level": "Sr",
    "code": [
      {
        "label": "projection in DDL",
        "lang": "sql",
        "lines": [
          "TBLPROPERTIES (",
          "  'projection.enabled'='true',",
          "  'projection.dt.type'='date',",
          "  'projection.dt.range'=",
          "    '2024-01-01,NOW',",
          "  'projection.dt.format'='yyyy-MM-dd'",
          ")"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "What's the failure mode if your physical S3 layout doesn't match the projection definition?",
        "a": "Projection blindly generates the partition paths from the declared ranges/format, so if files live at a different path or a projected partition has no data, queries either miss data or scan non-existent prefixes (returning empty). There's no catalog to reconcile reality against. Projection demands a strictly predictable, gap-free layout; irregular layouts need real partitions or a table format."
      },
      {
        "q": "A query without a partition filter on a projected table behaves badly. Why?",
        "a": "Without a `WHERE` on the projected column, Athena must enumerate the entire projected range (e.g. every day from the start date to `NOW`), generating a huge cross-product of partitions to scan. That can be slower and costlier than a metastore table. Projection rewards selective filters and punishes unbounded scans, so always constrain the projected dimension."
      },
      {
        "q": "When would you choose Iceberg over partition projection for the same problem?",
        "a": "`Iceberg` handles irregular layouts, hidden partitioning, schema/partition evolution, row-level updates/deletes, and time travel — none of which projection supports. Projection is a lightweight fix for static, predictable, append-only layouts. If the table mutates, evolves partitioning, or needs ACID, move to Iceberg; if it's just append-only date-partitioned logs, projection is simpler."
      }
    ]
  },
  {
    "q": "Design an IAM policy for a Glue job that reads from one S3 bucket and writes curated data to another. What does least-privilege look like?",
    "a": "Give the job an IAM role (not a user with long-lived keys) whose policy scopes actions to the exact resources: s3:GetObject on the raw bucket's objects, s3:PutObject on the curated prefix, and the specific Glue/CloudWatch actions it needs. Avoid s3:* and arn:aws:s3:::* — list the bucket ARNs and prefixes, and separate the ListBucket (bucket ARN) from object actions (bucket/* ARN). Lean on IAM Access Analyzer and last-accessed data to trim anything unused.",
    "fs": "Uses a role, scopes by action and resource ARN/prefix, splits ListBucket from object perms, validates with Access Analyzer.",
    "fj": "Attaches AmazonS3FullAccess or s3:* on * because 'it was failing and that fixed it'.",
    "level": "Sr",
    "code": [
      {
        "label": "scoped, not wildcard",
        "lang": "ts",
        "lines": [
          "{ Effect: 'Allow',",
          "  Action: ['s3:GetObject'],",
          "  Resource:",
          "   'arn:aws:s3:::raw-bucket/*' }",
          "// separate stmt: ListBucket on",
          "//   arn:aws:s3:::raw-bucket"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "The buckets are SSE-KMS encrypted and the job still gets AccessDenied despite S3 perms. What's missing?",
        "a": "Reads/writes of KMS-encrypted objects also require KMS permissions: `kms:Decrypt` for reads and `kms:GenerateDataKey` (+ `Decrypt`) for writes, scoped to the specific key ARN. S3 perms alone aren't enough because the data key lives in KMS. Add the KMS statement and ensure the key policy or a grant allows the role."
      },
      {
        "q": "Why split ListBucket from GetObject into two statements with different resource ARNs?",
        "a": "`s3:ListBucket` is a bucket-level action so its resource is the bucket ARN (`...:::raw-bucket`), while object actions like `GetObject` operate on object ARNs (`...:::raw-bucket/*`). Putting both under one resource ARN means one of them silently won't match and fails. You can further scope ListBucket to a prefix with an `s3:prefix` condition."
      },
      {
        "q": "Both buckets are in another account. What additional control enforces least-privilege there?",
        "a": "The role's IAM policy alone isn't enough cross-account — the bucket owner's S3 bucket policy must also grant the role's principal, so access is the intersection of both. Add a bucket policy allowing the specific role ARN on the specific prefixes, and consider an S3 Access Point with its own scoped policy to keep cross-account grants narrow and auditable."
      }
    ]
  },
  {
    "q": "Why are IAM roles preferred over IAM users with access keys for data services, and how does cross-account access work?",
    "a": "Roles issue short-lived STS credentials that auto-rotate, so there are no long-lived secrets to leak or rotate manually; you attach roles to services (Glue, EMR, Lambda) instead of embedding keys. Cross-account access uses sts:AssumeRole: the target account's role has a trust policy naming the source account/principal, and the caller's identity policy allows assuming it, so both sides must agree. Scope the trust policy to specific principals (no wildcard) and optionally require MFA or an ExternalId.",
    "fs": "Explains STS short-lived creds, the two-sided trust + identity policy handshake, and locks the trust policy down.",
    "fj": "Hardcodes access keys in the job or environment and rotates them 'eventually'.",
    "level": "Mid",
    "followups": [
      {
        "q": "What problem does ExternalId solve that a trust policy naming the account doesn't?",
        "a": "It prevents the 'confused deputy': if a third-party SaaS assumes a role into many customers' accounts, a trust policy naming only the SaaS account would let one customer trick the SaaS into accessing another's role. Requiring a unique `sts:ExternalId` per customer ties each assume-call to the right tenant. It's specifically for cross-account access brokered by a shared third party."
      },
      {
        "q": "A workload runs in EKS/Kubernetes, not on EC2. How does it get role credentials without a node role?",
        "a": "Use IRSA (IAM Roles for Service Accounts) or EKS Pod Identity: the pod's service account is mapped to an IAM role, and the pod gets short-lived STS creds via an OIDC web-identity token — no node-wide role, no static keys. This gives per-pod least-privilege. For non-AWS workloads generally, the pattern is `AssumeRoleWithWebIdentity` against a trusted OIDC provider."
      },
      {
        "q": "How does role chaining bite you with session duration?",
        "a": "When one role assumes another (chaining), the resulting session is capped at 1 hour regardless of the role's `MaxSessionDuration`, and you can't extend it by re-assuming within the chain the same way. Long-running jobs that chain roles can have credentials expire mid-run. Refresh via the SDK's credential provider or avoid unnecessary chaining for long workloads."
      }
    ]
  },
  {
    "q": "For an intermittent nightly Spark batch, would you run EMR on EC2 or EMR Serverless, and how do Spot Instances factor in?",
    "a": "For spiky/intermittent jobs EMR Serverless is usually simpler and cheaper because you pay only for the vCPU/memory the job consumes with no cluster to keep warm. EMR on EC2 wins for sustained or 24/7 workloads, especially with Spot on task nodes (up to ~90% off) using instance fleets across multiple instance types and AZs so a Spot reclaim just shifts to another pool. Keep the primary and core nodes on On-Demand for stability; EMR Serverless doesn't support Spot.",
    "fs": "Matches workload shape to the service, puts only task nodes on Spot via diversified instance fleets.",
    "fj": "Runs everything on a single On-Demand instance type, or puts core/primary nodes on Spot and loses HDFS.",
    "level": "Sr",
    "followups": [
      {
        "q": "Why is putting core nodes on Spot specifically dangerous, beyond just 'losing a node'?",
        "a": "Core nodes run HDFS DataNodes, so a Spot reclaim takes block replicas with it; lose enough and you get under-replicated or lost blocks and failed stages, not just reduced capacity. Task nodes are stateless compute, so reclaiming them only loses in-flight tasks Spark re-runs. Keep state-bearing core/primary on On-Demand; gamble only on stateless task nodes."
      },
      {
        "q": "How do you make a Spot-heavy EMR job resilient to a wave of reclamations?",
        "a": "Use instance fleets with diversified instance types/sizes and an allocation strategy (capacity-optimized) so EMR draws from the deepest, least-interruptible pools and refills from alternates on reclaim. Pair with Spark task retry and speculative execution, and keep a small On-Demand core baseline. The goal is no single Spot pool being a single point of failure."
      },
      {
        "q": "EMR Serverless can't use Spot — so how does it still control cost for bursty jobs?",
        "a": "It scales workers up and down per job and bills only for the vCPU/GB-seconds actually consumed, with pre-initialized capacity optional for fast starts. There's no idle cluster to pay for between nightly runs, which is often the bigger win than Spot's discount for intermittent workloads. You trade the Spot discount for zero idle cost and zero cluster ops."
      }
    ]
  },
  {
    "q": "An EMR Spark job runs but is slow and the cluster is half-idle. How do you diagnose and tune it?",
    "a": "Half-idle usually means under-parallelized: too few partitions for the executor count, or skew sending most data to a few tasks. Check the Spark UI for stage straggler tasks and spill, right-size executor instances/cores/memory to the cluster, and repartition or salt skewed keys so work spreads evenly. Also confirm you're not bottlenecked on a small-file read or a single-shard source upstream.",
    "fs": "Goes to the Spark UI, reasons about partition count vs cores and skew/spill, then salts or repartitions.",
    "fj": "Just adds bigger instances or more nodes without finding the actual bottleneck.",
    "level": "Sr",
    "code": [
      {
        "label": "salt a skewed join key",
        "lang": "pyspark",
        "lines": [
          "from pyspark.sql import functions as F",
          "n = 16",
          "df = df.withColumn('salt',",
          "  (F.rand()*n).cast('int'))",
          "# join on (key, salt) to spread",
          "# the hot key across n tasks"
        ],
        "accent": "fix"
      }
    ],
    "followups": [
      {
        "q": "In Spark 3.x with AQE on, would you still hand-salt a skewed join?",
        "a": "Often not first — Adaptive Query Execution's skew-join handling auto-splits oversized partitions at runtime, so enable `spark.sql.adaptive.enabled` and `skewJoin.enabled` and re-measure. Hand-salting is the fallback when AQE can't detect the skew (e.g. skew hidden behind a UDF or non-equi join) or for streaming. Try AQE before manual salting; salt only the cases it misses."
      },
      {
        "q": "The Spark UI shows heavy disk spill on a shuffle stage. What does that point to and how do you fix it?",
        "a": "Spill means partitions don't fit in executor memory during shuffle/aggregation, so Spark writes to disk, tanking speed. Increase shuffle partitions (`spark.sql.shuffle.partitions`) so each is smaller, give executors more memory, or reduce data volume earlier with predicate/column pushdown. Spill is a sizing signal: smaller partitions or more memory, not a bigger cluster blindly."
      },
      {
        "q": "Idle could mean the bottleneck is the driver, not executors. How would you spot that?",
        "a": "If executors sit idle while the job crawls, suspect the driver: huge `collect()` results, broadcasting an oversized table, or driver-side metadata listing of millions of files. The Spark UI shows the driver busy and stages not launching tasks. Fix by avoiding `collect`, capping broadcast size, and using a table format/manifest to avoid massive S3 listings on the driver."
      }
    ]
  },
  {
    "q": "When does Amazon Bedrock beat Amazon SageMaker for a data/AI feature, and when do you need SageMaker?",
    "a": "Bedrock is a serverless API over hosted foundation models (Claude, Titan, Llama) with managed Knowledge Bases for RAG and Agents, so reach for it for fast time-to-value, spiky traffic that scales to zero cost, and access to proprietary models you can't self-host. SageMaker is for control: custom training, deep fine-tuning, bespoke architectures, and hosting your own models on dedicated infrastructure. A common split is Bedrock for the GenAI app layer and SageMaker for classic ML training/serving.",
    "fs": "Frames Bedrock as managed FM inference + RAG and SageMaker as train/fine-tune/host-your-own control.",
    "fj": "Thinks SageMaker and Bedrock are competitors for the same job, or that you must train a model to do RAG.",
    "level": "Jr",
    "followups": [
      {
        "q": "Can you fine-tune on Bedrock, or does any customization force you to SageMaker?",
        "a": "Bedrock supports custom models via fine-tuning and continued pre-training on supported base models, plus model distillation, with the result hosted on Provisioned Throughput. So light-to-moderate customization stays on Bedrock. You move to SageMaker when you need a novel architecture, full control of the training loop, or to host an open-weight model Bedrock doesn't offer."
      },
      {
        "q": "When is per-token Bedrock pricing the wrong cost model versus a SageMaker endpoint?",
        "a": "At sustained high volume, per-token on-demand can exceed the cost of a dedicated endpoint. Bedrock's answer is Provisioned Throughput (committed model units) for predictable high load; if even that's pricier than self-hosting an open model, a SageMaker real-time endpoint on right-sized GPUs wins. Match steady, heavy traffic to committed capacity, spiky traffic to per-token."
      },
      {
        "q": "Your data can't leave the VPC. Does Bedrock still work?",
        "a": "Yes — Bedrock supports VPC interface endpoints (PrivateLink) so API calls stay off the public internet, and prompts/completions aren't used to train the base models. For stricter isolation or fully self-contained inference you'd self-host on SageMaker. So data-residency alone doesn't force SageMaker; verify the PrivateLink + no-data-retention guarantees meet the requirement."
      }
    ]
  },
  {
    "q": "You're building RAG over internal docs on AWS. What does a Bedrock Knowledge Base handle for you, and what still needs engineering?",
    "a": "A Bedrock Knowledge Base manages the RAG plumbing: it chunks and embeds your S3 documents, stores vectors in a backing store (e.g. OpenSearch Serverless, Aurora pgvector), and at query time retrieves relevant chunks and passes them to the foundation model with citations. You still own data quality, chunking strategy and metadata, ingestion/refresh scheduling, access control on the source data, and evaluation of answer quality and hallucination. The retrieval config (top-k, filters) and prompt template are yours to tune.",
    "fs": "Knows the KB does chunk/embed/retrieve+cite but still owns chunking strategy, refresh, access, and eval.",
    "fj": "Assumes 'point it at S3 and it's done' with no thought to chunking, freshness, or evaluation.",
    "level": "Sr",
    "followups": [
      {
        "q": "Vector store choice now includes S3 Vectors — when do you pick it over OpenSearch Serverless?",
        "a": "S3 Vectors is the low-cost, quick-create option (up to ~2 billion vectors per index, 10,000 indexes per vector bucket) ideal for large, latency-tolerant RAG corpora where storage cost dominates. OpenSearch Serverless suits low-latency, high-QPS retrieval, hybrid keyword+vector search, and binary vectors. Pick S3 Vectors to cut cost at scale; OpenSearch when query latency and richer search matter."
      },
      {
        "q": "Retrieval pulls the right chunks but answers are still wrong. Where do you look?",
        "a": "Separate retrieval from generation: check whether the cited chunks actually contain the answer (a chunking problem) or whether the model ignored them (a prompt problem). Tune chunk size/overlap, add metadata filters, raise top-k, or tighten the prompt to force citation-grounded answers. Use Bedrock's RAG evaluation to score retrieval relevance and faithfulness separately."
      },
      {
        "q": "Different users must only retrieve docs they're authorized to see. How do you enforce that in a KB?",
        "a": "Attach metadata to each document (e.g. `tenant`, `acl_group`) at ingestion and pass metadata filters on the retrieve call scoped to the caller's identity, so retrieval only returns permitted chunks. Don't rely on the model to redact — filter at retrieval. For hard isolation, separate knowledge bases or indexes per tenant prevent any cross-tenant leakage."
      }
    ]
  },
  {
    "q": "How do the Well-Architected Framework pillars actually change a data pipeline design? Give a concrete tradeoff.",
    "a": "The pillars (Operational Excellence, Security, Reliability, Performance Efficiency, Cost Optimization, and Sustainability) force you to make tradeoffs explicit rather than defaulting. For example, Reliability might push you to multi-AZ Kinesis plus DLQs, while Cost Optimization pushes Spot and lifecycle policies, and you decide where each matters per workload. The value is naming the tradeoff (e.g. lower cost via Spot accepts occasional task reprocessing) instead of pretending one design is best on every axis.",
    "fs": "Names the six pillars and discusses an explicit tradeoff (cost vs reliability) for a real pipeline choice.",
    "fj": "Recites the pillar names as a checklist with no tradeoff or application to the actual design.",
    "level": "Jr",
    "followups": [
      {
        "q": "How does the Sustainability pillar concretely change a data pipeline, beyond being a slogan?",
        "a": "It pushes choices that reduce compute/storage footprint: Graviton instances (better perf-per-watt), serverless that scales to zero, aggressive lifecycle tiering and data minimization, columnar formats that scan fewer bytes, and right-sizing to avoid idle capacity. Many of these overlap with Cost Optimization, which is the point — efficient designs are usually both cheaper and lower-carbon."
      },
      {
        "q": "Operational Excellence is vague for a batch pipeline. What does it actually demand?",
        "a": "Concretely: pipeline-as-code (IaC, versioned), automated deploys with rollback, runbooks, structured logging/metrics/alerting on freshness and failure, and safe replay/backfill. It's the difference between a pipeline a human babysits and one that's observable, reproducible, and recoverable. The test: can on-call diagnose and fix a 3am failure from the dashboards and runbook?"
      },
      {
        "q": "How do you turn the framework from a doc into something that catches issues continuously?",
        "a": "Use the Well-Architected Tool to run reviews and track high-risk items, and wire automated guardrails: AWS Config rules, Security Hub, Trusted Advisor, and Compute Optimizer surface pillar violations (unencrypted buckets, idle resources, single-AZ) continuously instead of at a one-off review. The framework becomes living checks rather than a slide deck."
      }
    ]
  },
  {
    "q": "Give three high-leverage cost levers for a petabyte-scale S3 + Athena lake and explain why each works.",
    "a": "First, convert raw CSV/JSON to partitioned, compressed Parquet, since Athena bills per byte scanned, so columnar plus pruning can cut scan cost by up to ~90%. Second, apply S3 lifecycle policies or Intelligent-Tiering to move cold data to cheaper tiers automatically based on access patterns. Third, compact small files into ~128MB-1GB objects to cut request and planning overhead. Use Storage Lens and Cost Explorer to find where the money actually is before optimizing.",
    "fs": "Quantifies each lever (bytes scanned, tiering, file size) and measures with Storage Lens/Cost Explorer first.",
    "fj": "Jumps to 'use a cheaper region' or guesses at cuts without measuring where spend concentrates.",
    "level": "Mid",
    "code": [
      {
        "label": "lifecycle to cheaper tiers",
        "lang": "ts",
        "lines": [
          "{ Rules: [{",
          "  Filter: { Prefix: 'raw/' },",
          "  Transitions: [{",
          "    Days: 30,",
          "    StorageClass:",
          "      'INTELLIGENT_TIERING' }],",
          "  Status: 'Enabled' }] }"
        ]
      }
    ],
    "followups": [
      {
        "q": "Compaction reduces Athena cost but the compaction job itself costs money. How do you net out positive?",
        "a": "Compact incrementally and only where it pays: target partitions that are queried often and currently have many small files, not the whole cold archive. A table format's `OPTIMIZE`/compaction action does this targeted rewrite. Measure scan-cost savings against the one-time rewrite cost — for hot, frequently-scanned data it's strongly positive; for write-once cold data that's rarely read, skip it."
      },
      {
        "q": "Why might Intelligent-Tiering save less than expected on this lake specifically?",
        "a": "If the lake is millions of small objects, the per-object monitoring fee and the <128KB rule (tiny objects aren't auto-tiered and bill at frequent-access) erode savings. Petabyte lakes with healthy ~128MB-1GB files tier well; small-file lakes don't. Compact first, then tier — and for predictable access curves, explicit lifecycle rules avoid the monitoring fee entirely."
      },
      {
        "q": "You suspect a few queries drive most Athena cost. How do you prove it before optimizing?",
        "a": "Query Athena's query history / `system` views or CloudTrail for per-query `DataScannedInBytes`, and use workgroup metrics in CloudWatch to attribute scan volume by team. Sort by bytes scanned to find the handful of `SELECT *`-on-unpartitioned offenders. Optimize the top scanners first — Pareto usually means a few queries are 80% of the bill."
      }
    ]
  },
  {
    "q": "What is S3 Intelligent-Tiering and when does it backfire?",
    "a": "Intelligent-Tiering auto-moves objects between access tiers (frequent, infrequent, and optional archive tiers) based on observed access, charging a small per-object monitoring fee instead of you guessing the right class. It's ideal when access patterns are unknown or changing. It backfires on huge numbers of tiny objects, where the per-object monitoring and minimum-size accounting (objects under 128KB aren't auto-tiered and bill at frequent-access rates) can outweigh savings, so there predictable lifecycle rules are cheaper.",
    "fs": "Knows the per-object monitoring fee and the small-object (<128KB) caveat, picks lifecycle for tiny files.",
    "fj": "Thinks Intelligent-Tiering is always free savings with no downside.",
    "level": "Jr",
    "followups": [
      {
        "q": "The optional Archive Access tiers can save more — what's the catch versus the auto tiers?",
        "a": "The Frequent/Infrequent auto-tiers have no retrieval latency change, but if you opt into the Archive Access or Deep Archive Access tiers, retrieving those objects requires a restore that takes minutes to hours, like Glacier. So enabling archive tiers reintroduces retrieval latency you must tolerate. Only opt in for data you're confident won't need millisecond access."
      },
      {
        "q": "If access is fully predictable, why is Intelligent-Tiering strictly worse than lifecycle?",
        "a": "Because you're paying the per-object monitoring fee to discover something you already know. Lifecycle rules transition on a fixed schedule with no monitoring charge, so for a known hot-then-cold curve (e.g. logs) explicit `STANDARD_IA`/`GLACIER` transitions are cheaper. Intelligent-Tiering earns its fee only when access patterns are genuinely unknown or irregular."
      },
      {
        "q": "How would you migrate an existing bucket of small files to actually benefit from tiering?",
        "a": "Compact/aggregate them first — pack many small objects into fewer larger ones (e.g. roll up into Parquet or tar archives) so they clear the 128KB auto-tier threshold and amortize the per-object fee. Then either apply Intelligent-Tiering or, since access is now better understood, set lifecycle rules. Fixing the small-file problem is the prerequisite to any tiering win."
      }
    ]
  },
  {
    "q": "What is the Glue Data Catalog's role across Athena, Glue, EMR, and Redshift Spectrum?",
    "a": "The Glue Data Catalog is a central, Hive-compatible metastore holding table schemas, partition metadata, and S3 locations, so Athena, Glue, EMR/Spark, and Redshift Spectrum all query the same definitions without each maintaining its own. A crawler can infer schema and register partitions, though many teams define tables explicitly to avoid surprise schema drift. It's the single source of truth that lets one S3 dataset be queried by many engines consistently.",
    "fs": "Calls it the shared Hive-compatible metastore across engines and is wary of crawler-driven schema drift.",
    "fj": "Thinks the catalog stores the data itself, or that each engine needs its own separate catalog.",
    "level": "Jr",
    "followups": [
      {
        "q": "Why do experienced teams often disable or tightly scope crawlers?",
        "a": "Crawlers re-infer schema on a schedule and can silently change column types, add spurious partitions, or merge incompatible files, breaking queries — schema drift you didn't approve. Defining tables in DDL/IaC makes the schema a reviewed artifact. Crawlers suit discovery of unknown data; for production tables, explicit definitions plus controlled partition registration are safer."
      },
      {
        "q": "How do you add fine-grained (column/row-level) access on top of the catalog?",
        "a": "Layer AWS Lake Formation over the Glue Catalog: it adds column-, row-, and cell-level permissions and tag-based access (LF-Tags) enforced consistently across Athena, Redshift Spectrum, EMR, and Glue, brokering credentials to the engines. The plain catalog plus IAM only gives coarse table/bucket-level control. Lake Formation is the governance layer; the catalog is the metadata layer."
      },
      {
        "q": "Multiple accounts need the same tables. How do you share the catalog without copying definitions?",
        "a": "Use Lake Formation cross-account sharing (or `RAM`) to grant catalog resources to other accounts, and/or Glue resource policies, so consumers query the central catalog directly. With Lake Formation you grant specific databases/tables/columns to a principal in another account. This avoids duplicating and drifting table definitions across accounts."
      }
    ]
  },
  {
    "q": "What's the difference between S3 and a real filesystem that trips up data engineers, and how does it affect Spark writes?",
    "a": "S3 is an object store with a flat key namespace, not a hierarchical filesystem: 'directories' are just key prefixes, there's no atomic rename, and listings can be expensive at scale. Spark's default commit protocol relies on rename to move files from a temp location, which on S3 is a slow copy-then-delete and can leave partial output on failure. Use the EMRFS S3-optimized committer or a table format (Iceberg/Hudi/Delta) to get atomic, correct commits.",
    "fs": "Knows rename isn't atomic on S3 and reaches for the S3 committer or a table format for safe commits.",
    "fj": "Treats S3 like HDFS and assumes _temporary rename-based commits are atomic and free.",
    "level": "Sr",
    "followups": [
      {
        "q": "Now that S3 is strongly consistent, is the commit problem fully solved by the committer?",
        "a": "Strong consistency removed the old eventual-consistency hazards (missed LISTs, 404 after write), but it does not make a multi-file job atomic — a failure mid-commit can still leave a partial set of objects, since there's no transaction across keys. The S3-optimized committer avoids slow renames and reduces partial output, but true all-or-nothing commit needs a table format's atomic metadata swap."
      },
      {
        "q": "How exactly does Iceberg get atomic multi-file commits on a non-transactional store?",
        "a": "Iceberg writes new data files first, then commits by atomically swapping a single metadata pointer (the table's current snapshot) — readers see either the old or new snapshot, never a half-written set. The atomicity lives in the catalog's compare-and-swap on that one pointer, not in S3. This is why table formats give ACID semantics on plain object storage."
      },
      {
        "q": "What's the operational cost of those left-behind partial files and how do you clean up?",
        "a": "Failed jobs and multipart uploads leave orphaned objects that silently accrue storage cost and clutter listings. Set an S3 lifecycle rule to abort incomplete multipart uploads after N days, and use the table format's expire-snapshots/remove-orphan-files maintenance to reclaim space. Unmanaged, these are a slow storage leak nobody notices until the bill."
      }
    ]
  },
  {
    "q": "How do you load 500GB into Redshift efficiently, and why is a single big INSERT the wrong call?",
    "a": "Use COPY from S3, not row-by-row INSERTs: COPY reads many files in parallel, one file slice per node slice, so splitting the data into a number of files that's a multiple of the slice count maximizes parallelism. Single INSERTs serialize through the leader node and are orders of magnitude slower. Pre-sort or load in sort-key order, and run ANALYZE (and VACUUM if needed) after a large load so the planner and zone maps stay accurate.",
    "fs": "Uses parallel COPY with file count tuned to slice count, then ANALYZE/VACUUM after the load.",
    "fj": "Loops INSERT statements or loads one giant gzip file and wonders why only one slice is busy.",
    "level": "Mid",
    "code": [
      {
        "label": "parallel COPY",
        "lang": "sql",
        "lines": [
          "COPY sales",
          "FROM 's3://bucket/sales/part-'",
          "IAM_ROLE 'arn:aws:iam::...:role/rs'",
          "FORMAT AS PARQUET;",
          "ANALYZE sales;"
        ]
      }
    ],
    "followups": [
      {
        "q": "Why does one giant gzip file load on a single slice while many gzip files parallelize?",
        "a": "Gzip isn't splittable, so a single compressed file can't be divided across slices — only one slice reads it, leaving the rest idle. Splitting into many files (ideally a multiple of total slices) lets each slice grab files in parallel. Alternatively use a splittable format like Parquet, which COPY parallelizes even as a smaller number of files."
      },
      {
        "q": "After the COPY you skip VACUUM and queries get slower over time. What degraded?",
        "a": "If rows weren't loaded in sort-key order, they sit in the unsorted region, so zone maps lose pruning power and scans read more blocks. Deletes/updates also leave tombstoned rows consuming space. `VACUUM` re-sorts and reclaims; `ANALYZE` refreshes planner stats. Modern Redshift auto-vacuums in the background, but after a huge load an explicit VACUUM SORT restores performance faster."
      },
      {
        "q": "How would you load this without managing files and slice math at all?",
        "a": "Use a Redshift auto-copy job (continuous COPY from an S3 prefix) or zero-ETL/streaming ingestion, or query the data in place with Redshift Spectrum if you don't need it materialized. For one-off bulk, COPY is still king; for ongoing ingestion, auto-copy removes the manual trigger while keeping COPY's parallelism. Pick managed ingestion when the load is recurring."
      }
    ]
  },
  {
    "q": "Walk me through choosing an EC2 instance family for a workload. What do the family letters actually mean?",
    "a": "The family letter encodes the resource profile: M is balanced general-purpose, C is compute-optimized (high CPU:memory), R/X are memory-optimized for caches and in-memory databases, I/D are storage-optimized with local NVMe, and G/P are GPU/accelerated. You pick the family from the bottleneck, not the total size, then size within it. Graviton (the 'g' suffix, e.g. m7g, c7g) is ARM and usually gives better price-performance, so prefer it unless you have x86-only dependencies.",
    "fs": "Maps the family letter to the bottleneck and reaches for Graviton (ARM) for price-performance by default.",
    "fj": "Picks the biggest instance they can afford, or treats all families as interchangeable 'just more CPU'.",
    "level": "Jr",
    "followups": [
      {
        "q": "What's the actual risk of moving a data workload to Graviton, and how do you de-risk it?",
        "a": "Graviton is ARM64, so any x86-only native dependency (compiled libraries, certain JNI/.so files, some vendor agents) won't run; pure-JVM, Python, and most managed runtimes are fine. De-risk by building multi-arch container images and testing the workload on a Graviton instance before committing. The risk is binary compatibility, not performance — most data tooling has ARM builds now."
      },
      {
        "q": "A job is throughput-bound on local disk, not CPU or RAM. Which family and why?",
        "a": "Storage-optimized I-family (e.g. `i4i`, `im4gn`) with local NVMe gives the highest local IOPS/throughput for shuffle, scratch, or local caches; D-family targets dense HDD throughput for big sequential scans. The catch is local NVMe is ephemeral — wiped on stop — so it's for recomputable scratch, not durable state. Match the storage family to disk-bound work, not a bigger general-purpose box."
      },
      {
        "q": "When does burstable T-family bite a data workload?",
        "a": "T-instances accrue CPU credits and throttle to a baseline when credits run out, so a sustained-CPU batch job will exhaust credits and crawl (unless unlimited mode, which then bills overage). They're fine for spiky, mostly-idle services but wrong for steady compute. For a continuous Spark/ETL node, use M/C; reserve T for low-utilization, bursty endpoints."
      }
    ]
  },
  {
    "q": "Your Java Lambda has painful cold starts hurting p99 latency. What are your options and their tradeoffs?",
    "a": "Provisioned Concurrency keeps a pool of initialized environments warm so there's no init cost, but you pay for it 24/7 whether used or not. SnapStart instead takes a snapshot of the initialized environment and restores it, giving sub-second starts at no extra charge, and now supports Java, Python 3.12+, and .NET 8+. SnapStart can't be combined with Provisioned Concurrency and doesn't allow EFS or >512MB ephemeral storage. For steady predictable traffic use Provisioned Concurrency; for spiky traffic prefer SnapStart, and trim the init phase (lazy-load, smaller deps) either way.",
    "fs": "Distinguishes Provisioned Concurrency (always-paid warm pool) from SnapStart (free snapshot restore) and knows they're mutually exclusive.",
    "fj": "Pings the function on a schedule to keep it warm and calls it solved, ignoring concurrency scale-out.",
    "level": "Mid",
    "followups": [
      {
        "q": "SnapStart restores from a snapshot — what subtle bug does that introduce around uniqueness or connections?",
        "a": "Anything in the snapshot is shared by every restored environment: a random seed, a generated UUID, or an open DB connection becomes duplicated/stale across restores. Use the `afterRestore` runtime hook to re-seed RNGs, refresh credentials, and re-open connections. The footgun is treating init-time state as per-invocation unique when it's actually frozen in the snapshot."
      },
      {
        "q": "Why doesn't a scheduled 'keep-warm' ping actually solve cold starts under real load?",
        "a": "A ping keeps one (or a few) environments warm, but a traffic spike that needs 50 concurrent executions still cold-starts 49 new environments. Cold starts scale with concurrency, not just idleness. Provisioned Concurrency reserves a warm pool sized to expected concurrency; SnapStart makes each new start fast. Pinging addresses idle, not scale-out — which is where p99 actually suffers."
      },
      {
        "q": "Provisioned Concurrency costs too much at your scale. How do you size it instead of buying for peak?",
        "a": "Set Provisioned Concurrency to cover the steady baseline and let on-demand (with SnapStart or a tolerable cold start) absorb bursts above it, using Application Auto Scaling to schedule or target-track PC up/down by time of day. You pay warm-pool rates only for predictable baseline traffic. Buying PC for absolute peak wastes money on rarely-used warm capacity."
      }
    ]
  },
  {
    "q": "ECS on Fargate vs ECS on EC2 vs EKS — how do you actually choose for a containerized service?",
    "a": "Fargate is serverless containers: no nodes to patch or scale, you pay per-task vCPU/memory per second, best for spiky or low-ops workloads but pricier per unit of sustained compute. ECS on EC2 lets you own and bin-pack the instances, cheaper for steady high-utilization workloads and needed for things Fargate can't do (GPUs in some cases, daemon sets, custom kernels). EKS gives you upstream Kubernetes for portability and a rich ecosystem but adds a $0.10/hr-per-cluster control-plane fee plus real operational complexity. Default to ECS/Fargate for AWS-native simplicity; pick EKS when you need Kubernetes portability or your team already runs it.",
    "fs": "Separates the orchestrator (ECS vs EKS) from the compute model (Fargate vs EC2) and ties EKS to portability + k8s skills.",
    "fj": "Thinks Fargate, ECS, and EKS are three competing things at the same layer, or reaches for EKS by default.",
    "level": "Mid",
    "followups": [
      {
        "q": "Can EKS use Fargate too, and when would you mix EKS with EC2 node groups?",
        "a": "Yes — EKS runs pods on Fargate profiles or on managed/self-managed EC2 node groups, and you can mix them: Fargate for bursty/isolated pods, EC2 nodes for cost-efficient steady load or GPU/daemonset needs. Karpenter then auto-provisions right-sized EC2 nodes on demand. So Fargate-vs-EC2 is a compute choice that applies under either orchestrator, not just ECS."
      },
      {
        "q": "A team picks Fargate for a steady 24/7 high-CPU service. Why might that be the wrong cost call?",
        "a": "Fargate's per-task pricing carries a convenience premium, so for sustained, predictably high utilization you often pay more than bin-packing the same load onto reserved/Spot EC2 with ECS or EKS. Fargate wins when utilization is spiky or ops headcount is scarce. For a constantly-busy fleet you can densely pack, EC2 (especially with Savings Plans/Spot) is typically cheaper."
      },
      {
        "q": "What does Fargate give up that forces EC2 for some data workloads?",
        "a": "Fargate has no GPU support, no privileged containers/custom kernel modules, no daemonsets, capped ephemeral storage, and you can't SSH to the host — so GPU inference/training, host-level agents, or large local-disk shuffle need EC2. EC2 launch type also lets you attach instance-store NVMe for fast scratch. Reach for EC2 when the workload needs hardware or host access Fargate abstracts away."
      }
    ]
  },
  {
    "q": "Explain S3 storage classes and how you'd design a lifecycle policy for logs that are hot for a week then rarely touched.",
    "a": "Standard is for frequently accessed data; Standard-IA and One Zone-IA cost less to store but charge per-GB retrieval and have a 30-day minimum; Glacier Instant Retrieval is for archive needing millisecond access, Glacier Flexible Retrieval for minutes-to-hours, and Deep Archive for the cheapest, 12-hour-retrieval cold data. For week-hot logs, keep them in Standard, then a lifecycle rule transitions to Standard-IA at ~30 days, Glacier Flexible at ~90, and expires or moves to Deep Archive after the retention horizon. Mind the per-class minimum-duration and per-object transition charges so you don't transition tiny objects too aggressively.",
    "fs": "Knows IA/Glacier minimum-duration + retrieval fees and stages transitions to match the access curve.",
    "fj": "Dumps everything in Standard forever, or jumps logs straight to Deep Archive and then can't read them quickly.",
    "level": "Jr",
    "code": [
      {
        "label": "tiered lifecycle",
        "lang": "ts",
        "lines": [
          "{ Rules: [{ Status: 'Enabled',",
          "  Filter: { Prefix: 'logs/' },",
          "  Transitions: [",
          "    { Days: 30, StorageClass:",
          "      'STANDARD_IA' },",
          "    { Days: 90, StorageClass:",
          "      'GLACIER' }],",
          "  Expiration: { Days: 365 } }] }"
        ]
      }
    ],
    "followups": [
      {
        "q": "Logs are hot for only a week — why does the lifecycle rule still wait 30 days before Standard-IA?",
        "a": "Standard-IA and the Glacier classes carry minimum-duration charges (30 days for IA, 90 for Glacier Flexible, 180 for Deep Archive): transitioning before that bills you for the full minimum anyway, plus a per-object transition fee. So transitioning at day 7 can cost more than leaving it in Standard until day 30. The minimums set the floor on when transitions actually save money."
      },
      {
        "q": "When is One Zone-IA appropriate for logs and what's the real risk?",
        "a": "One Zone-IA stores in a single AZ, ~20% cheaper than Standard-IA, fine for data you can recreate or that isn't business-critical — like reprocessable raw logs. The risk: if that AZ is lost, the data is gone (no multi-AZ durability). Use it only for recomputable or non-critical data; never for the sole copy of something you can't regenerate."
      },
      {
        "q": "Compliance requires logs be immutable for 7 years. How do you enforce that with lifecycle alongside tiering?",
        "a": "Use S3 Object Lock in compliance mode with a 7-year retention so nobody — including root — can delete or overwrite before expiry, and pair it with lifecycle transitions to Glacier Deep Archive for cheap long-term storage. Lifecycle expiration won't fire until the lock retention lapses. Object Lock enforces the immutability; tiering minimizes the cost of holding it."
      }
    ]
  },
  {
    "q": "What is S3's consistency model today, and how does it change how you write pipelines?",
    "a": "Since Dec 2020 S3 provides strong read-after-write consistency for all GET, PUT, LIST, and DELETE operations in every region, so a successful write is immediately readable and a listing reflects it. That removed the old eventual-consistency hazard where a freshly written object might 404 or a LIST might miss it, which used to force workarounds like consistency layers (EMRFS consistent view). You no longer need those, but note consistency is per-object: it does not make a multi-object write transactional, so for atomic multi-file commits you still want a table format like Iceberg.",
    "fs": "States S3 is now strongly consistent for reads/lists but stresses it's per-object, not multi-object transactional.",
    "fj": "Still adds eventual-consistency retry hacks, or assumes strong consistency makes a multi-file write atomic.",
    "level": "Mid",
    "followups": [
      {
        "q": "Strong consistency is per-object — what consistency does object versioning add or not add?",
        "a": "With versioning, a PUT creates a new version and reads are strongly consistent for that version, but lists and the 'current version' pointer still describe single-object state, not a cross-object transaction. A DELETE adds an immediately-consistent delete marker. Versioning gives recoverability and per-object history, not multi-object atomicity — that still needs a table format."
      },
      {
        "q": "Given strong consistency, why might a Spark job still see a 'missing' file it just wrote?",
        "a": "Not an S3 consistency issue — more likely the writer and reader disagree on the path (eventual catalog/metastore update, a committer still finalizing, or a partition not yet registered), or the file is in a different prefix than the listing scanned. The bytes are consistent; the metadata/coordination layer lags. Look at the commit protocol and catalog, not S3 read-after-write."
      },
      {
        "q": "Does strong consistency mean you can drop idempotency in S3-triggered pipelines?",
        "a": "No — consistency is about reads reflecting writes, not about exactly-once event delivery. S3 event notifications are still at-least-once, so duplicate triggers happen regardless of consistency. You still need idempotent processing and dedup. Strong consistency removed the 'wait-and-retry for the object to appear' hack, not the need to handle duplicate events."
      }
    ]
  },
  {
    "q": "EBS vs EFS vs instance store — when do you use each for a data workload?",
    "a": "EBS is a network block volume attached to one instance (gp3 for general SSD, io2 for high-IOPS, st1/sc1 for throughput HDD), durable and resizable, ideal for a database's data directory. EFS is a managed NFS filesystem mountable by many instances at once, elastic and pay-per-use, good for shared scratch or a fleet reading the same files, but higher latency than EBS. Instance store is physically attached NVMe that's blazing fast but ephemeral — it's wiped on stop/terminate — so it's only for caches, scratch, or shuffle data you can recompute. Pick EBS for single-attach durable block, EFS for shared POSIX, instance store for fast-but-disposable.",
    "fs": "Maps EBS=single-attach durable block, EFS=shared NFS, instance store=ephemeral fast scratch; warns instance store is wiped on stop.",
    "fj": "Stores a database on instance store, or thinks EBS can be mounted read-write by many instances like EFS.",
    "level": "Jr",
    "followups": [
      {
        "q": "Doesn't EBS Multi-Attach let many instances share a volume — why isn't that 'EFS but block'?",
        "a": "io1/io2 Multi-Attach allows a volume to attach to up to 16 instances, but they share raw block storage with no built-in coordination, so you need a cluster-aware filesystem (e.g. GFS2) or you'll corrupt data — a standard filesystem assumes single-writer. EFS is a managed NFS that handles concurrent POSIX access for you. Multi-Attach is a niche HA-cluster feature, not a general shared filesystem."
      },
      {
        "q": "On gp3 you can tune IOPS and throughput independently. Why is that a cost win over gp2?",
        "a": "gp2's IOPS scaled with volume size, so to get more IOPS you over-provisioned capacity. gp3 decouples them: you pay for the baseline plus exactly the extra IOPS/throughput you need without inflating size. For an IOPS-hungry but not space-hungry DB, gp3 is cheaper and more predictable. Right-size the three dimensions separately instead of buying disk you don't need."
      },
      {
        "q": "Spark shuffle on instance store is fast but a node dies mid-job. What's the resilience story?",
        "a": "Instance-store shuffle data vanishes with the node, so Spark must recompute the lost map outputs from lineage — fine for a few losses, costly if a whole stage's shuffle is gone. Mitigate with replication or external shuffle service, or accept recompute for cheap interruptible (Spot) clusters. Instance store trades durability for speed; the framework's lineage is your safety net, not the disk."
      }
    ]
  },
  {
    "q": "How does a NAT Gateway work, why is it a common cost surprise, and what's the cheaper alternative for AWS-service traffic?",
    "a": "A NAT Gateway lets instances in a private subnet make outbound connections to the internet while staying unreachable inbound, by translating their private IPs to its own. It bills both an hourly rate and a per-GB data-processing charge, so high-volume egress (e.g. pulling large datasets, container image churn) racks up cost fast, and you pay per AZ if you run one per AZ for HA. For traffic to AWS services like S3 or DynamoDB, use a VPC Gateway Endpoint (free) instead of routing through NAT, and Interface (PrivateLink) endpoints for other services to keep traffic off the public internet.",
    "fs": "Knows NAT charges hourly + per-GB and routes S3/DynamoDB via free Gateway Endpoints to dodge NAT data-processing fees.",
    "fj": "Routes all private-subnet traffic including S3 through the NAT Gateway and is shocked by the data-processing bill.",
    "level": "Mid",
    "code": [
      {
        "label": "free gateway endpoint for S3",
        "lang": "ts",
        "lines": [
          "// VPC Gateway Endpoint (no NAT, no $/GB)",
          "{ ServiceName:",
          "   'com.amazonaws.us-east-1.s3',",
          "  VpcEndpointType: 'Gateway',",
          "  RouteTableIds: [privateRt] }",
          "// S3/DynamoDB only support Gateway type"
        ]
      }
    ],
    "followups": [
      {
        "q": "Interface endpoints aren't free — when do they still beat NAT, and when do they cost more?",
        "a": "Interface (PrivateLink) endpoints bill hourly per-AZ plus per-GB, so for high-volume traffic to a supported AWS service they beat NAT (lower per-GB, keep traffic private). But for low-volume access to many different services, the per-endpoint hourly fees can exceed a single NAT. Weigh number-of-services and traffic volume; for S3/DynamoDB always prefer the free Gateway endpoint."
      },
      {
        "q": "After adding an S3 Gateway endpoint, some cross-region S3 calls still go through NAT. Why?",
        "a": "A Gateway endpoint only serves S3/DynamoDB in the same region as the endpoint; traffic to a bucket in another region has no matching prefix-list route and falls back to the NAT/internet path. Either keep access in-region or add interface endpoints/replication. The endpoint is region-scoped, so cross-region access silently bypasses it and incurs NAT charges."
      },
      {
        "q": "What's the resilience tradeoff of running one NAT Gateway instead of one per AZ to save money?",
        "a": "A single NAT lives in one AZ; if that AZ fails, every private subnet routing through it loses outbound connectivity, and cross-AZ traffic to reach it also incurs cross-AZ data charges. One-NAT-per-AZ removes that single point of failure and the cross-AZ hop, at the cost of more hourly fees. It's the classic Reliability-vs-Cost tradeoff — decide by the workload's availability requirement."
      }
    ]
  },
  {
    "q": "A teammate says 'just open the security group inbound, NACLs are redundant.' Where are they wrong?",
    "a": "Security Groups and NACLs operate at different layers and behave differently. A Security Group is stateful and attached to an ENI/instance: return traffic for an allowed connection is automatically permitted, and it supports allow-rules only. A NACL is stateless and attached to a subnet: it evaluates each direction independently, supports both allow and deny, and because it's stateless you must explicitly allow ephemeral return ports. NACLs are the right tool for coarse subnet-wide deny rules (e.g. block a bad CIDR) that a Security Group can't express; SGs are your fine-grained per-resource allow-listing. They complement, not replace, each other.",
    "fs": "Frames SG=stateful per-resource allow-only vs NACL=stateless per-subnet allow+deny, needing ephemeral return ports.",
    "fj": "Thinks SGs and NACLs are duplicates, or forgets NACL statelessness and blocks all return traffic.",
    "level": "Jr",
    "followups": [
      {
        "q": "A NACL allows inbound 443 but connections still hang. What's the classic stateless mistake?",
        "a": "The outbound NACL rule for the ephemeral return ports (typically 1024-65535) is missing or denied, so the response packets get dropped — NACLs evaluate each direction independently and don't track connections. With a stateful SG you'd never notice. Always add the ephemeral-port range on the opposite direction when using NACLs."
      },
      {
        "q": "How does SG rule ordering differ from NACL rule ordering, and why does it matter?",
        "a": "SG rules are unordered and allow-only — the effect is the union of all allows, with no denies and no precedence. NACL rules are numbered and evaluated in order, first match wins, and support explicit deny, so a low-numbered deny can short-circuit a later allow. So in a NACL, rule numbering is load-bearing; in an SG, you just add allows and order is irrelevant."
      },
      {
        "q": "Give a real case where you genuinely need a NACL, not just an SG.",
        "a": "Blocking a specific malicious CIDR subnet-wide: SGs can't express deny, so you can't 'allow everything except this range' with them. A NACL deny rule on the subnet stops that CIDR before it reaches any instance. NACLs are also the coarse blast-radius control for an entire subnet during an incident — one place to lock down many resources at once."
      }
    ]
  },
  {
    "q": "When would you choose PrivateLink (interface endpoints) vs VPC peering vs Transit Gateway to connect networks?",
    "a": "VPC peering is a 1:1 non-transitive link between two VPCs — simple and cheap for a handful, but it doesn't scale because N VPCs need N-squared peerings and routes can't transit through. Transit Gateway is a hub-and-spoke router that connects many VPCs (and on-prem via VPN/Direct Connect) with transitive routing, the standard for a large multi-VPC network. PrivateLink exposes a single service privately across VPCs/accounts via an interface endpoint without exposing the whole network or overlapping-CIDR concerns, so it's for 'expose one service to consumers' rather than 'connect two networks.' Choose by whether you're sharing a service (PrivateLink) or routing between networks at small (peering) or large (TGW) scale.",
    "fs": "Distinguishes service-exposure (PrivateLink) from network-routing, and peering's non-transitive N-squared limit vs TGW hub.",
    "fj": "Uses VPC peering for everything and ends up with a mesh that can't transit or scale.",
    "level": "Sr",
    "followups": [
      {
        "q": "Two VPCs have overlapping CIDRs but must share one service. Why does PrivateLink work where peering and TGW fail?",
        "a": "Peering and TGW route at the IP layer, so overlapping CIDRs are ambiguous and disallowed. PrivateLink exposes a single endpoint via an ENI in the consumer's own address space behind a Network Load Balancer, so no route between the two networks exists — only the one service is reachable, and the CIDR overlap is irrelevant. It's service-level, not network-level connectivity."
      },
      {
        "q": "Transit Gateway gives transitivity, but what's its cost/latency catch versus peering?",
        "a": "TGW bills per attachment-hour plus per-GB data processing, and adds a small extra hop/latency versus a direct peering connection. For just two VPCs exchanging high volume, peering is cheaper and lower-latency. TGW earns its cost once you have many VPCs needing transitive any-to-any and on-prem connectivity, where the peering mesh becomes unmanageable."
      },
      {
        "q": "How do you stop TGW's transitive routing from letting every VPC reach every other VPC?",
        "a": "Use multiple TGW route tables and associate/propagate attachments selectively so you create isolated routing domains (e.g. prod can't route to dev, spokes can reach shared services but not each other). Transitivity is a capability, not a mandate — segment with route tables to enforce least-connectivity. Otherwise a flat TGW becomes one big open network."
      }
    ]
  },
  {
    "q": "Explain Route 53 routing policies and give a use case where the wrong one causes an outage.",
    "a": "Route 53 supports simple, weighted (split traffic by percentage, good for canaries), latency-based (route to the lowest-latency region), failover (active/passive with health checks), geolocation/geoproximity, and multivalue answer. Failover and latency policies depend on health checks to pull unhealthy endpoints; if you use a plain weighted or simple record without health checks, DNS keeps sending traffic to a dead endpoint. A classic outage is a multi-region active/passive setup configured as 'simple' so failover never triggers, or TTLs set so high that clients cache the failed IP long after Route 53 updates.",
    "fs": "Names the policies, ties failover/latency routing to health checks, and flags high TTL as a failover trap.",
    "fj": "Thinks Route 53 is just DNS hosting and doesn't know it can do health-checked failover or weighted canaries.",
    "level": "Mid",
    "followups": [
      {
        "q": "Failover is configured with health checks but clients still hit the dead region for minutes. What's the lever?",
        "a": "DNS TTL: clients and resolvers cache the old record for the TTL duration regardless of how fast Route 53 flips, so a 300s TTL means up to 5 minutes of stale routing. Lower the TTL (e.g. 60s) on failover-critical records to shrink the window, accepting more DNS queries. For instant failover, front with a regional health-checked load balancer instead of relying on DNS alone."
      },
      {
        "q": "Latency-based routing sends users to a region that's up but degraded. Why, and how do you catch it?",
        "a": "Latency routing picks the lowest-latency healthy endpoint, but a shallow health check (just TCP/HTTP 200 on the LB) can report 'healthy' while the app behind it is failing requests. The check passes, so traffic keeps flowing to a broken region. Use deep/calculated health checks that exercise a real dependency path so 'degraded' actually deregisters the endpoint."
      },
      {
        "q": "When is multivalue answer routing the right pick over a load balancer?",
        "a": "Multivalue returns several healthy IPs and lets the client pick, giving health-checked DNS-level distribution without an ELB — useful for lightweight or cost-sensitive setups. But it's not a load balancer: no connection-aware balancing, and clients may not retry well. Use it for cheap basic availability; use an ALB/NLB when you need real load balancing and routing logic."
      }
    ]
  },
  {
    "q": "Why is an IAM role preferred over an IAM user, and how does STS AssumeRole actually deliver credentials?",
    "a": "An IAM user has long-lived static access keys that must be manually rotated and can be leaked; a role has no permanent credentials and instead vends short-lived STS tokens on demand. When a principal calls sts:AssumeRole, STS returns a temporary access key, secret, and session token that expire (15 min to 12 hr), and the SDK refreshes them automatically. Services like EC2/Lambda/ECS assume their execution role transparently via the instance/task metadata, so your code never holds a static key. The role's trust policy controls who may assume it; its permission policy controls what the resulting session can do.",
    "fs": "Explains roles vend expiring STS tokens via AssumeRole and that trust policy (who) is separate from permissions (what).",
    "fj": "Creates an IAM user, bakes its access keys into the app, and rotates them 'when we get to it'.",
    "level": "Mid",
    "code": [
      {
        "label": "assume a role for temp creds",
        "lang": "python",
        "lines": [
          "sts = boto3.client('sts')",
          "r = sts.assume_role(",
          "  RoleArn='arn:aws:iam::111:role/etl',",
          "  RoleSessionName='job-42')",
          "c = r['Credentials']  # expires",
          "s3 = boto3.client('s3',",
          "  aws_access_key_id=c['AccessKeyId'],",
          "  aws_session_token=c['SessionToken'])"
        ]
      }
    ],
    "followups": [
      {
        "q": "How does an EC2 instance get role creds without anyone calling AssumeRole in code?",
        "a": "EC2 assumes the instance profile's role for you and exposes rotating credentials via the Instance Metadata Service; the SDK's default credential chain fetches and auto-refreshes them. You should enforce IMDSv2 (session-token-required) to block SSRF attacks that steal those creds. No code, no static keys — but lock down metadata access."
      },
      {
        "q": "A session policy can shrink an assumed role's permissions. When is that useful?",
        "a": "When you assume a broad role but want a narrower scope for one session — pass an inline session policy (or policy ARNs) to `AssumeRole` and the effective permissions become the intersection of the role and the session policy. It's used for delegation and per-request least-privilege without minting a new role each time. The session can only be equal to or more restrictive than the role."
      },
      {
        "q": "How would you trace which human ultimately performed an action behind an assumed role?",
        "a": "CloudTrail records the `RoleSessionName` and the assuming principal in the `userIdentity`, and STS source-identity (`sts:SourceIdentity`) can propagate the original human's identity through role chains immutably. Set source identity at assume time and require it via the trust policy so the actor is auditable even after chaining. Without it, sessions can be anonymous behind a shared role."
      }
    ]
  },
  {
    "q": "A user has an IAM policy allowing s3:GetObject but still gets AccessDenied. Walk me through IAM policy evaluation.",
    "a": "AWS denies by default, then evaluates all applicable policies for an explicit deny first — and any explicit Deny (in the identity policy, an SCP, an RCP, a resource policy, a permission boundary, or a session policy) wins over every Allow. For access to be granted the request must be Allowed and not Denied across the intersection of all of these: an SCP or permission boundary that doesn't include s3:GetObject silently blocks it even though the identity policy allows it. So the GetObject denial is usually an org SCP, a permission boundary capping the role, or a bucket policy explicitly denying the principal. Trace it with the IAM Policy Simulator or the Access Denied message's encoded context.",
    "fs": "Knows explicit-deny-wins and that effective permission is the intersection of identity/SCP/boundary/resource policies.",
    "fj": "Just adds s3:* to the identity policy and is confused when access is still denied by an SCP or boundary.",
    "level": "Sr",
    "followups": [
      {
        "q": "Cross-account: identity policy allows it, no deny anywhere, but still denied. What's the rule for resource policies?",
        "a": "Cross-account, both sides must allow: the caller's identity policy AND the resource policy (e.g. the bucket policy) must grant it — neither alone suffices. Within one account a resource-policy allow can stand in for an identity allow, but across accounts you need both. A missing bucket-policy grant for the external principal silently blocks otherwise-valid identity permissions."
      },
      {
        "q": "How do the newer Resource Control Policies (RCPs) change this evaluation?",
        "a": "RCPs are org-level guardrails like SCPs but applied to resources, not principals, so they can deny resource access (e.g. 'only principals from this org may access these S3 buckets') org-wide. They add another deny layer. A request must now survive identity policy, SCP, RCP, permission boundary, session policy, and resource policy — any explicit deny among them wins."
      },
      {
        "q": "What's the fastest way to pinpoint which layer is denying without guessing?",
        "a": "Decode the encoded authorization failure message in the AccessDenied response (`aws sts decode-authorization-message`) to see the evaluated context, and use IAM Access Analyzer / the Policy Simulator to test the exact action+resource+principal. These show whether an SCP, boundary, or resource policy is the blocker. Beats adding `s3:*` and hoping — that won't fix a deny one layer up."
      }
    ]
  },
  {
    "q": "What is a permission boundary and how is it different from an SCP?",
    "a": "A permission boundary is an IAM policy attached to a user or role that caps the maximum permissions that identity can ever have — its effective permissions are the intersection of its identity policies and the boundary, so granting s3:* in the identity policy does nothing if the boundary omits it. It's commonly used for delegated admin: you let a team create roles but attach a boundary so the roles they mint can't exceed a safe envelope. An SCP is an AWS Organizations control that caps permissions for entire accounts/OUs and applies to everyone including the root user, whereas a boundary applies to one specific principal. Both are guardrails that only restrict; neither grants permissions on its own.",
    "fs": "Defines a boundary as a per-principal max-permission cap (intersection) vs SCP as an account-wide org guardrail.",
    "fj": "Thinks a permission boundary grants permissions, or conflates it with an SCP.",
    "level": "Sr",
    "followups": [
      {
        "q": "How do you let a team create roles but guarantee those roles can't escalate privilege?",
        "a": "Grant `iam:CreateRole`/`PutRolePolicy` but add a condition (`iam:PermissionsBoundary`) requiring every role they create to attach a specific boundary, and deny modifying or detaching that boundary. So any role they mint is capped by the envelope, and they can't remove the cap. This is the canonical delegated-admin pattern boundaries exist for."
      },
      {
        "q": "Does an SCP denying an action override a permission boundary that allows it?",
        "a": "They're both restrictive and stack as an intersection, so the effective permission must pass every layer: SCP, boundary, and identity policy all have to allow (and none explicitly deny). An SCP deny wins org-wide regardless of the boundary. The boundary doesn't 'override' the SCP; access requires being permitted by all guardrails simultaneously."
      },
      {
        "q": "Why does the SCP applying to the root user matter operationally?",
        "a": "Because the account root normally bypasses IAM policies, an SCP is the only control that constrains root — so SCPs enforce org-wide guardrails (e.g. 'no leaving the org', 'deny disabling CloudTrail') even a compromised root can't override. A permission boundary only caps the specific principal it's attached to and never touches root. That's a key reason SCPs anchor org security."
      }
    ]
  },
  {
    "q": "What does AWS Lake Formation add on top of the Glue Data Catalog?",
    "a": "Lake Formation layers fine-grained, centralized governance over the data lake that plain IAM + Glue Catalog can't express conveniently: column-, row-, and cell-level permissions, tag-based access control (LF-Tags), and a single grant model enforced consistently across Athena, Redshift Spectrum, EMR, and Glue. Instead of writing sprawling S3 bucket policies and IAM, you grant table/column permissions like a database, and Lake Formation brokers credentials to the query engines. It also streamlines blueprints for ingest and cross-account data sharing. Use it when you need governed, least-privilege analytics access at column/row granularity rather than coarse bucket-level IAM.",
    "fs": "Calls out column/row/cell-level + tag-based permissions enforced across engines, beyond bucket-level IAM.",
    "fj": "Thinks Lake Formation is just another name for the Glue Catalog or only does coarse S3 permissions.",
    "level": "Sr",
    "followups": [
      {
        "q": "Why do LF-Tags scale better than granting on individual tables?",
        "a": "LF-Tag-based access control (TBAC) lets you tag databases/tables/columns (e.g. `classification=PII`) and grant permissions on the tag, so new resources inheriting the tag are automatically governed — no per-resource grant. Direct table grants become an O(tables x principals) sprawl. TBAC turns access into attribute-based policy that scales as the lake grows."
      },
      {
        "q": "How does Lake Formation actually enforce column-level security when Athena queries S3 directly?",
        "a": "Lake Formation registers the S3 location and brokers temporary, scoped credentials to the engine, so Athena/EMR/Redshift Spectrum query through the LF-governed path rather than reading the raw bucket directly. To make this airtight you remove direct IAM S3 access to the data and force all access through Lake Formation. Then it can filter columns/rows before results reach the user."
      },
      {
        "q": "A team bypasses Lake Formation by reading the S3 path directly with IAM. How do you close that hole?",
        "a": "Enforce 'Lake Formation permissions only' by revoking the legacy IAMAllowedPrincipals default and removing direct IAM S3 read grants on the registered locations, so the only path to the data is through LF credential vending. If users keep raw bucket access, column/row controls are moot. Governance only holds when LF is the sole door to the data."
      }
    ]
  },
  {
    "q": "Compare SQS, SNS, and EventBridge. Give a concrete pattern where you'd combine them.",
    "a": "SQS is a durable pull-based queue for decoupling and buffering work so a slow consumer can't drop messages; SNS is push-based pub/sub fan-out delivering one message to many subscribers instantly; EventBridge is a content-based event router with rich rule filtering, schema registry, archive/replay, and SaaS/partner event sources. A common pattern is SNS-to-SQS fan-out: SNS publishes once and multiple SQS queues subscribe, each feeding an independent consumer at its own pace with its own DLQ. EventBridge shines when you need to route by event content to different targets or integrate third-party events, rather than simple broadcast.",
    "fs": "Distinguishes pull-queue (SQS) vs push fan-out (SNS) vs rule-routing (EventBridge) and uses SNS->SQS fan-out.",
    "fj": "Uses SNS as a queue (and loses messages when no subscriber is ready) or thinks EventBridge is just SNS.",
    "level": "Mid",
    "code": [
      {
        "label": "SNS fan-out to SQS",
        "lang": "ts",
        "lines": [
          "// publish once, many queues consume",
          "SNS topic 'order-events'",
          "  -> SQS queue 'billing' (DLQ)",
          "  -> SQS queue 'analytics' (DLQ)",
          "  -> SQS queue 'fulfillment' (DLQ)",
          "// each consumer scales independently"
        ]
      }
    ],
    "followups": [
      {
        "q": "EventBridge can also fan out and filter — why still use SNS->SQS for this?",
        "a": "SNS->SQS gives the highest throughput, lowest latency, and cheapest fan-out where each consumer needs a durable buffer that absorbs backpressure. EventBridge wins for content-based routing, schema registry, archive/replay, or third-party sources — richer but higher per-event cost and latency. For high-volume broadcast to buffered consumers, SNS->SQS is leaner."
      },
      {
        "q": "Why put an SQS queue between SNS/EventBridge and a Lambda instead of invoking Lambda directly?",
        "a": "The queue gives durable buffering, backpressure control (batch size, max concurrency), and a real DLQ, so a downstream outage or throttle doesn't drop events — they wait in the queue. Direct invocation can drop on throttling and has weaker retry semantics. The queue decouples producer spikes from consumer capacity. It's the standard durability buffer in event-driven pipelines."
      },
      {
        "q": "How does EventBridge's archive/replay change incident recovery versus SNS?",
        "a": "EventBridge can archive matched events and replay them to targets later, so after fixing a consumer bug you can reprocess the exact events that failed during the window. SNS has no built-in replay — once delivered (or not), it's gone unless a subscribed SQS queue retained them. For auditable, recoverable event flows, EventBridge's archive is a real operational advantage."
      }
    ]
  },
  {
    "q": "When do you need an SQS FIFO queue instead of Standard, and what does it cost you?",
    "a": "Standard queues give best-effort ordering and at-least-once delivery with very high throughput; FIFO queues give strict in-order delivery and exactly-once processing within a message group, deduplicating by a dedup ID over a 5-minute window. You need FIFO when order matters per entity (e.g. apply account transactions in sequence) — ordering is guaranteed only within a MessageGroupId, and different groups process in parallel. The cost is throughput: FIFO is capped (high-throughput mode helps) and far lower than Standard's effectively unlimited rate, so don't reach for FIFO unless ordering or dedup is a real requirement.",
    "fs": "Knows ordering/dedup is per MessageGroupId and that FIFO trades throughput for exactly-once + ordering.",
    "fj": "Defaults to FIFO 'to be safe' and bottlenecks throughput, or expects global ordering across all groups.",
    "level": "Mid",
    "followups": [
      {
        "q": "How do you choose MessageGroupId to get both ordering and parallelism?",
        "a": "Pick the smallest entity that truly needs ordering as the group — e.g. `account_id` or `user_id` — so messages for one entity stay ordered while different entities process concurrently. One global group serializes everything and tanks throughput; per-message groups give no ordering at all. The group key is the dial between strict order and parallel scale."
      },
      {
        "q": "FIFO 'exactly-once' depends on a 5-minute dedup window. When does a duplicate still slip through?",
        "a": "Content-based or explicit dedup only suppresses duplicates within a 5-minute window; if the same message is re-sent after 5 minutes (e.g. a delayed retry), it's treated as new and delivered again. So FIFO gives exactly-once delivery within the window, not forever. For longer-horizon idempotency you still need a downstream dedup store."
      },
      {
        "q": "Throughput on a FIFO queue is too low. What's the lever before abandoning FIFO?",
        "a": "Enable high-throughput mode for FIFO, which raises the per-queue transaction limit substantially by parallelizing across many message groups — so spreading load across many `MessageGroupId`s is what unlocks the higher rate. If you only have one or a few groups, you stay throughput-bound. Many groups + high-throughput mode is how you scale FIFO without dropping ordering guarantees."
      }
    ]
  },
  {
    "q": "A message keeps reappearing in SQS and getting reprocessed forever. What's happening and how do you fix it?",
    "a": "When a consumer receives a message it becomes invisible for the visibility timeout; if the consumer doesn't delete it within that window (because processing is slow or it crashed), the message becomes visible again and is redelivered. A poison message that always fails will loop forever unless you bound retries. Fix it by setting the visibility timeout above your real processing time (or extend it with ChangeMessageVisibility for long jobs), deleting only after success, and configuring a redrive policy so after maxReceiveCount failures the message moves to a dead-letter queue for inspection instead of cycling.",
    "fs": "Ties redelivery to visibility-timeout vs processing time and adds a DLQ redrive policy to break the poison-message loop.",
    "fj": "Just deletes messages on receipt (losing failures) or keeps bumping the timeout without a DLQ.",
    "level": "Mid",
    "code": [
      {
        "label": "redrive to DLQ",
        "lang": "python",
        "lines": [
          "sqs.set_queue_attributes(QueueUrl=q,",
          "  Attributes={'RedrivePolicy': json.dumps(",
          "    {'deadLetterTargetArn': dlq_arn,",
          "     'maxReceiveCount': '5'})})",
          "# delete only after successful work:",
          "sqs.delete_message(QueueUrl=q,",
          "  ReceiptHandle=h)"
        ]
      }
    ],
    "followups": [
      {
        "q": "Processing time varies wildly. Is a huge fixed visibility timeout the right fix?",
        "a": "No — an oversized timeout means a genuinely failed message sits invisible for a long time before it can retry, hurting recovery latency. Instead set a reasonable timeout and call `ChangeMessageVisibility` to extend it heartbeat-style while a long job is still working. That keeps the message hidden only as long as work is actually progressing."
      },
      {
        "q": "With a Lambda SQS event source, who controls deletes and how does partial batch failure behave?",
        "a": "Lambda deletes messages from the batch automatically on success; if the function throws, the whole batch returns to the queue and retries, re-running already-successful records. Enable `ReportBatchItemFailures` so the function returns only the failed message IDs, and just those are retried. Without it, one poison message reprocesses the entire batch."
      },
      {
        "q": "Messages are landing in the DLQ. What do you do with them operationally?",
        "a": "Alarm on DLQ depth (`ApproximateNumberOfMessagesVisible` > 0) so failures are visible, inspect the messages to find the bug, fix the consumer, then use the SQS DLQ redrive feature to move them back to the source queue for reprocessing. The DLQ is a quarantine and debugging surface, not a graveyard — the loop is detect, fix, redrive."
      }
    ]
  },
  {
    "q": "You're orchestrating a multi-step data pipeline. Step Functions Standard or Express, and why does the choice matter?",
    "a": "Standard workflows run up to a year, are exactly-once, durable, and keep full execution history — use them to orchestrate long, auditable, non-idempotent steps like spinning up EMR, running a Glue job, then loading Redshift. Express workflows run up to 5 minutes, are at-least-once (so steps can re-run), bill by invocation/duration, and handle very high event rates — use them for high-volume short event processing. Picking Express for a long non-idempotent pipeline risks duplicate side effects and a 5-minute cap; picking Standard for a million tiny events costs more in state transitions. You can even nest Express inside a Standard Map for the best of both.",
    "fs": "Matches Standard=long/exactly-once/auditable to orchestration and Express=short/at-least-once/high-volume to event streams.",
    "fj": "Picks based on which sounds faster, ignoring the exactly-once vs at-least-once and 5-minute distinctions.",
    "level": "Sr",
    "followups": [
      {
        "q": "Why is Express's at-least-once execution model specifically dangerous for non-idempotent steps?",
        "a": "Express can re-run the whole workflow on retry, so a step with side effects (charge a card, send an email, insert a row) can fire twice. Standard's exactly-once execution avoids that. If you must use Express for high volume, make every step idempotent (dedup keys, upserts) so re-runs are safe. The delivery model dictates whether your steps need idempotency."
      },
      {
        "q": "How does the difference in execution-history retention change debugging?",
        "a": "Standard records full, queryable execution history (every state transition) for ~90 days, so you can inspect exactly where a run failed. Express doesn't persist detailed history by default — you rely on CloudWatch Logs (and EMF) for observability, which you must explicitly enable. For audit-heavy or hard-to-reproduce pipelines, Standard's built-in history is a major operational advantage."
      },
      {
        "q": "Why does the billing model push high-volume short events to Express?",
        "a": "Standard bills per state transition, so a million tiny executions each with several steps explodes transition count and cost. Express bills by number of executions plus duration/memory, which is far cheaper for huge volumes of brief workflows. So event-stream-scale workloads go Express on cost alone, while a few long orchestrations are cheap under Standard's per-transition model."
      }
    ]
  },
  {
    "q": "How does a Step Functions Distributed Map differ from an inline Map, and when do you need it?",
    "a": "An inline Map iterates over an in-memory array inside one execution, capped at 40 concurrent iterations and the 256KB state payload limit, fine for modest fan-out. Distributed Map runs each iteration as its own child execution and can read items directly from a large S3 dataset or manifest, scaling to massive parallelism (up to ~10,000 concurrent child executions) well beyond the payload limit. You need Distributed Map for large-scale fan-out like processing millions of S3 objects or CSV rows in parallel, with batching and a tolerated-failure threshold. Inline Map is for small arrays already in your state.",
    "fs": "Knows inline Map is payload-bound/low-concurrency vs Distributed Map reading S3 at massive parallelism.",
    "fj": "Tries to fan out millions of items with an inline Map and hits the 256KB / 40-concurrency wall.",
    "level": "Sr",
    "followups": [
      {
        "q": "Distributed Map can run 10,000 children, but Lambda downstream throttles. How do you protect it?",
        "a": "Set `MaxConcurrency` on the Distributed Map below the downstream service's capacity (Lambda's account concurrency, a database connection pool, an API rate limit), and use ItemBatcher to process N items per child to reduce invocation count. The 10,000 ceiling doesn't exempt you from downstream limits, so throttle the fan-out to match the slowest dependency."
      },
      {
        "q": "One bad record shouldn't fail a million-item job. What controls that?",
        "a": "Distributed Map's tolerated-failure threshold (`ToleratedFailurePercentage`/count) lets the job continue past a configurable number of failed items and still complete, writing per-item results and failures to S3. Inline Map fails the whole state on a single error. So for large batch jobs you set a failure budget and inspect the failed-item manifest afterward rather than losing the whole run."
      },
      {
        "q": "Why nest an Express workflow as the Distributed Map's processor?",
        "a": "Each child execution can be an Express workflow, which is far cheaper per-execution and faster to start than Standard — ideal when you're spawning hundreds of thousands of short item-processing runs. The parent stays Standard for durability/auditability while the high-volume inner work runs as cheap Express children. It's the canonical 'durable outer, cheap massive inner' pattern."
      }
    ]
  },
  {
    "q": "RDS vs Aurora — what does Aurora's architecture actually change about replicas and failover?",
    "a": "Standard RDS replicates with engine-level async replication and Multi-AZ keeps a separate synchronous standby on its own storage, so failover means promoting/repointing and read replicas can lag. Aurora decouples compute from a shared distributed storage layer replicated 6 ways across 3 AZs, so all up to 15 read replicas read the same volume — replica lag is tiny and failover just promotes an existing replica in seconds with no volume catch-up. Aurora tolerates losing an entire AZ without data loss. Choose RDS for standard engines and lower cost on modest workloads; Aurora for higher throughput, faster failover, and lots of low-lag read replicas.",
    "fs": "Explains Aurora's shared 6-way storage gives near-zero replica lag and seconds-fast failover vs RDS async replicas.",
    "fj": "Thinks Aurora is just 'RDS but faster' with no idea the storage layer is shared across replicas.",
    "level": "Mid",
    "followups": [
      {
        "q": "Since all Aurora replicas read the same storage, why is there any replica lag at all?",
        "a": "The storage is shared, so there's no data to copy, but a reader still applies in-memory updates (buffer cache, redo apply) to reflect the latest writes, which introduces a tiny millisecond-scale lag. It's far smaller than RDS's async log shipping but not literally zero. For strict read-your-writes you still route critical reads to the writer or use the writer endpoint."
      },
      {
        "q": "When does Aurora Serverless v2 make sense over a provisioned Aurora cluster?",
        "a": "Serverless v2 auto-scales capacity (ACUs) in fine-grained steps with load, so it fits spiky, unpredictable, or intermittent workloads (dev/test, variable traffic) where you'd otherwise over-provision. For steady, predictable high load, provisioned instances with reserved pricing are cheaper. Use Serverless v2 when utilization varies a lot and you want to avoid paying for idle peak capacity."
      },
      {
        "q": "What does Aurora give up versus self-managed Postgres/MySQL on EC2?",
        "a": "You can't touch the OS or the custom storage engine, some extensions and exact engine versions lag upstream, and you're tied to AWS (limited portability). You also pay an Aurora premium over plain RDS. In exchange you get the distributed storage, fast failover, and low-lag replicas. Pick self-managed only when you need an unsupported extension/version or full host control."
      }
    ]
  },
  {
    "q": "Design the DynamoDB key for an app that queries 'all orders for a customer, newest first.' What goes wrong with a naive design?",
    "a": "Use a composite primary key: partition key = CUSTOMER#<id> to co-locate one customer's orders, sort key = a date-prefixed value like ORDER#<timestamp> so a Query on the partition returns them already ordered (ScanIndexForward=false for newest-first). The naive mistake is making order_id the partition key, which scatters a customer's orders across partitions and forces a full Scan to gather them — slow and expensive. Also avoid a low-cardinality or skewed partition key (e.g. a status flag) that creates a hot partition. Model the access pattern into the keys; DynamoDB rewards query-by-key and punishes scans.",
    "fs": "Designs PK=CUSTOMER#id, SK=ORDER#ts for an ordered Query, and avoids order_id-as-PK that forces a Scan.",
    "fj": "Makes each row's unique id the partition key, then Scans + filters to find a customer's orders.",
    "level": "Mid",
    "code": [
      {
        "label": "single-customer query",
        "lang": "python",
        "lines": [
          "# PK=CUSTOMER#42  SK=ORDER#<ts>",
          "table.query(",
          "  KeyConditionExpression=Key('PK')",
          "    .eq('CUSTOMER#42'),",
          "  ScanIndexForward=False,  # newest 1st",
          "  Limit=20)",
          "# no Scan, no filter scan-and-throw-away"
        ]
      }
    ],
    "followups": [
      {
        "q": "One whale customer has millions of orders on a single partition key. What breaks and how do you fix it?",
        "a": "All their orders hash to one partition, which has a hard ~10GB and ~3,000 RCU/1,000 WCU per-partition-key ceiling, so that customer becomes a hot/oversized partition. Shard the key by suffixing a bucket (`CUSTOMER#42#3`) and scatter-gather across shards, or partition orders by time period. The fix is spreading one logical entity's data across multiple partition keys."
      },
      {
        "q": "Now you also need 'orders by status, newest first' across customers. What's the design move?",
        "a": "Add a GSI with `GSI1PK = STATUS#<status>` and `GSI1SK = <timestamp>`, so a Query on the index returns ordered results without scanning. This is GSI overloading in single-table design — reuse generic `GSI1PK/GSI1SK` attributes for many access patterns. Each new query pattern maps to a key design, not a Scan-and-filter."
      },
      {
        "q": "Why is a date-prefixed sort key better than relying on a separate timestamp attribute with FilterExpression?",
        "a": "A sort key lets DynamoDB do a server-side range Query and return items pre-sorted, reading only what matches. A `FilterExpression` runs after the read, so you're billed for and scan all items in the partition before discarding non-matches — expensive and not ordered. Encoding the time into the SK turns a filter-after-scan into an efficient, ordered key query."
      }
    ]
  },
  {
    "q": "When do you add a DynamoDB GSI vs an LSI, and what are the catches with each?",
    "a": "A Local Secondary Index shares the table's partition key but gives an alternate sort key, supports strong consistency, but must be created at table-creation time and shares the 10GB-per-partition-key limit. A Global Secondary Index has a completely different partition and sort key, can be added anytime, but is eventually consistent and has its own provisioned/on-demand capacity. Reach for a GSI when you need to query by a different attribute entirely (the common case in single-table design with overloaded GSI1PK/GSI1SK); use an LSI only when you need an alternate sort within the same partition with strong consistency. Watch GSI projection — projecting all attributes inflates cost and write amplification.",
    "fs": "Knows LSI=same PK/alt SK, create-time-only, strongly consistent vs GSI=new keys, anytime, eventually consistent, own capacity.",
    "fj": "Reaches for an LSI to query by a totally different attribute, or projects ALL into every GSI without cost thought.",
    "level": "Sr",
    "followups": [
      {
        "q": "Why does a GSI with under-provisioned capacity throttle your main table's writes?",
        "a": "Every write to the base table that touches a GSI's keys must also write to that index; if the GSI can't absorb the write (insufficient WCU), DynamoDB throttles the base-table write itself — back-pressure from the index. So an under-provisioned or hot GSI degrades the whole table. Size GSI capacity for the write rate, or use on-demand to avoid the trap."
      },
      {
        "q": "How does GSI projection choice trade read efficiency against write cost?",
        "a": "Projecting `ALL` makes index reads self-contained (no base-table fetch) but copies every attribute on every write, amplifying storage and WCU. `KEYS_ONLY`/`INCLUDE` keep the index small and cheap to write but may force a second read against the base table for missing attributes. Project only the attributes the index's queries actually need — pay for reads you make, not data you don't."
      },
      {
        "q": "Why can't you just add an LSI later when you discover a new sort requirement?",
        "a": "LSIs share the base partition's underlying storage/throughput, so DynamoDB only allows defining them at table creation — there's no add-LSI-later. If you need a new access pattern post-launch, you use a GSI (addable anytime) or rebuild the table. This is a key reason single-table designs lean on GSIs: they're the only secondary index you can evolve into."
      }
    ]
  },
  {
    "q": "Explain KMS envelope encryption and why it's used instead of encrypting data directly with the KMS key.",
    "a": "KMS keys never leave KMS and can't directly encrypt large payloads (there's a ~4KB limit), so services use envelope encryption: KMS generates a unique data key, hands you a plaintext copy to encrypt your data locally and an encrypted copy to store alongside the ciphertext. To decrypt, you send the encrypted data key back to KMS, get the plaintext key, and decrypt locally. This keeps the root key in KMS, lets you encrypt arbitrarily large data fast, and gives per-object keys. Services like S3 SSE-KMS, EBS, and Secrets Manager all do this transparently; the catch at scale is KMS API throttling and per-request cost, which S3 Bucket Keys mitigate by caching a key.",
    "fs": "Describes data-key generation + local encryption with the KMS root key never leaving KMS, and S3 Bucket Keys to cut KMS calls.",
    "fj": "Thinks the KMS key itself encrypts every object directly, with no concept of data keys.",
    "level": "Sr",
    "code": [
      {
        "label": "generate a data key",
        "lang": "python",
        "lines": [
          "kms = boto3.client('kms')",
          "dk = kms.generate_data_key(",
          "  KeyId='alias/app', KeySpec='AES_256')",
          "plaintext_key = dk['Plaintext']  # use",
          "stored_key = dk['CiphertextBlob']  # save",
          "# encrypt data locally with plaintext_key",
          "# discard plaintext, keep stored_key"
        ]
      }
    ],
    "followups": [
      {
        "q": "A Spark job writing millions of SSE-KMS objects hits KMS throttling. How do S3 Bucket Keys fix it?",
        "a": "Without Bucket Keys, each object's encryption calls KMS, so high write volume blows past KMS request quotas. An S3 Bucket Key has S3 generate a short-lived bucket-level data key and reuse it for many objects, collapsing thousands of KMS calls into a few — cutting both throttling and KMS request cost dramatically. Enable Bucket Keys on high-volume SSE-KMS buckets."
      },
      {
        "q": "What does an encryption context add, and why use it?",
        "a": "An encryption context is non-secret key-value pairs bound into the operation that must be supplied identically to decrypt, acting as additional authenticated data. It lets you scope grants/policies (e.g. only decrypt objects for a given tenant) and shows in CloudTrail. It binds ciphertext to its intended context so a data key can't be misused elsewhere."
      },
      {
        "q": "When would you choose a customer-managed key over an AWS-managed key despite the extra work?",
        "a": "Customer-managed keys let you control the key policy, rotation, grants, enable/disable, and cross-account sharing, with CloudTrail events per use — needed for compliance, separation of duties, or revoking access by disabling the key. AWS-managed keys are simpler but you can't customize policy or share them. Use customer-managed for governance, auditability, or cross-account control."
      }
    ]
  },
  {
    "q": "What do GuardDuty and AWS WAF each protect against, and why aren't they interchangeable?",
    "a": "GuardDuty is a threat-detection service: it continuously analyzes CloudTrail, VPC Flow Logs, and DNS logs (and now EKS/S3/malware findings) with ML to surface compromised credentials, crypto-mining, unusual API calls, and reconnaissance — it detects and alerts, it doesn't block. WAF is a web application firewall sitting in front of CloudFront/ALB/API Gateway that inspects HTTP(S) requests and blocks attacks like SQL injection, XSS, and bad bots via managed and custom rules with rate limiting. They cover different layers: WAF is inline prevention at the application edge, GuardDuty is account-wide detection across your AWS activity. You run both, plus respond to GuardDuty findings with automation.",
    "fs": "Separates WAF (inline L7 request blocking) from GuardDuty (account-wide ML threat detection/alerting) — prevent vs detect.",
    "fj": "Thinks one tool does everything, or expects GuardDuty to block attacks inline like a firewall.",
    "level": "Mid",
    "followups": [
      {
        "q": "GuardDuty only alerts. How do you turn a finding into an actual response automatically?",
        "a": "GuardDuty findings emit to EventBridge, so you wire rules that trigger Lambda or Step Functions to remediate — isolate an instance's security group, revoke compromised credentials, quarantine an S3 object, or page on-call. That's the detect-then-respond loop. GuardDuty detects; EventBridge + automation (or Security Hub) closes the gap to action."
      },
      {
        "q": "WAF blocks at L7, but a volumetric L3/L4 DDoS is saturating you. What actually handles that?",
        "a": "AWS Shield (Standard, automatic; Advanced for larger attacks + DDoS response team) handles volumetric network/transport-layer DDoS, which WAF's request-inspection model can't — WAF only sees HTTP requests that already reached the edge. Shield protects the network layer; WAF protects the application layer. For a full picture you layer Shield + WAF, not one or the other."
      },
      {
        "q": "How do you stop a WAF rate-limit or managed rule from blocking legitimate traffic on deploy?",
        "a": "Deploy rules in Count mode first to observe how many legitimate requests they'd block via CloudWatch metrics and sampled requests, then flip to Block once tuned. Scope-down statements and allow-lists protect known-good clients. Rolling a strict managed ruleset straight to Block risks a self-inflicted outage; Count-then-Block is the safe rollout."
      }
    ]
  },
  {
    "q": "What's the difference between a CloudWatch metric, a log, and a trace, and where does X-Ray fit?",
    "a": "CloudWatch Metrics are numeric time series (CPU, latency, custom business counters) you alarm and dashboard on cheaply at scale. CloudWatch Logs are the raw text/JSON event records you search with Logs Insights for detail and debugging. X-Ray provides distributed tracing: it follows a single request across services (API Gateway to Lambda to DynamoDB) and visualizes a service map with per-segment latency to find where time is spent. The mental model: metrics tell you something is wrong, logs tell you what, traces tell you where in a distributed call path. Custom metrics from high-cardinality dimensions get expensive, so push detail to logs/EMF rather than thousands of metrics.",
    "fs": "Maps metrics=alarmable time series, logs=detail, X-Ray traces=cross-service request path; warns on high-cardinality custom metrics.",
    "fj": "Treats logs and metrics as the same thing, or doesn't know X-Ray follows one request across services.",
    "level": "Jr",
    "followups": [
      {
        "q": "You want per-customer latency but a custom metric per customer is too expensive. What's the pattern?",
        "a": "Use the Embedded Metric Format (EMF): log structured JSON with the customer dimension, and CloudWatch extracts metrics from the logs — you keep high-cardinality detail in logs (cheap, queryable) while only materializing the aggregate metrics you alarm on. This avoids exploding the custom-metric count. EMF is the bridge between log detail and metric alarming for high-cardinality data."
      },
      {
        "q": "An average-latency alarm looks fine but customers report slowness. What's the metric mistake?",
        "a": "Averages hide tail latency — a healthy mean can mask a bad p99 affecting a slice of users. Alarm on percentile statistics (p95/p99) and use anomaly detection, not just `Average`. Distributed tracing then localizes which downstream segment causes the tail. Watching averages is the classic reason 'dashboards green, users angry.'"
      },
      {
        "q": "X-Ray sampling drops most traces. How do you still catch the rare slow request?",
        "a": "Default sampling captures a small fraction to control cost, so rare anomalies get missed. Configure sampling rules to always trace specific routes or error conditions, and raise the rate for paths under investigation. For broader coverage, AWS Distro for OpenTelemetry with tail-based sampling keeps slow/error traces. Tune sampling to the debugging need, not the default rate."
      }
    ]
  },
  {
    "q": "Savings Plans vs Reserved Instances — which do you recommend and why, for a fleet that changes instance types often?",
    "a": "For a fleet whose instance families/regions shift, a Compute Savings Plan is usually the best fit: you commit to a $/hour of compute spend for 1 or 3 years (up to ~66% off) and the discount auto-applies across instance families, sizes, regions, OS, tenancy, and even Fargate and Lambda — so refactoring doesn't strand the commitment. An EC2 Instance Savings Plan gives a deeper discount (~72%) but locks you to one family in one region. Standard RIs give the deepest discount but are the most rigid and tied to specific attributes (Convertible RIs trade some discount for flexibility). Match commitment flexibility to how stable the workload is; cover only the steady baseline and leave bursts on On-Demand/Spot.",
    "fs": "Recommends Compute Savings Plan for a shifting fleet (auto-applies across families/Fargate/Lambda) over rigid Standard RIs.",
    "fj": "Buys Standard RIs for a changing fleet and strands the commitment when instance types change.",
    "level": "Mid",
    "followups": [
      {
        "q": "How do you decide how much commitment to buy without over-committing?",
        "a": "Cover only the steady baseline of usage with Savings Plans/RIs and leave the variable peak on On-Demand/Spot — committing to peak strands money during troughs. Use Cost Explorer's Savings Plans recommendations and utilization/coverage reports to size the commitment to the always-on floor. The rule: commit to what you'll definitely run 24/7, flex the rest."
      },
      {
        "q": "Why might an EC2 Instance Savings Plan still beat a Compute Savings Plan despite less flexibility?",
        "a": "It gives a deeper discount (~72% vs ~66%) because you commit to a specific family in a specific region. For a stable, long-lived workload pinned to one instance family (e.g. a steady RDS-adjacent compute tier you won't refactor), the extra savings outweigh the lost flexibility. Match the deeper-but-rigid plan to the part of the fleet that genuinely won't change."
      },
      {
        "q": "What happens to a Standard RI you no longer need, and how does that differ from a Savings Plan?",
        "a": "A Standard RI is tied to specific attributes and can't be reconfigured; if unused you can try to sell it on the RI Marketplace or eat the cost — Convertible RIs can be exchanged but at a lower discount. A Savings Plan has no instance to strand; the $/hour commitment just floats to whatever compute you run. That flexibility is exactly why Savings Plans suit changing fleets."
      }
    ]
  },
  {
    "q": "A tagging-free account has a surprise bill and no idea which team owns what. What's the senior cost-governance playbook?",
    "a": "Start by enforcing a tagging strategy (cost-allocation tags like team, env, project) via tag policies and SCPs that deny resource creation without required tags, then activate those tags in the Billing console so Cost Explorer and Cost and Usage Reports can group by them. Use AWS Budgets with alerts per team/account and anomaly detection to catch spikes early, and consider a multi-account structure (one account per team/env under Organizations) so billing is naturally isolated. Pair this with Compute Optimizer/Trusted Advisor for rightsizing and Savings Plans coverage. The real fix is accountability: untagged, un-isolated accounts make cost everyone's problem and no one's.",
    "fs": "Enforces required cost-allocation tags via tag policies/SCPs, activates them for Cost Explorer/CUR, and isolates by account.",
    "fj": "Opens Cost Explorer hoping to see owners, but with no tags or account isolation there's nothing to group by.",
    "level": "Sr",
    "code": [
      {
        "label": "deny untagged creation (SCP)",
        "lang": "ts",
        "lines": [
          "{ Effect: 'Deny',",
          "  Action: 'ec2:RunInstances',",
          "  Resource: 'arn:aws:ec2:*:*:instance/*',",
          "  Condition: { Null: {",
          "    'aws:RequestTag/team': 'true' } } }",
          "// blocks launches missing a team tag"
        ]
      }
    ],
    "followups": [
      {
        "q": "Why is per-account isolation often more reliable for cost attribution than tags alone?",
        "a": "Tags can be missing, misspelled, or unsupported on some resource types, leaving spend unattributed. An account boundary is unavoidable — every cost lands in exactly one account, so consolidated billing groups cleanly by account/OU with no tagging discipline required. Accounts give structural attribution; tags give finer slicing within them. Mature setups use both."
      },
      {
        "q": "The SCP denies untagged new resources, but the existing bill is full of untagged legacy resources. Now what?",
        "a": "The SCP only stops future drift; for the backlog, run Tag Editor / Resource Groups to bulk-find and tag existing resources, and use AWS Config rules to flag non-compliant ones continuously. Cost Explorer's 'untagged' bucket shows the remaining gap to chase. Enforce-going-forward plus a remediation sweep of the legacy tail is the full play."
      },
      {
        "q": "Budgets alert after spend happens. How do you catch a runaway cost faster?",
        "a": "Add Cost Anomaly Detection, which uses ML to flag unusual spend patterns within roughly a day rather than waiting for a monthly threshold, and scope monitors per service/account/tag. Pair with near-real-time CloudWatch billing alarms for hard ceilings. Budgets set the guardrail; anomaly detection catches the unexpected spike a fixed threshold would miss until it's large."
      }
    ]
  },
  {
    "q": "Name three non-obvious AWS data-transfer cost traps and how you avoid each.",
    "a": "First, cross-AZ traffic: chatty services or a database and its clients split across AZs pay per-GB in both directions, so co-locate tightly-coupled components in one AZ (balancing against HA). Second, NAT Gateway egress: routing S3/DynamoDB or large public pulls through NAT incurs per-GB processing — use free VPC Gateway Endpoints for S3/DynamoDB and Interface endpoints for other services. Third, internet egress and cross-region replication: data out to the internet and S3 cross-region replication are billed per-GB, so cache at CloudFront (cheaper egress) and only replicate what compliance truly requires. The meta-lesson: data transfer is often invisible until the bill, so trace it with Cost Explorer's transfer dimension.",
    "fs": "Calls out cross-AZ, NAT egress (fix with endpoints), and internet/cross-region transfer (fix with CloudFront/selective replication).",
    "fj": "Assumes data transfer is free or trivial and only looks at compute/storage line items.",
    "level": "Sr",
    "followups": [
      {
        "q": "Co-locating in one AZ cuts cross-AZ transfer but hurts availability. How do you resolve that tension?",
        "a": "Decide per data path against RTO/RPO: keep extremely chatty, latency-sensitive, recoverable components (e.g. a cache and its app) AZ-local, but keep the durable system of record multi-AZ. Accept cross-AZ cost where availability is non-negotiable, save it where traffic is high-volume and the component is replaceable. It's an explicit cost-vs-reliability tradeoff, not a blanket rule."
      },
      {
        "q": "Why does CloudFront in front of S3 reduce egress cost, not just add a CDN layer?",
        "a": "Data transfer from S3 to CloudFront is free, and CloudFront's per-GB egress to the internet is cheaper than direct S3 egress, plus caching serves repeat requests from the edge without re-hitting the origin. So for internet-facing content, fronting with CloudFront cuts both the origin fetch volume and the egress unit price. It's a cost lever, not only a latency one."
      },
      {
        "q": "How would you actually find where data-transfer cost is hiding before optimizing?",
        "a": "Use the Cost and Usage Report (CUR) and Cost Explorer filtered to the data-transfer usage types, which break out cross-AZ, inter-region, and internet-out separately, and enable VPC Flow Logs to attribute traffic to specific resources/paths. Transfer cost is opaque in the summary bill, so you query the usage-type dimension to see which flow dominates — then fix the biggest one first."
      }
    ]
  },
  {
    "q": "How do the Well-Architected Reliability and Cost pillars pull against each other in a multi-AZ data platform, and how do you decide?",
    "a": "Reliability pushes toward redundancy that costs money: multi-AZ RDS/Aurora, a NAT Gateway per AZ, cross-AZ replicas, and DLQs with retries. Cost Optimization pushes the opposite: single-AZ where tolerable, Spot for interruptible compute, lifecycle tiering, and avoiding cross-AZ chatter. The senior move is to set the tradeoff per workload against an explicit RTO/RPO and business impact: a payments path justifies multi-AZ everything, while a nightly batch that can rerun can ride single-AZ Spot. Make the tradeoff visible and reversible (IaC, tags, budgets) rather than reflexively maxing one pillar. Well-Architected's value is forcing that conversation, not prescribing one answer.",
    "fs": "Sets redundancy spend against explicit RTO/RPO and business impact per workload instead of maxing one pillar.",
    "fj": "Either makes everything multi-AZ 'for safety' or cuts all redundancy to save money, with no RTO/RPO reasoning.",
    "level": "Sr",
    "followups": [
      {
        "q": "How do RTO and RPO concretely translate into which redundancy you buy?",
        "a": "RPO (tolerable data loss) drives replication: near-zero RPO needs synchronous multi-AZ or continuous backups; hours of RPO can ride async replication or snapshots. RTO (tolerable downtime) drives failover speed: seconds-RTO needs hot standby/Aurora auto-failover; hours-RTO allows restore-from-backup. You buy exactly the redundancy each number demands — that's how the objectives bound spend."
      },
      {
        "q": "Give a case where spending more on reliability is actually the cheaper decision.",
        "a": "When the cost of downtime dwarfs the redundancy cost — e.g. a revenue-generating checkout path where an hour of outage loses far more than multi-AZ + standby costs per year. There, single-AZ 'savings' are a false economy once you price expected downtime. The decision is total cost including risk-weighted outage cost, not just the infrastructure line item."
      },
      {
        "q": "How do you keep these tradeoffs from silently drifting after the initial design?",
        "a": "Encode them as IaC with tags marking each workload's tier/RTO/RPO, set budgets and alarms per tier, and re-review periodically with the Well-Architected Tool so a workload's reliability posture is visible and auditable. Drift happens when someone single-AZs a critical path or over-provisions a batch job unnoticed. Making the tradeoff a versioned, tagged artifact lets you catch and reverse it."
      }
    ]
  },
  {
    "q": "What does Amazon Athena Federated Query let you do, and when is it the wrong tool?",
    "a": "Athena Federated Query uses Lambda-based connectors to run SQL across non-S3 sources — DynamoDB, RDS/Aurora, Redshift, CloudWatch, JDBC databases — and even join them with S3 data in one query, without first ETL-ing everything into the lake. It's great for ad-hoc cross-source exploration and avoiding a pipeline for occasional questions. It's the wrong tool for high-frequency or latency-sensitive queries: each connector invocation pulls data through Lambda with limited pushdown, so it can be slow and load the source database. For repeated heavy analytics, materialize the data into partitioned Parquet in S3 instead and query that.",
    "fs": "Knows federated query joins external sources via Lambda connectors but warns of weak pushdown/source load for hot paths.",
    "fj": "Points federated query at a production OLTP database for frequent dashboards and hammers the source.",
    "level": "Sr",
    "followups": [
      {
        "q": "How does limited predicate pushdown actually hurt a federated query's performance?",
        "a": "If a connector can't push filters/aggregations to the source, it pulls large result sets through Lambda into Athena to filter there — moving far more data than needed and loading the source DB. Pushdown support varies by connector, so a `WHERE`-heavy query that looks selective can still scan the whole table. Check the connector's pushdown capabilities before using it on big sources."
      },
      {
        "q": "What's the blast radius of pointing federated query at a production OLTP database?",
        "a": "Each query opens connections and runs reads against the live DB, so frequent or heavy federated queries can exhaust the connection pool and add load that degrades transactional performance — your analytics directly impact production. Mitigate by querying a read replica, rate-limiting, or materializing to S3. Never put a busy dashboard's federated query on the primary OLTP instance."
      },
      {
        "q": "When is the right move to abandon federation and build a real pipeline?",
        "a": "Once the same cross-source query runs often, needs low latency, or scans large volumes, materialize the source into partitioned Parquet in S3 (via a scheduled Glue/zero-ETL job) and query that with normal Athena. Federation is for occasional, low-volume joins; recurring heavy analytics belong on a curated copy. The trigger is frequency x volume crossing into hot-path territory."
      }
    ]
  },
  {
    "q": "A junior asks 'what's the simplest way to get started with AI on AWS without training a model?' Give the honest framing.",
    "a": "For most teams the entry point is Amazon Bedrock: a serverless API over hosted foundation models (Claude, Llama, Titan, and others) where you call an endpoint, scale to zero when idle, and add managed RAG via Knowledge Bases — no GPUs, training, or model hosting. For ready-made capabilities you can also use the AI services (Comprehend, Textract, Transcribe, Rekognition) that wrap pretrained models behind a simple API. You only need SageMaker when you must train, fine-tune deeply, or host custom models on your own infrastructure. Start with Bedrock or an AI service; reach for SageMaker when managed inference genuinely can't meet the requirement.",
    "fs": "Routes 'AI without training' to Bedrock/managed AI services and reserves SageMaker for custom train/fine-tune/host.",
    "fj": "Assumes any AI feature requires standing up SageMaker and training a model from scratch.",
    "level": "Jr",
    "followups": [
      {
        "q": "When is a purpose-built AI service like Textract or Comprehend better than prompting a Bedrock LLM?",
        "a": "For a well-defined, structured task — OCR/form extraction (Textract), entity/sentiment (Comprehend), transcription (Transcribe) — the purpose-built service is cheaper, lower-latency, more deterministic, and pre-tuned for that job than a general LLM prompt. Reach for an LLM when you need open-ended reasoning, generation, or to combine tasks. Match the narrowest tool that solves the specific task."
      },
      {
        "q": "The junior's prototype works but costs balloon in production. What did they likely miss with Bedrock?",
        "a": "On-demand per-token pricing is great for prototypes but scales linearly with usage, so high volume gets expensive; they likely also sent oversized prompts/contexts every call. Fix with prompt/context trimming, caching, a smaller model where adequate, and Provisioned Throughput for steady high load. The honest framing: easy to start, but token economics and right-sizing matter at scale."
      },
      {
        "q": "What's the honest limitation of 'just use Bedrock' for a task needing deep domain specialization?",
        "a": "A general foundation model may lack domain knowledge or exact output behavior, and RAG only injects retrieved context, not new skills. If RAG and prompt engineering don't get there, you fine-tune (still possible on Bedrock for supported models) or move to SageMaker for a custom model. So 'no training needed' holds for most cases but not deep specialization — be honest about that ceiling."
      }
    ]
  }
]
