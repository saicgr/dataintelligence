[
  {
    "kind": "choice",
    "tag": "Why won't it compile · Jr",
    "level": "Jr",
    "tool": "Rust",
    "q": "This won't compile — use after move. What's the reason?",
    "lang": "rust",
    "lines": [
      "let s = String::from(\"hello\");",
      "let t = s;",
      "println!(\"{}\", s); // E0382"
    ],
    "opts": [
      { "t": "s was moved into t; String is not Copy", "ok": true },
      { "t": "println! requires a mutable reference", "ok": false, "why": "println! borrows immutably; mutability is unrelated." },
      { "t": "String literals can't be bound twice", "ok": false, "why": "The literal is fine; the issue is ownership transfer, not the literal." },
      { "t": "t must be the same type as s first", "ok": false, "why": "Both are String — type identity is not the problem here." }
    ],
    "why": "Assigning `s` to `t` moves ownership; `String` doesn't implement `Copy`, so `s` is no longer valid after the move (E0382). Fix: borrow with `&s`, or call `s.clone()` to give `t` an independent copy.",
    "fj": "\"Just use t — s is gone.\"",
    "fs": "\"Move = transfer of ownership; borrow or clone to keep both alive.\""
  },
  {
    "kind": "choice",
    "tag": "Pick the fix · Jr",
    "level": "Jr",
    "tool": "Rust",
    "q": "Compiler: E0382 — use of moved value `s`. Pick the fix.",
    "lang": "rust",
    "lines": [
      "fn takes_ownership(s: String) {}",
      "",
      "let s = String::from(\"hi\");",
      "takes_ownership(s);",
      "println!(\"{}\", s); // E0382"
    ],
    "opts": [
      { "t": "Make s mutable: let mut s", "ok": false, "why": "Mutability doesn't prevent a move; `mut` controls write access, not ownership." },
      { "t": "Wrap the call in unsafe { }", "ok": false, "why": "unsafe doesn't bypass move semantics; ownership rules are compile-time." },
      { "t": "Pass a borrow: takes_ownership(&s)", "ok": true },
      { "t": "Use a raw pointer: &raw s", "ok": false, "why": "Raw pointers bypass safety guarantees and are not the idiomatic fix here." }
    ],
    "why": "Passing `s` by value transfers ownership into `takes_ownership`, leaving `s` invalid afterward (E0382). Borrowing with `&s` (or calling `s.clone()` if the callee truly needs ownership) keeps `s` alive for the later `println!`.",
    "fj": "\"Add unsafe — that always works.\"",
    "fs": "\"Borrow with `&s`; or clone if the callee must own the value.\""
  },
  {
    "kind": "choice",
    "tag": "Why won't it compile · Mid",
    "level": "Mid",
    "tool": "Rust",
    "q": "Why does the compiler reject this borrow pattern (E0502)?",
    "lang": "rust",
    "lines": [
      "let mut v = vec![1, 2, 3];",
      "let first = &v[0];   // immutable borrow",
      "v.push(4);           // mutable borrow",
      "println!(\"{}\", first); // E0502"
    ],
    "opts": [
      { "t": "Vec::push returns a bool, not ()", "ok": false, "why": "push returns () — the return type is unrelated to the borrow error." },
      { "t": "first must be declared mutable to index a Vec", "ok": false, "why": "Indexing for reading never requires a mutable reference." },
      { "t": "An immutable borrow of v lives until println!; push needs a mutable borrow — they overlap (E0502)", "ok": true },
      { "t": "Vec indexing always clones so there's no borrow at all", "ok": false, "why": "Indexing yields a reference, not a clone, so the borrow is real." }
    ],
    "why": "The immutable borrow `first` stays alive until its last use (`println!`). `v.push` needs a mutable borrow in between — Rust forbids overlapping mutable and immutable borrows (E0502). Fix: use `first` before pushing, or copy the value out: `let first = v[0];`.",
    "fj": "\"Just reorder the lines — order doesn't matter to the borrow checker.\"",
    "fs": "\"The borrow extends to its last use; push needs exclusive access — they can't overlap.\""
  },
  {
    "kind": "choice",
    "tag": "Pick the fix · Mid",
    "level": "Mid",
    "tool": "Rust",
    "q": "HashMap get + insert causes a double-mutable-borrow. Pick the idiomatic fix.",
    "lang": "rust",
    "lines": [
      "let mut map: HashMap<&str, u32>",
      "    = HashMap::new();",
      "// ERROR: can't borrow `map` mutably",
      "// twice in the same scope",
      "if map.get(\"hits\").is_none() {",
      "    map.insert(\"hits\", 0);",
      "}"
    ],
    "opts": [
      { "t": "Use map.entry(\"hits\").or_insert(0)", "ok": true },
      { "t": "Clone the map before get, insert into the clone", "ok": false, "why": "Cloning the whole map just to insert one key is wasteful and misses the point." },
      { "t": "Wrap map in RefCell to defer borrow checks to runtime", "ok": false, "why": "RefCell adds runtime overhead and still panics on simultaneous borrows." },
      { "t": "Use unsafe to bypass the borrow check", "ok": false, "why": "unsafe doesn't fix the logical race; the entry API is the safe, zero-cost solution." }
    ],
    "why": "The `entry` API takes exactly one mutable borrow for the entire check-and-insert operation, avoiding the double-borrow. `entry(key).or_insert(default)` inserts the default only when the key is absent — idiomatic, efficient, and compile-safe.",
    "fj": "\"Just use contains_key then insert — two lines, easy.\"",
    "fs": "\"entry().or_insert() is one borrow, one lookup — that's the Rust way.\""
  },
  {
    "kind": "choice",
    "tag": "Pick the fix · Mid",
    "level": "Mid",
    "tool": "Rust",
    "q": "E0515: returning a reference to a local. Which fix compiles?",
    "lang": "rust",
    "lines": [
      "fn make_greeting() -> &str {",
      "    let s = String::from(\"hello\");",
      "    &s  // E0515: `s` dropped here",
      "}"
    ],
    "opts": [
      { "t": "Change return type to &'static str and use a literal", "ok": false, "why": "Only works if the value is truly static; a heap-allocated String can't be 'static." },
      { "t": "Add #[allow(dangling_references)] to suppress the error", "ok": false, "why": "No such lint exists; E0515 is a hard compiler error, not a warning." },
      { "t": "Return an owned String instead of a reference", "ok": true },
      { "t": "Wrap &s in Box::new(&s) to heap-allocate the reference", "ok": false, "why": "Boxing a reference doesn't extend the referenced value's lifetime; `s` still drops." }
    ],
    "why": "`s` is a local that is dropped when the function returns; returning `&s` would be a dangling reference (E0515). The fix is to return an owned `String` — transfer ownership to the caller rather than borrowing a value that no longer exists.",
    "fj": "\"Just add a lifetime annotation 'a and it'll work.\"",
    "fs": "\"Local variables die at the closing brace; give the caller ownership instead of a borrow.\""
  },
  {
    "kind": "choice",
    "tag": "Why won't it compile · Mid",
    "level": "Mid",
    "tool": "Rust",
    "q": "Mutating a Vec while iterating over it — which statement is correct?",
    "lang": "rust",
    "lines": [
      "let mut v = vec![1, 2, 3];",
      "for x in &v {",
      "    v.push(*x * 2); // E0502",
      "}"
    ],
    "opts": [
      { "t": "The for loop holds an immutable borrow of v; push needs a mutable borrow — they overlap (E0502)", "ok": true },
      { "t": "push is not available on Vec<i32>; use append instead", "ok": false, "why": "push works fine on Vec<i32>; the error is about borrow conflicts, not the method." },
      { "t": "x must be dereferenced before the loop, not inside it", "ok": false, "why": "Dereferencing inside the loop is valid; the problem is the concurrent borrow." },
      { "t": "for loops always move the collection; use iter() explicitly", "ok": false, "why": "for x in &v already calls .iter() implicitly; the borrow conflict is the real issue." }
    ],
    "why": "The `for x in &v` loop borrows `v` immutably for its entire duration. `v.push(...)` inside the loop requests a mutable borrow — Rust disallows the overlap (E0502). Fix: collect indices or values first, then mutate after the loop.",
    "fj": "\"Use iter_mut() and it'll let you push inside the loop.\"",
    "fs": "\"Collect what you need first, then push — you can't hold a shared borrow while mutating.\""
  },
  {
    "kind": "choice",
    "tag": "Pick the fix · Sr",
    "level": "Sr",
    "tool": "Rust",
    "q": "Rc<T> across threads — compiler rejects it. What's the correct fix?",
    "lang": "rust",
    "lines": [
      "use std::rc::Rc;",
      "let data = Rc::new(vec![1, 2, 3]);",
      "let d2 = Rc::clone(&data);",
      "std::thread::spawn(move || {",
      "    println!(\"{:?}\", d2);",
      "}); // error: Rc<Vec<i32>> is not Send"
    ],
    "opts": [
      { "t": "Wrap Rc in a Mutex to make it Send", "ok": false, "why": "Mutex<Rc<T>> is still not Send because Rc's non-atomic ref count is inside the Mutex." },
      { "t": "Replace Rc with Arc (atomic reference counting)", "ok": true },
      { "t": "Mark the thread closure with #[allow(non_send)]", "ok": false, "why": "No such attribute exists; Send is a marker trait enforced at compile time." },
      { "t": "Clone the Vec and move the clone — Rc is unnecessary here if you clone", "ok": false, "why": "Cloning the Vec works only if you don't need shared ownership; the question asks for the Rc fix." }
    ],
    "why": "`Rc<T>` uses non-atomic reference counting and explicitly implements `!Send`, so it cannot be moved across thread boundaries. `Arc<T>` (atomic reference counted) uses atomic operations for its count and is `Send + Sync`, making it the drop-in replacement for multi-threaded scenarios.",
    "fj": "\"Just add unsafe impl Send for Rc<Vec<i32>>.\"",
    "fs": "\"Rc = single-thread only; swap to Arc the moment a thread boundary appears.\""
  },
  {
    "kind": "choice",
    "tag": "Pick the idiomatic version · Sr",
    "level": "Sr",
    "tool": "Rust",
    "q": "Propagating errors idiomatically — which version is most idiomatic Rust?",
    "lang": "rust",
    "lines": [
      "// Version A",
      "fn read_port(s: &str) -> u16 {",
      "    s.parse::<u16>().unwrap()",
      "}",
      "",
      "// Version B",
      "fn read_port(s: &str)",
      "    -> Result<u16, std::num::ParseIntError> {",
      "    let n = s.parse::<u16>()?;",
      "    Ok(n)",
      "}"
    ],
    "opts": [
      { "t": "Version A — unwrap is cleaner and avoids verbose Result types", "ok": false, "why": "unwrap() panics on bad input; it's only acceptable in tests or provably-safe one-liners." },
      { "t": "Version B — ? propagates the error to the caller instead of panicking", "ok": true },
      { "t": "Both are equivalent; the compiler optimises unwrap into ?", "ok": false, "why": "They have different behavior: unwrap panics, ? returns Err to the caller." },
      { "t": "Neither; use match on the Result to handle both arms explicitly", "ok": false, "why": "match is valid but verbose for simple propagation; ? is the idiomatic shorthand." }
    ],
    "why": "The `?` operator desugars to an early `return Err(e.into())` on failure, propagating the error to the caller without panicking. Version B lets callers decide how to handle parse failures; Version A crashes the process on bad input, which is rarely appropriate in library or production code.",
    "fj": "\"unwrap() is fine — the input is always valid in practice.\"",
    "fs": "\"Use `?` to propagate; reserve unwrap for tests or provably-safe values.\""
  }
]
