[
  {
    "kind": "choice",
    "tag": "Spot the bug · Jr",
    "level": "Jr",
    "tool": "Go",
    "q": "This code panics at runtime. What is the bug?",
    "lang": "go",
    "lines": [
      "var counts map[string]int",
      "counts[\"hits\"] = 1"
    ],
    "opts": [
      { "t": "counts is a nil map; writing to it panics", "ok": true },
      { "t": "map[string]int should be map[string]float64", "ok": false, "why": "The type is fine; the problem is the map was never initialized." },
      { "t": "The key must be an int, not a string", "ok": false, "why": "String keys are valid; the panic is caused by writing to an uninitialized nil map." }
    ],
    "why": "var m map[K]V declares a nil map. Reading from it is safe (returns zero value), but any write triggers a runtime panic. Fix: counts := make(map[string]int).",
    "fj": "\"The type must be wrong.\"",
    "fs": "\"var only declares the header — make allocates the hash table; nil map writes always panic.\""
  },
  {
    "kind": "choice",
    "tag": "Pick the fix · Jr",
    "level": "Jr",
    "tool": "Go",
    "q": "This goroutine leaks because it blocks forever. Pick the fix.",
    "lang": "go",
    "lines": [
      "func generate() <-chan int {",
      "  ch := make(chan int)",
      "  go func() {",
      "    for i := 0; ; i++ {",
      "      ch <- i",
      "    }",
      "  }()",
      "  return ch",
      "}"
    ],
    "opts": [
      { "t": "Pass a context.Context and select on ctx.Done() to stop the goroutine", "ok": true },
      { "t": "Close ch inside the goroutine after the loop", "ok": false, "why": "The loop is infinite, so close is unreachable; the goroutine still leaks." },
      { "t": "Make the channel buffered: make(chan int, 100)", "ok": false, "why": "A buffer delays but does not prevent the leak; the goroutine still blocks when the buffer fills and the caller stops reading." },
      { "t": "Add time.Sleep(time.Second) inside the loop", "ok": false, "why": "Sleep slows production but does not provide a way to stop the goroutine." }
    ],
    "why": "Without a cancellation signal the goroutine blocks on ch <- i forever once the caller stops reading. Accepting a context.Context and selecting on ctx.Done() lets the caller cancel cleanly.",
    "fj": "\"Just buffer the channel bigger.\"",
    "fs": "\"Any long-running goroutine needs a stop signal — context is the idiomatic way.\""
  },
  {
    "kind": "choice",
    "tag": "Spot the bug · Mid",
    "level": "Mid",
    "tool": "Go",
    "q": "All goroutines print the same number. What is the bug? (Go < 1.22)",
    "lang": "go",
    "lines": [
      "for i := 0; i < 3; i++ {",
      "  go func() {",
      "    fmt.Println(i)",
      "  }()",
      "}"
    ],
    "opts": [
      { "t": "The closure captures i by reference; by the time goroutines run, i is 3", "ok": true },
      { "t": "Goroutines run before the loop increments i", "ok": false, "why": "Goroutines are scheduled concurrently; the loop usually finishes first, so all see the final value." },
      { "t": "fmt.Println is not goroutine-safe", "ok": false, "why": "fmt.Println is safe for concurrent use; the data race is on i, not on Println." }
    ],
    "why": "In Go < 1.22, all loop iterations share one i variable. The closures capture the variable itself, not a snapshot. When the goroutines execute, i has advanced to 3. Fix: pass i as an argument — go func(n int) { fmt.Println(n) }(i).",
    "fj": "\"Goroutines print as they start, so they should see 0, 1, 2.\"",
    "fs": "\"The closure captures the variable, not the value — shadow it or pass it as an arg.\""
  },
  {
    "kind": "choice",
    "tag": "Spot the bug · Mid",
    "level": "Mid",
    "tool": "Go",
    "q": "Modifying b unexpectedly changes a. What is wrong?",
    "lang": "go",
    "lines": [
      "a := []int{1, 2, 3, 4}",
      "b := a[:2]    // len=2, cap=4",
      "b = append(b, 99)",
      "fmt.Println(a) // [1 2 99 4]"
    ],
    "opts": [
      { "t": "b shares a's backing array; append writes into a's unused capacity", "ok": true },
      { "t": "append always allocates a new array, so this is a separate bug", "ok": false, "why": "append only allocates when len == cap; here cap=4 > len=2, so no allocation occurs." },
      { "t": "The slice header for a is copied, making them independent", "ok": false, "why": "Slice headers are independent, but both still point to the same underlying array." }
    ],
    "why": "b := a[:2] gives b len=2 but cap=4 pointing to the same array. append finds free capacity and writes 99 at index 2, which is a[2]. Use a[:2:2] to set cap=len, forcing a copy on append.",
    "fj": "\"append always makes a copy.\"",
    "fs": "\"Three-index slice a[:2:2] caps b's capacity, guaranteeing append allocates a fresh array.\""
  },
  {
    "kind": "choice",
    "tag": "Pick the fix · Mid",
    "level": "Mid",
    "tool": "Go",
    "q": "The error check misses wrapped errors in production. Pick the fix.",
    "lang": "go",
    "lines": [
      "var ErrNotFound = errors.New(\"not found\")",
      "",
      "err := fetchRecord(id)",
      "if err == ErrNotFound {",
      "  // handle",
      "}"
    ],
    "opts": [
      { "t": "Replace == with errors.Is(err, ErrNotFound)", "ok": true },
      { "t": "Use err.Error() == \"not found\" instead", "ok": false, "why": "String comparison breaks if the message ever changes and still ignores wrapping." },
      { "t": "Change ErrNotFound to a struct type so == works", "ok": false, "why": "Struct comparison still fails when the error is wrapped with fmt.Errorf(\"%w\", err)." }
    ],
    "why": "When fetchRecord wraps the error — e.g., fmt.Errorf(\"db: %w\", ErrNotFound) — direct == comparison returns false because the pointers differ. errors.Is traverses the Unwrap chain and correctly matches the sentinel.",
    "fj": "\"== is fine for sentinel errors.\"",
    "fs": "\"Wrapping is idiomatic in Go; always use errors.Is so the check survives wrapping.\""
  },
  {
    "kind": "choice",
    "tag": "Spot the bug · Mid",
    "level": "Mid",
    "tool": "Go",
    "q": "This loop leaks file descriptors. What is the bug?",
    "lang": "go",
    "lines": [
      "func readAll(paths []string) {",
      "  for _, p := range paths {",
      "    f, _ := os.Open(p)",
      "    defer f.Close()",
      "    process(f)",
      "  }",
      "}"
    ],
    "opts": [
      { "t": "defer is scoped to the function, not the loop; all files stay open until readAll returns", "ok": true },
      { "t": "os.Open should be os.Create to get write access", "ok": false, "why": "Open is correct for reading; the bug is defer accumulating across iterations." },
      { "t": "process(f) should run inside a goroutine", "ok": false, "why": "Concurrency is irrelevant here; the leak happens regardless of whether process is concurrent." }
    ],
    "why": "defer runs when the surrounding function returns, not at the end of each loop iteration. With many paths, all file descriptors accumulate until readAll exits. Fix: wrap the body in an IIFE — func() { f, _ := os.Open(p); defer f.Close(); process(f) }() — or call f.Close() explicitly.",
    "fj": "\"defer closes the file after each iteration.\"",
    "fs": "\"defer is function-scoped; wrap the body in a closure or call Close() directly after process.\""
  },
  {
    "kind": "choice",
    "tag": "Predict the output · Sr",
    "level": "Sr",
    "tool": "Go",
    "q": "What does this program print?",
    "lang": "go",
    "lines": [
      "ch := make(chan string, 1)",
      "go func() {",
      "  time.Sleep(5 * time.Second)",
      "  ch <- \"done\"",
      "}()",
      "select {",
      "case msg := <-ch:",
      "  fmt.Println(msg)",
      "case <-time.After(1 * time.Second):",
      "  fmt.Println(\"timeout\")",
      "}"
    ],
    "opts": [
      { "t": "timeout", "ok": true },
      { "t": "done", "ok": false, "why": "The goroutine sleeps 5 s but time.After fires after 1 s, so the timeout case wins." },
      { "t": "Deadlock — select blocks both cases", "ok": false, "why": "select picks whichever case is ready first; time.After(1s) fires well before the 5 s goroutine." },
      { "t": "Empty string — ch receives before select", "ok": false, "why": "The goroutine hasn't sent yet when select runs; it sends after 5 s." }
    ],
    "why": "select blocks until one of its cases is ready. time.After(1s) fires first because the goroutine sleeps for 5 s. The timeout arm runs and prints \"timeout\".",
    "fj": "\"The goroutine sends first, so it prints done.\"",
    "fs": "\"select races both cases; time.After wins because 1 s < 5 s — classic timeout pattern.\""
  },
  {
    "kind": "choice",
    "tag": "Pick the fix · Sr",
    "level": "Sr",
    "tool": "Go",
    "q": "The unbuffered send deadlocks with no receiver. Pick the fix.",
    "lang": "go",
    "lines": [
      "func notify() {",
      "  ch := make(chan int)",
      "  ch <- 42",
      "  fmt.Println(\"sent\")",
      "}"
    ],
    "opts": [
      { "t": "Make the channel buffered: make(chan int, 1)", "ok": true },
      { "t": "Move ch <- 42 into a goroutine: go func() { ch <- 42 }()", "ok": false, "why": "Moving the send to a goroutine unblocks notify, but the goroutine leaks because nobody ever receives from ch." },
      { "t": "Replace ch <- 42 with close(ch)", "ok": false, "why": "close signals closure, not a value send; it does not fix the original intent of sending 42." }
    ],
    "why": "An unbuffered channel blocks the sender until a receiver is ready. With no receiver, the goroutine deadlocks. A buffer of 1 lets the send complete immediately; add a receiver elsewhere when the value must be consumed.",
    "fj": "\"Move the send to a goroutine — that unblocks the main function.\"",
    "fs": "\"Buffer size 1 decouples sender from receiver; a goroutine-only fix just moves the leak.\""
  }
]
